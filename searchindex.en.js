var relearn_searchindex = [
  {
    "breadcrumb": "",
    "content": "Fisk AI turns any fisk-based command-line application into an LLM agent. It introspects the app’s command tree, exposes the allowed commands as tools, and runs an agent loop against the Anthropic API that calls those commands to satisfy a prompt.\nNo glue code. If your CLI is built with Fisk, Fisk AI can turn it into a purpose-built agentic harness.\nThe main focus is on safety and determinism. This harness sets itself apart by what it leaves out rather than by a long list of features.\nIt is designed in particular to complement Choria App Builder. App Builder lets you define a command line application declaratively in YAML, and because it is built on fisk, any App Builder application can be introspected and driven by fisk-ai. Together they let you define a strict, purpose-built set of tools in a YAML file and expose exactly those to an agent, without writing or compiling any code: App Builder describes the commands, fisk-ai’s configuration selects which of them the agent may use and how it should behave.\nSmall and Focused\rDeliberately restricted AI Harness to create safe, deterministic, AI Agents for Operations use\rSafety first\rAgents can only interact with the tools provided. Using tags in the tool the Harness provides Human-in-the-Loop safety guardrails.\rUtilities from Prose\rSupports creating Shell hosted utilities that have Agentic abilities and zero code - describe a flow, supply the tools and have it dynamically react\nBuilt-in tools for HITL and Memory complement those from Fisk.\nSupports Local Models\rHost local models using ollama, llama.cpp, LM Studio and any other provider that supports the Anthropic API.\rSingle binary RAG\rFisk AI includes a zero dependency, single binary, RAG system to create local knowledge bases for your Markdown data.\nZero to full-text and vector search in under 1 minute for 1000s of Markdown files.\nLoves App Builder\rFisk AI is tailor-made to create AI Agents using just YAML files and utilities you already have by targeting App Builder as tool provider\nApp Builder can create tools with strict guardrails, input validation and integration with secret providers like 1Password.\nDeterministic AI Harnesses are only a few YAML files away.\nUse cases I’ve used this with success in numerous problem areas:\nPull request review - do not want to give the LLM access to gh command as it will try to do a lot of things it is not supposed to. So I wrap gh with App Builder giving it commands such as “abt pr triage” which will apply the correct label Built a DMARC email parsing system, do not want to give it shell access, turned a set of SKILLs into a standalone agent with just the tools it needs, no more randomly calling whatever the LLM wants. Once while performing this task Claude tried Bashisms on my Zsh and did rm -rf /, now that is impossible Created various MCP servers to plug into Claude Code with strict control over how the tools are called Tool to interpret GitHub repository stats - being able to just ask questions to interpret the data without fear of complex Bash callouts really helps Drives complex testing scenarios against an API-driven Cluster Manager Local knowledge base indexing Open Source project websites for local RAG based discussions In all these cases the best solution is to apply understanding and language interpretation to the problem, but doing so safely and repeatedly from within Claude Code is difficult because that favors running Bash commands - and not always the same ones to solve the same problem.\nWrapping CLI tools like gh using App Builder and then only giving it these deterministic tools means we can get much better outcomes from LLM based utilities.\nShell example Here we use the nats command line utility to create a Stream Management Agent.\n# agent.yaml # Command to introspect and expose as an agent application_path: /usr/bin/nats include: # Include the entire Stream and Consumer command set nothing else tools: - ^stream - ^consumer harness: # Allow the LLM to prompt us for information if needed human_in_the_loop: enabled: true # Map nats command tags of impact to HITL prompts - any # command that changes the system requires human approval confirm_tags: [impact:rw] llm: model: claude-haiku-4-5-20251001 budget: max_tokens: 100000 max_iterations: 50 system_prompt: | You manage NATS JetStream Streams using tools. Assist users with questions related to Streams and Consumers in their JetStream account. Above we create an agent with various Stream and Consumer management utilities as tools, here we use it on the CLI:\nWe can now prompt this agent knowing it can only interact with these nats commands as tools.\nHow many consumers does the biggest stream (by messages) have? Show their names and when last they had activity",
    "description": "Turn any Fisk CLI application into a safe LLM agent",
    "tags": [],
    "title": "Introduction",
    "uri": "/fisk-ai/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "The main feature of Fisk AI is creating AI agents from CLI tools written with Fisk.\nAny tool built with Fisk, such as the nats or choria CLI, or an application made with Choria Application Builder, can be turned into an AI agent.\nFisk AI creates capable systems that use the abilities LLMs have, such as reasoning and text interpretation, in a safe and deterministic manner.\nBuilding an agent resembles building a CLI tool: describe the goals, give broad guidance, supply tools to interact with the world deterministically, then run it on a shell like any other utility.\nInstallation On a Mac you can install fisk-ai using homebrew:\nbrew tap choria-io/tap brew install choria-io/tap/fisk-ai Other Operating System users can download the latest release from the releases page.\nBasic agent This example builds an AI agent that speaks in cowsay bubbles.\nThe steps make a quick CLI application using App Builder and then drive it in various ways using the LLM.\nThe example needs an Anthropic API key, the cowsay application (try brew install cowsay) and fisk-ai installed.\nCreating a CLI tool This example uses Choria Application Builder to create a basic CLI tool that can say and think. Any command line tool built with Fisk works.\nFirst create an ABTaskFile:\nname: cowsay description: Tools for the Cowsay LLM Agent author: fisk-ai@choria.io commands: - name: say description: Say something using a talking cow, does not accept emoji type: exec arguments: - name: message description: The message to send to the terminal required: true validate: is_shellsafe(value) command: | {{ default .Config.Cowsay \"cowsay\" }} {{ .Arguments.message | escape }} - name: think description: Think something using a thinking cow, does not accept emoji type: exec arguments: - name: message description: The message to send to the terminal required: true validate: is_shellsafe(value) command: | cowthink {{ .Arguments.message | escape }} Now install appbuilder:\n$ brew tap choria-io/tap $ brew install appbuilder Then confirm the CLI tool works:\n$ abt usage: abt [\u003cflags\u003e] \u003ccommand\u003e [\u003cargs\u003e ...] Tools for the Cowsay LLM Agent Help: https://choria-io.github.io/appbuilder Commands: help [\u003ccommand\u003e...] say \u003cmessage\u003e think \u003cmessage\u003e $ abt say 'Hello AI' __________ \u003c Hello AI \u003e ---------- \\ ^__^ \\ (oo)\\_______ (__)\\ )\\/\\ ||----w | || || Creating an LLM agent Turning this CLI into an LLM agent needs an agent.yaml file.\n# Command to introspect and expose as an agent application_path: /opt/homebrew/bin/abt harness: # Allow the LLM to prompt us for information if needed human_in_the_loop: enabled: true llm: # Choose a Model and set safety budgets model: claude-haiku-4-5-20251001 budget: max_tokens: 100000 max_iterations: 50 # We want a cow joke machine! system_prompt: | Tell jokes using Cows! You have tools that can render a cow saying \u003c 120 character sentences, when asked use the tools to tell funny jokes. You tell cow jokes, no other kinds of jokes, strictly jokes about cows. If asked to tell non cow jokes, refuse and show no joke. Keep narration short, just stick to the jokes, don't say what you are doing or planning to do, just do it and don't repeat the joke Run the agent after setting the API key:\n$ export ANTHROPIC_API_KEY=\".....\" $ fisk run --tool-output --no-tui 'tell me a joke ' -\u003e say -- Why did th...space? \u003c- ______________________________ \u003c Why did the cow go to space? \u003e ------------------------------ \\ ^__^ \\ (oo)\\_______ (__)\\ )\\/\\ ||----w | || || -\u003e think -- To visit t...oooon! \u003c- ______________________ ( To visit the Moooon! ) ---------------------- o ^__^ o (oo)\\_______ (__)\\ )\\/\\ ||----w | || || There you go! A classic cow joke for you! Run summary: model=claude-haiku-4-5-20251001 llm_calls=2 tool_calls=2 tokens=3536/113 thinking=0 latency=3.613s The default is a running TUI. To make the output easy to show here, the run passes --no-tui and shows the tool call output with --tool-output.\nNow ask about a cat joke:\n$ fisk run 'tell me a joke about a cat' I appreciate the request, but I only tell jokes about cows! I'm strictly a cow joke specialist. If you'd like to hear some funny cow jokes instead, I'd be happy to moo-ve right into those for you! Run summary: model=claude-haiku-4-5-20251001 llm_calls=1 tool_calls=0 tokens=1632/54 thinking=0 latency=1.341s Running the agent The agent runs in one of these modes:\nA shell script style output, plain text to STDOUT with an exit at the end of the task A TUI for interaction, optionally continuing to chat with the agent after the main task completes Hosted behind a channel, taking work from a queue or serving its tools to other agents TUI The TUI mode is the default: a visual runner with hot-keys to show or hide thinking and tool output, and scrolling up and down the session history. A chat box can optionally be enabled to continue a session.\nIn the TUI press the ? key to get interactive help.\nChat after turn In the TUI mode the chat bar opens once the prompt is processed, instead of exiting, for follow up questions related to the session. Every full-screen run works this way; --no-tui answers one prompt and exits, since it has no bar to open.\nType a follow-up and press Enter to send it; Ctrl-D ends the session, Ctrl-C aborts it. Up/Down recall this session’s earlier follow-ups. Alt-Enter (Option-Enter) moves to the next line rather than send. Ctrl-L empties the transcript on screen and leaves the conversation and any half-typed follow-up alone, where /clear does the opposite and drops the conversation while leaving the scrollback.\nShell mode The TUI is turned off with --no-tui, and the system falls back to a simple terminal output format suitable for scripting.\nThe model’s prose is markdown: both the final answer and any mid-conversation updates. When stdout is a terminal it is rendered for readability with a style matched to the terminal background; when stdout is piped or redirected, the raw markdown is written so the result stays free of ANSI escape codes. Rendering can also be disabled with --no-color, or the standard NO_COLOR environment variable.\nOutput is separated by kind. Only the final answer goes to stdout; everything else goes to stderr: the commands being run, mid-conversation updates, a final run summary (LLM calls, tool calls, tokens, latency), and, with --thinking, the model’s reasoning (each line prefixed with a thought bubble). This keeps stdout safe to pipe into other tools.\nOne-shot runs The common use case gives a system_prompt that describes the goals and approach (think of it as a one-file SKILL) and a user prompt that provides the question to solve.\nThe LLM runs through the prompt and, once it reaches the end of its turn, finishes processing, and the session cannot continue later. This resembles a shell utility.\nHTTP debugging As a debug or learning aid, all the HTTP requests can be logged to http-debug.log using the --http-debug flag.\nModel and run settings The agent.yaml sets which model runs the agent, the budget a run may spend, and whether the model exposes its reasoning. The Basic agent example above shows these together. The full set of configuration fields is in the configuration reference.\nModel llm.model selects the model and is required. It accepts any model identifier the Anthropic API accepts:\nllm: model: claude-sonnet-5 Larger models reason better on complex, long-horizon tasks; smaller models like Haiku are faster and cheaper for narrow ones. When the agent exposes ten or more tools it relies on the model’s server-side tool search, which recent models support and older ones (Claude Opus 4.1 and earlier and local models) do not. Set llm.no_tool_search to send every tool directly on an endpoint that does not implement tool search; when a large tool set cannot use it the run warns that all tools are being sent directly. The configuration reference lists the known models and their trade-offs.\nBudget llm.budget limits the agent loop so it cannot run without end:\nllm: budget: max_tokens: 500000 max_iterations: 50 call_timeout: 120s Setting Description max_tokens tokens a whole conversation may process, default 500000 max_iterations agent loop iterations one turn may take, default 50 call_timeout per-call timeout as a duration string, default 120s The two caps have different scopes. max_iterations applies to a single turn, and every turn of a conversation gets the same allowance. max_tokens applies to the whole conversation, so every turn draws on one allowance. Start a new conversation to get a fresh one.\nWhen a turn reaches the iteration cap, it stops, says so, and you can carry on with another prompt. When a conversation reaches its token cap, it is finished: the next prompt is refused before it runs. Start a new conversation, or raise llm.budget.max_tokens on the machine running the agent.\nmax_tokens counts tokens, not money. It adds up the uncached input, the output, and both prompt-cache tiers. A cache read counts the same as an uncached input token here even though it costs a fraction as much, so two conversations with the same token count can cost very different amounts. Set this value against your own usage rather than treating it as a spending limit.\nThinking Extended thinking lets the model expose its reasoning before it answers. Some providers call this reasoning rather than thinking; it is the same setting.\nllm: thinking: enabled: true Reasoning is never displayed unless asked for. --thinking (or THINKING=1) shows it, on fisk run and on fisk session show --transcript.\nthinking=N on the run summary and in the TUI status bar reports the tokens spent reasoning, shown whether or not reasoning is displayed. It is part of the output half of tokens=in/out, not extra.\nNote Older models that predate adaptive thinking, such as Sonnet 4.5 and Haiku 4.5, reject the parameter. Both explicit states send one, so for those models remove the thinking block rather than setting enabled: false. The same applies to an endpoint behind ANTHROPIC_BASE_URL whose proxy does not implement it.\nTerminal UI These harness settings govern the full-screen UI for an agent, independent of the per-run --no-tui flag:\nharness: no_tui: true no_bell: true no_tui is a persistent off switch: the agent always uses the line-by-line output, even on an interactive terminal, and the command line cannot turn the UI back on. Use --no-tui instead for a one-off run. no_bell silences the terminal bell. By default the full-screen UI rings the bell each time a run blocks on an approval gate or an ask_human_* prompt, so a waiting run is noticed even when unattended. Both are negative switches and have no effect in the line UI.\nTool selection Run the fisk info command to verify what tools the agent has access to:\n$ fisk info ╭───────────────────┬────────┬───────────────────────────────────────────────────────┬──────╮ │ TOOL │ SOURCE │ DESCRIPTION │ TAGS │ ├───────────────────┼────────┼───────────────────────────────────────────────────────┼──────┤ │ say │ local │ Say something using the configured command │ │ │ think │ local │ Think something using a cow │ │ │ ask_human_confirm │ local │ Ask the human operator a yes/no question at the te... │ │ │ ask_human_select │ local │ Ask the human operator to choose one option from a... │ │ │ ask_human_input │ local │ Ask the human operator to type a free-text value a... │ │ ╰───────────────────┴────────┴───────────────────────────────────────────────────────┴──────╯ Prompt: Tell short jokes using Cows! ... The output shows the say and think tools and some Human in the Loop tools. When the configuration sets a model, fisk info also prints a Model section first, listing the resolved model and provider, whether thinking is enabled, and how tool search will behave, so you can confirm the backend and feature gates without starting a run.\nApplication tags The application can declare that the LLM never gets the think tool:\n- name: think description: Think something using a cow type: exec tags: [ ai:deny ] # ... Adding the ai:deny tag to a command means Fisk AI never exposes that tool to the LLM. fisk info confirms the LLM only gets the say tool now.\nAgent configuration The agent.yaml can also include only certain tools:\ninclude: tools: - ^say Or exclude certain tools specifically:\nexclude: tools: - ^think This uses regular expressions over the tool name, and both can be used together. For example, include ^cow but exclude ^cow_think.\nA tool’s name is its command path joined with underscores, so a nested command like cow think becomes the tool cow_think. Grouping commands and hidden commands are skipped and never become tools.\nTools can also be included or excluded by tag:\nexclude: tags: - scope:system This excludes any command that has the scope:system tag.\nGlobal flags A wrapped binary often has application-level global flags that apply to every subcommand. nats, for example, has --context to select a stored connection profile, alongside sensitive globals such as --user and --password. By default none of these are exposed to the model. global_flags is an allowlist of the globals you want the model to be able to set per command:\nglobal_flags: - context Each named global becomes an argument on every leaf command tool, so the model can run nats stream ls against a chosen context without you hard-wiring one. Names are the long flag name, with or without the leading dashes, and are validated against the binary’s real global flags at load; a name matching none is an error. Hidden and framework flags (like --help) cannot be exposed, and a global that clashes with a command’s own flag or argument is skipped for that command. A global the application marks required is always exposed, whether or not it is listed, since the command cannot run without it.\nRun fisk info to see which globals a binary exposes; it lists the application’s global flags and marks the ones you have allowlisted.\nSession snapshots and resumption Every run is a conversation, and every conversation is journaled. You do not turn this on. Leave a run and continue it later, in a fresh process or on another machine, using the id it prints when it ends.\nContinuing a conversation Ask something. fisk prints the id of the conversation when the run ends:\n$ fisk run \"report on the ORDERS stream\" Continue it by that id. No prompt is given, since the conversation is restored from the journal; passing one is an error:\n$ fisk run --resume t-3f2a9c... fisk reads the conversation back before it goes on, so you continue in context rather than from a blank screen.\nAgainst an agent somewhere else, see remote agents, the journal is on the worker rather than here, so --resume takes the conversation token instead of the id. fisk session show on the worker prints it.\nChat sessions Every full-screen run is a durable, resumable conversation.\nEach turn is journaled, so the whole conversation survives leaving, a stop or a crash. Press Ctrl-D to leave the input bar when you are finished for now; the status bar reads ctrl-d done. This does not end the conversation. fisk prints how to continue it as it exits. Ctrl-C asks the current turn to stop at its next safe point. The conversation is kept either way.\nResuming reads the conversation back into the viewport before the input bar opens, so you continue in context rather than from a blank screen. Because the bar needs a real terminal, a conversation can only be continued in the full-screen UI, not with --no-tui or over a pipe, where a run answers one prompt. A conversation has no “completed” state; remove it with session rm once it is no longer needed.\nStopping The first Ctrl-C, or a SIGTERM, asks the run to stop where the conversation can be continued: the current step finishes, the turn is journaled, and fisk prints how to continue it. A second gives up on the run and leaves.\nBoth keep the conversation. The difference is that the first lets the turn reach a safe point, so the work it had already done is recorded rather than lost part way.\nRemote agents --nats-context points a terminal at an agent somebody else is running rather than starting one in this process:\n$ fisk run --nats-context production \"how many streams are there\" identity in the configuration names which agent to talk to, and you must set it. A default or a name derived from the application binary is shared by every agent built the same way, so the run could reach any of them.\nThe worker calls the model, runs the tools and writes the journal. Flags that describe that work are refused rather than ignored: --api-key, --base-url, --trace, --http-debug, --verbose, --state-dir and --no-telemetry. Setting one of these through an environment variable is ignored without an error, since you did not type it.\nDurability A session is journaled event by event as the run proceeds: each model turn and each tool result is recorded as it happens.\nA clean suspend is exactly-once. Nothing runs after the last recorded event, so a resume never repeats a tool call or an LLM call. A crash resumes from the last recorded event, so at most one tool call is repeated. A tool whose side effect completed but whose result was not yet recorded runs again on resume, since fisk cannot make an external side effect idempotent. Already-recorded turns and results are never replayed. Resume a session against the same agent configuration it started with. A session can be resumed from anywhere, including a machine that no longer has the original agent.yaml, so care is required: continuing a conversation against a different model, tool set, or system prompt can make the replayed transcript incoherent. fisk fingerprints the configuration when the conversation started and refuses to continue it when that no longer matches, naming what changed. --force overrides it, except for the provider: a session started against one llm.provider can never be resumed against another. A session that already completed cannot be resumed.\nManaging sessions A suspended or completed session is kept until it is removed. List, inspect, and remove sessions with the session subcommands:\nfisk session ls fisk session show \u003cid\u003e fisk session show \u003cid\u003e --transcript fisk session rm \u003cid\u003e session ls lists each session with its status, model, and prompt. session show prints a session’s counters and status; --transcript shows the full conversation (prompt, thinking, narration, tool calls, and tool output). On an interactive terminal --transcript opens the full-screen viewer with thinking and tool output folded, which z and Z expand; --no-tui/NO_TUI prints it as line output instead. session rm deletes a session.\nAnswering a deferred tool call A tool can report that its answer arrives later rather than now. The run then suspends, releasing the process, and resumes once the answer exists. session show lists what such a session is waiting on under Waiting on, giving the tool_use id, the tool, and whatever the tool said it is waiting for.\nThe answer travels on a request carrying the conversation’s token, described in Answering after the run ended. The tool is never called again: it already started the work, which is why it deferred.\nNo tool that ships with fisk defers; the mechanism is for tools a Go program registers through agent.Options.CustomTools.\nThese commands read the file backend under --state-dir by default. To inspect sessions in a configured backend, a jetstream stream or a file directory named in the config, pass that config with --config:\nfisk session ls --config agent.yaml fisk session show \u003cid\u003e --config agent.yaml Where a session is journaled is configurable through harness.sessions, which mirrors the shape of harness.memory. Two backends ship: file (the default) and jetstream Version0.0.3. The block is optional; leaving it out keeps the file backend under the XDG state directory.\nfisk info shows a Sessions section with the resolved backend and, for the jetstream backend, the stream and NATS context, so you can confirm where sessions are stored without starting a run.\nFile backend The file backend keeps each session as a JSON-lines journal under a directory. Sessions are stored under the XDG state directory, $XDG_STATE_HOME/fisk-ai/runs, defaulting to ~/.local/state/fisk-ai/runs. Set options.directory to move it off the default XDG path:\nharness: sessions: backend: file options: directory: /var/lib/fisk-ai/runs --state-dir overrides options.directory for a single run or session command, so the flag always wins over the configured path. It applies only to the file backend: combining it with a non-file backend is an error rather than a silently ignored flag.\nJetStream backend The jetstream backend keeps sessions as messages on a NATS JetStream stream instead of on disk, so a run suspended on one machine resumes on another over a broker. It uses the connection from the configured nats_context, the same one memory and remote tools use, and binds to a stream that must already exist: the agent never creates it, so you own the stream’s retention policy.\nnats_context: production harness: sessions: backend: jetstream options: stream: FISK_SESSIONS Create the stream first, subscribed to one wildcard subject and keeping a single message per subject so each run record is write-once:\nnats --context production stream add FISK_SESSIONS \\ --subjects 'fisk.sessions.\u003e' --max-msgs-per-subject=1 \\ --discard=new --discard-per-subject The stream keeps messages forever by default, which suits sessions; do not set a max age or they would silently expire. The subject prefix (fisk.sessions above) is yours to choose; the backend derives it from the stream’s single wildcard subject when it binds, so it is not set in the config. The backend fails at run start, rather than degrading silently, if the stream does not exist or its configuration does not match this shape. Sessions are never namespaced by identity, so a run started by one agent is found by another reading the same stream; keep separate environments in separate streams.\nHuman in the loop (HITL) When enabled, fisk gives the model built-in tools to ask the operator a question at the terminal and wait for the answer. They are off by default and only available when running the agent:\nharness: human_in_the_loop: enabled: true Enabling it offers these tools:\nask_human_confirm - a yes/no question. Returns {\"confirmed\": true} or {\"confirmed\": false} ask_human_select - choose one of a list of options the model provides. Returns {\"selected\": \"\u003coption\u003e\"}, or {\"selected\": null} if no choice was made ask_human_input - a free-text value, optionally pre-filled with a default the operator can accept or edit. Returns {\"value\": \"\u003ctext\u003e\"}, or {\"value\": null} if none was given Optional communication from the agent The model decides when to call the HITL tools, shaped through the prompt. They suit decisions the model should not make alone: confirming a destructive action, choosing between options that depend on operator intent, or supplying a value it cannot derive. The question is rendered on the terminal (stderr, so a piped final answer stays clean), and the model-supplied text is stripped of terminal control sequences first so it cannot spoof what is shown. Each tool denies by default: with no terminal attached the call returns a negative answer (no confirmation, no selection, no value) and a reason rather than hanging on a prompt no one can answer, and they are never exposed over MCP, where there is no operator. Tool calls within a turn run one at a time, so a prompt has the terminal to itself.\nIf you interrupt a question, or close the input, fisk does not treat that as a reply. The run stops there and the conversation is kept, and when you continue it the same question is asked again. No answer is recorded, so a run you interrupt never carries a decision you did not make.\nRequired tool use confirmations These mechanisms put a human in the loop:\nhuman_in_the_loop (a configuration flag) lets the model ask its own question through a fisk-provided ask_human_* tool, with no application command involved. The human answers a question the model chose to ask. ai:confirm (a command tag) lets the application author gate an ordinary, non-interactive command so the operator must approve it before it runs. The human is a checkpoint on a command the model wanted to run anyway; nothing about the command itself changes. Reach for human_in_the_loop when the model should decide when to check in; reach for ai:confirm when a normal command should run only with the operator’s say-so, typically something destructive or irreversible.\nCommand Tags Fisk commands can carry tags, set in their fisk definition (or, for App Builder applications, in YAML). Tags can be referenced by the include/exclude rules to select commands by group, and the ai: prefix is reserved for the tags fisk interprets. The full vocabulary is listed under Command tags in the Reference guide; the tags that control how a command is exposed to the model are:\nTag Description ai:deny Never expose the command to the model; it is dropped before include/exclude and can never be added back. ai:no_defer Always send the command directly instead of deferring it behind the tool-search tool. ai:confirm Require the operator to approve the command at the terminal before it runs; an “allow for the conversation” answer is remembered for that command for the rest of the conversation, across resumes of the conversation. The behavior tags (ai:read_only, ai:destructive, ai:additive, ai:idempotent) describe what a command does rather than controlling it. They reach the model and, over MCP, the client; they gate nothing.\nA tag under the ai: prefix that fisk does not recognize does nothing, so it is reported as a warning at startup and by fisk info.\nai:deny is the reliable way to keep a command the agent should never call out of reach, since it applies before any include/exclude rule. ai:no_defer keeps the handful of commands the model needs on most requests immediately available rather than discoverable only through tool search.\nai:confirm gates a command behind the operator’s explicit permission. When the model calls a command tagged ai:confirm, fisk pauses before running it and prompts the operator at the terminal, showing the resolved command line with its arguments, and offers three choices: run it once, run it and stop asking for that command for the rest of the conversation, or decline. Declining returns an authoritative result to the model (the command is not run and the model is told the decision is final), so it stops rather than working around the refusal. An “allow for the conversation” answer is remembered by command, regardless of its arguments: once you bless stream rm, every later stream rm call runs without asking again, so reserve that choice for a command you trust the agent to repeat.\nThe conversation records the answer, so continuing it honors the answer rather than asking again, and fisk session show lists what it holds. They are dropped by /clear and by a --force resume across a changed configuration, and a resume with no terminal attached declines a gated command rather than honoring one. The prompt is rendered on stderr (so a piped final answer stays clean), the displayed command line is stripped of terminal control sequences so model-supplied argument values cannot spoof what you see, and it denies by default: no interactive terminal, or a prompt that cannot be shown, declines rather than runs. An interrupt or an end-of-input at the prompt ends the run rather than declining, since the operator did not answer; the conversation stays continuable and asks again. Unlike human_in_the_loop, the tag is always active: there is no configuration flag to enable it.\nThe same gate can be extended to other tags with the harness.confirm_tags configuration key: any tag listed there gates its commands exactly as ai:confirm does, which lets an operator require confirmation for a tag the application already uses (for example ai:destructive, or an application’s own impact:rw) without editing the application. It is additive to the always-on ai:confirm tag and matching is exact rather than a regex. A confirm_tags entry that matches no loaded command is reported as a warning at startup, since a typo would otherwise leave a command ungated. The approval prompt names the tag that gated the command, so you can tell why you are being asked. Run fisk info to see each command’s tags and which commands a run would gate. Like ai:confirm, a confirm_tags tag gates both the agent loop and MCP, where it is requested through elicitation.\nAny other tags are free-form: they have no built-in meaning to fisk but can be matched by the tags field of an include or exclude rule.\nAll of a command’s tags, reserved and free-form alike, are also included in the tool description fisk sends the model, as a trailing Tags: ... line, in both the agent and over MCP. This lets your prompt reference them, for example “always use ask_human_confirm before running any command tagged impact:rw”. The human-facing fisk info listing keeps the plain description.\nMemory Memory gives the model a small key/value store that persists across runs, so it can keep durable notes (a layout it worked out, a convention, the outcome of an investigation) and pick them up next time rather than rediscovering them. It is opt-in and agent-mode only; like the human-in-the-loop tools it is never exposed over MCP.\nWarning Memory is shared state. Treat what a memory contains as data the model saved, not as trusted instructions.\nEnable it under harness.memory. The backend field selects where memories are kept; it defaults to file, so the minimal configuration is just:\nharness: memory: enabled: true When enabled the model is offered four tools: memory_list (keys and their descriptions), memory_read (one memory by key), memory_write (save a memory with a key, a one-line description, and a body), and memory_delete. A key uses letters, digits and ., _, = or - (no slashes or spaces), which keeps it valid both as a filename and as a NATS KV key. memory_write creates by default and refuses to overwrite an existing key unless called with overwrite: true, so the model does not silently clobber a note; the create still fails cleanly if two writers race for the same new key.\nread_only: true serves memory_list and memory_read and withholds the other two, for a run that should use what earlier runs saved without changing it. The store itself is unaffected, so anything else writing to it still does.\nAt the start of a run the stored keys and descriptions are injected into the system prompt as an index so the model knows what it has saved; memory_list is the live view during the run. Turn the index off with no_index: true.\nA memory body is capped at 64 KB and a store holds at most 1024 entries. Both limits are shared by every backend, and a write that would exceed them fails cleanly. The on-disk format is shared too, so a value written by one backend migrates to another unchanged.\nfisk info shows a Memory section with the resolved backend and, for the jetstream backend, the bucket, NATS context and key prefix, so you can confirm where memory is stored without starting a run.\nTwo backends ship today: file (the default) and jetstream Version0.0.3.\nFile backend The file backend keeps each memory as a markdown file named for its key under the configured directory, which defaults to memory/\u003cidentity\u003e.\nharness: memory: enabled: true backend: file options: directory: memory A relative directory, including that default, resolves under the store base when a deployment sets one and against the working directory otherwise; an absolute directory is used as-is. The identity is the agent’s name, set with the identity configuration field and defaulting to the application binary’s base name; the configuration reference covers it in detail. Point two agents at the same directory and they share a memory; leave the default and each agent keeps its own.\nJetStream backend The jetstream backend keeps memories in a NATS JetStream KV bucket instead of on disk, so a fleet of agents can share durable memory over a broker. It uses the connection from the configured nats_context, the same one remote tools use, and binds to a bucket that must already exist: the agent never creates it, so you own the bucket’s durability policy.\nnats_context: production harness: memory: enabled: true backend: jetstream options: bucket: agent-memory Create the bucket first, without a TTL so memories do not silently expire and with a max value size that fits a full entry (the 64 KB body cap plus the small frontmatter header stored with it), up to 1024 entries:\nnats --context production kv add agent-memory --history=1 --max-value-size=69600 The backend fails at run start, rather than degrading silently, if the bucket does not exist, has a TTL set, or caps values below that full-entry size.\nBy default each agent’s keys are namespaced under a prefix equal to its identity (stored as \u003cidentity\u003e.\u003ckey\u003e), mirroring the file backend’s per-identity directory so two agents pointed at one bucket do not collide. Set options.prefix to a shared value for agents that deliberately share memory, or to \"\" for a flat, unprefixed keyspace:\nharness: memory: enabled: true backend: jetstream options: bucket: agent-memory prefix: fleet-shared # agents with the same prefix share memory; \"\" is flat Read-before-update The jetstream backend adds a safety guard the file backend cannot: an overwrite must follow a read of the current value, and is refused if the memory was not read or has changed since it was read. The model then reads the current value and retries. This is the same read-before-edit discipline that keeps an editor from clobbering a file it has not seen.\nThe read counts for the whole conversation rather than one turn. A memory read on Monday and edited on Friday in the same conversation is overwritten without a fresh read, as long as nothing else wrote to it in between; if something did, the write is refused and the model reads again before retrying. One conversation’s reads never authorize another’s overwrite.\nThe check uses the KV entry’s revision, which makes it an atomic compare-and-swap: when two agents share a bucket and both try to update the same memory, the second write is rejected rather than silently overwriting the first. The file backend’s last-writer-wins overwrite would quietly drop that change, so a shared or concurrent deployment wants this backend.\nThe guard is on by default. Set no_require_read_before_update: true to allow blind overwrites, matching the file backend’s behavior:\nharness: memory: enabled: true backend: jetstream options: bucket: agent-memory no_require_read_before_update: true We can use memory to ensure our agent never repeats jokes; change thesystem_prompt as follows:\nharness: memory: enabled: true system_prompt: | Tell short jokes using Cows! You have tools that can render a cow saying short sentences, when asked use the tools to tell funny jokes. You tell cow jokes, no other kinds of jokes, strictly jokes about cows. If asked to tell non cow jokes, refuse and show no joke. Do not use emoji, keep general narration short, just stick to the jokes Save the jokes you told to a single memory file with all the past jokes and make sure you dont repeat jokes you previously told. Finish your turn by making a funny quip related to the joke or cows or similar We will get a new joke every time - be ready to get some awful jokes after a while :)\nSafety When Fisk AI runs a command in a CLI tool it passes a slice of arguments to the exec system call. No shell is involved that can be escaped or influenced.\nApp Builder is often involved and calls shell scripts, so App Builder commands need to be written defensively.\nUse type hints on arguments for ints, floats and so on Use is_shellsafe(value) on string input arguments Use escaping when passing arguments to commands, for example {{ .Arguments.message | escape }} Tag commands with the various helper tags so the harness understands the intent Mark every mandatory argument as required Fisk AI has no tools that can interact with arbitrary files on the system. The only way it interacts with the system is through the supplied tools or the Memory feature.\nEvery command the agent runs gets the same protections:\nIts output combines stdout and stderr, preserving order, and is capped at 64 KiB so a chatty command cannot flood the model’s context The ANTHROPIC_API_KEY is stripped from its environment, so a tool can never read the agent’s own credentials LLMFORMAT=1 is set, signalling fisk applications to render output suited to an LLM rather than a terminal Local LLMs Local LLM hosting tools like ollama, LM Studio and others support exposing an Anthropic-compatible API. Fisk AI can communicate with those tools.\nTo support a large number of tools, Fisk AI uses the Tool Search Tool, which these local runners do not support. When targeting a locally hosted model, the total tool count may need to stay around 15.\nI set these environment variables before invoking fisk to access my local Anthropic API instead of reaching to the internet.\n$ export ANTHROPIC_BASE_URL=http://localhost:1234 $ export ANTHROPIC_API_KEY=lmstudio The base_url is validated: a non-loopback host must use https, so the API key and conversation are never sent in cleartext. Plain http is allowed only for a loopback address (127.0.0.1, ::1, localhost) as used by the local runners above.",
    "description": "Create LLM agents from Fisk CLI applications",
    "tags": [],
    "title": "Agents",
    "uri": "/fisk-ai/agents/"
  },
  {
    "breadcrumb": "Introduction \u003e Design",
    "content": "This is a rough development roadmap, this isn’t set in stone - as we explore this area of development we might find additional features to add or directions to take.\nThis would give you guidance on our goals though.\nExpand the RAG system with a GraphRAG layer While the RAG system we have is sufficient for the kinds of use cases we target today - project documentation etc - it just is not good enough for all kinds of data we might encounter.\nIdeally we use a LLM to extract taxonomies out of source material and then create a Graph from that. RAG to find the starting point, Graph queries to get the relationships. This allows us to answer questions like List all episodes of the TV Series Criminal Minds that had a female perpetrator.\nSee GraphRAG.\nIntegration with Job Systems We need to integrate with job systems like Choria Async Jobs to facilitate external work arriving into our sphere.\nImagine a Webhook listener that receives a webhook and opens a Job. At this point the webhook is handled, the Agent will pick it up, do the work and update the record with the outcome\nFinish the A2A system Today we have tool calling using request-reply but no LLM prompting ability. We have the data types but nothing is wired up to them to facilitate true A2A\nChoria Transport Once the A2A system is mature expose it on the Choria transport with strong Identity, Authentication, Authorization and Auditing.\nMove API out of internal package A large goal of this project is to create libraries that can be used to build all sorts of AI Agent within the Choria ecosystem.\nAt the moment the libraries, while being developed and refactored, are all in internal. The aim is to make these public.\nThis would coincide with a end to end POC against Kestra doing complex problem solving using workflows as tools. This system would be running in a Orchestrator that creates new agents in containers.",
    "description": "Long term development goals",
    "tags": [],
    "title": "Roadmap",
    "uri": "/fisk-ai/design/roadmap/"
  },
  {
    "breadcrumb": "Introduction \u003e Channels",
    "content": "The fisk serve command hosts an agent behind the endpoints its configuration enables. It runs until interrupted. The agent is the one fisk run drives: tool set, prompt, model and harness settings come from the same configuration file.\nQueued jobs takes each job off a work queue, runs it, and stores the answer for the submitter to read later. Answering prompts takes a prompt from another agent and streams the run back while that agent waits. Serving tools runs one tool for another agent and starts no agent loop.\nNote At least one endpoint must be enabled. fisk serve exits with an error when the configuration enables none.\nStarting a worker A minimal configuration has the application path, the tool selection and one channel. Here that channel is queued jobs:\nidentity: worker application_path: /usr/local/bin/nats nats_context: production system_prompt: | You inspect NATS servers on behalf of an operator. Answer concisely. include: tools: - ^stream_ expose: agent: jobs: {} $ fisk serve --config nats.yaml The startup banner lists the endpoints it started and the settings every run uses:\nServing worker/1.2.0: Endpoints: asyncjobs/FISK_AI Model: claude-sonnet-5 Queue Context: production Sessions: jetstream (FISK_SESSIONS) Knowledge: disabled Telemetry: disabled Tool Directory: /var/lib/fisk-ai Tool Timeout: 5m0s Queue Workers: 1 (config) The banner adds an Agent Context line when the agent’s nats_context differs from the queue’s.\nEach endpoint prints its own section below. It shows the addresses that endpoint answers on and the limits it uses. Answering prompts and serving tools show examples.\nShared resources fisk serve builds the model provider, the session store, the memory store, the knowledge index and the NATS connection once at startup. Every run shares them. A missing stream or bucket fails the process immediately instead of failing whatever job happens to arrive first.\nWarning A worker whose storage does not exist fails at startup. Under a supervisor that restarts on failure it crash-loops.\nWhen the knowledge index does not exist at startup, each run opens the index for itself, so an index built after the worker started is visible to later runs.\nConcurrency Each channel limits its own runs, so a process serving two channels at two runs each is running four.\n--workers sets how many queued jobs run at once, overriding expose.agent.jobs.workers:\n$ fisk serve --config nats.yaml --workers 4 --workers affects the queued-jobs channel only. The prompts channel takes its count from expose.agent.a2a.prompts.workers and refuses a caller when every slot is busy.\nA work queue has a concurrency setting of its own that limits every worker on it together. Setting workers above what the queue allows leaves slots idle rather than raising throughput.\nTimeouts harness.tool_timeout limits a single tool call, in fisk serve and fisk run alike. The default is five minutes. 0s removes the limit, for commands that run for hours.\nNote --workers overrides the configuration file. harness.tool_timeout in the file overrides the built-in default.\nThe timeout stops a command and its process group. It does not stop an in-process handler that ignores its context.\nWhere tools run Command tools run in the worker’s own working directory unless --work-dir names another. It must be an absolute path that already exists.\nEvery run shares it. Set the worker count to 1 when a tool writes local state that concurrent runs would corrupt.\nNote A CLI that reads a context or a profile of its own does not inherit the agent’s nats_context, so pass the selection explicitly where it matters.\nShutdown On the first interrupt the worker drains. It takes no new work, and runs in flight continue to their next resumable point:\ndraining: no new work is taken and running work stops where it can resume. Interrupt again to stop now A second interrupt stops the worker at once. The queue redelivers any queued job still running, and the redelivery resumes from the journal. The prompts channel answers its callers with a failure instead.\nA drain stops every endpoint, so a worker also serving tools stops answering peers at the same point. A worker with no channel has nothing to resume:\ndraining: the endpoints stop answering. Interrupt again to stop now Sessions Each channel journals its runs, so an interrupted run resumes instead of making the same model calls a second time.\nSessions need a store every worker can read. On one machine the default file backend is enough. Across machines, configure a shared harness.sessions backend. Without one, a job redelivered to a different worker cannot read the journal and starts again.\nWhen two workers reach the same journal, the second one claims it. The claim is written before the run starts, and the first worker sees it before its next tool call and stops. Only a tool already running can execute twice.\nSettings a channel run ignores The following settings narrow the MCP and a2a tool endpoints, not a run served over a channel:\nexpose.agent.tools selects what is served over MCP and a2a. A channel runs the whole agent loop, so it uses the agent’s own include and exclude instead the waiver that lets a tool-serving configuration omit identity, system_prompt and llm.model does not apply to a channel, since a run needs all three Safety A served run is a full agent loop driven by caller-supplied prompt text, running every tool the configuration allows. The channel’s own admission check is therefore the only access control: queue publish permission for queued jobs, NATS publish permission for prompts.\nharness.tool_timeout limits each tool call and llm.budget limits the run. A caller may lower the budget, never raise it. The tool safety rules described in the Reference hold here as everywhere else: commands run as an argument vector rather than through a shell, each argument is checked against the command’s schema, and credentials are stripped from tool environments.",
    "description": "the serve command and the settings every channel shares",
    "tags": [],
    "title": "Serving",
    "uri": "/fisk-ai/channels/serving/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "Fisk AI is a single Go module with one binary. The layering is strict enough that reading any one package rarely requires reading its callers, and the recurring patterns are consistent enough that learning them once covers most of the tree.\nLayers main: run, session, info, knowledge, mcp, a2a, discover owns flags, signals, and every byte of terminal wording internal/agent the only package that composes all the others memory notes runstate journal rag index a2a peers mcpserver, tui surfaces internal/llm neutral message model internal/toolkit tool contracts internal/util shared primitives config pure data, no IO beyond reading the file, imports no internal package Each band imports only from bands below it. `config` sits at the bottom precisely so everything can read it. Two placements are worth calling out.\nconfig is the lowest layer and imports nothing internal, which is why it can define constants that other packages need to agree on. The knowledge tool’s name lives there so the MCP allowlist can be validated without importing the package that implements the tool.\ninternal/agent is the only package that composes all the others. Its own doc states the boundary: it owns no CLI concerns, so flags, signals, and terminal rendering stay with the caller. That is what makes it embeddable.\nThe subsystem band packages do not import each other. Memory does not know about knowledge, and the journal does not know about tools. They meet only in the agent.\nImplementations are linked in, never named in code Five things are selected by a string in the config file: the model provider, the A2A transport, the memory backend, the session backend, and the tool kinds. All but the last use the same registry pattern.\nconfig names it harness.memory.backend registry lookup by name only factory fails at construction blank import init registers itself an unknown name lists exactly what is linked in no host code ever names a backend Every registry error message enumerates the linked implementations, which turns a forgotten import into a self-diagnosing failure. The blank imports all live in internal/agent/agent.go, each with an inline comment saying what it enables. Adding a second implementation is a package plus one import line.\nEvery registry follows the same rules:\nRegister panics on an empty name, a nil factory, or a duplicate, mirroring database/sql.Register. Each is a programming error resolvable at compile time. Factories decode their options strictly, with unknown fields rejected, so an operator’s typo fails at run start. Construction failures are errors, so a missing bucket, an unwritable directory, or a misconfigured stream surfaces before the agent runs. A backend declares its own requirements rather than being special-cased. The JetStream memory and session backends register a “requires NATS” flag, which is the only reason the host knows to dial a connection without naming any backend. Invariants that repeat everywhere Recognizing these once makes the rest of the tree read quickly.\nOne flat tool namespaceApplication, built-in, remote, and custom tools share one name set. A collision aborts the run rather than shadowing, because shadowing a confirm-gated command would strip its gate. Borrowed versus ownedAnything injected by a caller, whether a NATS connection, a store, a transport, or a provider, is used and never closed. Only what the run itself dialed or opened gets a deferred close. The contract is restated at every field and every use site. Fail at constructionUnknown backend, unknown option key, unwritable directory, missing bucket, bad stream config, illegal prefix, unreachable remote host. All are startup errors, never a surprise at the first tool call. Determinism for resumeAnything hashed into the run fingerprint must be byte-stable across restarts, which is why schemas are cloned rather than mutated, lists are sorted, and optional fields are emitted unconditionally. Data, not instructionsMemory entries and retrieved knowledge are wrapped and labeled as data the agent saved or found, never as instructions. The framing appears in the system note, the injected block, and the tool description. Bounded everythingMessage sizes, output capture, source files, chunk sizes, query terms, batch sizes, response bodies, concurrency, and timeouts all have explicit constants with the reason in a comment. Soft states versus hard failuresA missing index, an absent memory, a declined confirmation, and a non-zero exit are all normal results. Only a harness failure is an error. Sanitize before truncateEverything reaching an operator's terminal is stripped of escape sequences before any length cap, so a cut can never leave a dangling escape. The single run goroutine The agent spawns no goroutines of its own. Several design decisions rest on that and say so:\nThe events sink needs no locking for a per-run consumer. The run statistics counters need no lock. The confirm gate is explicitly not concurrency-safe and must never be wired into the concurrent MCP path. Hooks run on that goroutine, in loop order. The serving modes are the exception, and both bound their concurrency with a semaphore for the same stated reason: an external caller has no iteration budget, so an ungated path could spawn unbounded concurrent commands.\nThree entry points, one selection run, mcp, and a2a read the same file and share the same tool-selection path. What differs is which validation mode applies and what happens to a confirm-gated tool.\nrun mcp a2a Needs a model and prompt yes no no Built-in tools all enabled ones the knowledge tools only, if allowlisted none declares a2a exposure Confirm-gated tools prompted locally exposed and gated by elicitation dropped entirely Concurrency one loop semaphore semaphore Human reachable yes, on a terminal maybe, through the client no Where the trust boundaries are Being specific about this is more useful than a general claim of safety.\nThe model is untrusted. It chooses tool names and arguments, and everything it produces is treated as data: arguments are bounded to a schema and become argv with no shell, text is sanitized before display, and stored or retrieved text is framed as data rather than instruction.\nThe wrapped application is trusted to run but not to behave. Its introspection output is size-capped and rejected if it overflows, its process group is killed on cancel, and its environment is scrubbed of credentials by name.\nA remote peer is untrusted at the protocol level. Messages are size-capped before decoding and schema-validated in both directions, and a reply is only ever sent to the inbox the transport supplied, never to an address taken from the message. Authentication and authorization are delegated entirely to NATS.\nThe operator is trusted. Custom tools, hooks, and an injected provider all run with the agent’s own privileges, and the source says so rather than implying a sandbox that does not exist.\nLoad-bearing decision None of the execution paths is a sandbox, and the source repeatedly refuses to imply otherwise. The per-run working directory is described as collision avoidance, not confinement. Credential scrubbing is name-based and cannot catch a secret exported under an undeclared name. The reliable exclusion mechanism is ai:deny, which drops a command before any filter or gate can reach it.\nNext Configuration is where a reader following the flow should go, since every entry point starts there.",
    "description": "How the packages layer, how implementations are linked in, and the invariants that repeat everywhere",
    "tags": [],
    "title": "Architecture",
    "uri": "/fisk-ai/design/codemap/architecture/"
  },
  {
    "breadcrumb": "Introduction \u003e Design",
    "content": "Fisk AI turns a Fisk command-line application into an LLM agent by introspecting its command tree and exposing the allowed commands as tools. This section is a reading guide to how that is implemented, aimed at contributors, reviewers, and anyone auditing the harness before trusting it with a production tool.\nSnapshot Generated 2026-07-24 against tag v0.0.3. Commits after this one may make parts of this map stale.\nThe mental model There is one core and several faces. A Fisk application is introspected once into a set of tools; a YAML file narrows that set and decides what needs approval; and then the same selection is either driven by a model in an agent loop, served to an MCP client, or served to other agents over NATS. Three durable stores sit underneath, and none of them is part of the conversation the model sees until the harness decides to put it there.\nfisk CLI app introspected once agent.yaml selects and gates tool set and loop one flat namespace gated, bounded, journaled run terminal, human gate mcp http clients a2a other agents memory model-written notes knowledge operator-owned docs sessions append-only journal the same selection drives all three faces; only the confirmation policy differs One core, three faces, three stores. What changes between the faces is whether a human can be reached. What the design optimizes for The project describes itself by what it does not have. Reading the source, three commitments show up repeatedly and explain most of the structure.\nNothing is silently weakened. A configured confirm tag that matches no tool is warned about, because leaving it unreported would give a false sense of safety. A tool-name collision aborts the run rather than shadowing, because shadowing a gated command would strip its gate. A tag-based exclude on a remote host is rejected outright, because discovery carries no tags and the filter could never be honored.\nFailures land at startup. An unknown backend, a typo in an options block, a bucket with a TTL, an unreachable remote agent, a stale knowledge manifest: all of them stop the process before the model is contacted, and the error names the fix.\nUntrusted text stays data. Model-written memories and retrieved documents are wrapped, labeled as data rather than instruction, sanitized at write time, and sanitized again at render time.\nHow to read this section Start with Architecture for the layering and the patterns that repeat, then Configuration, since every entry point begins by parsing a file. From there, The agent loop and Tools and introspection are the core, and the rest can be read in any order.\nEach page names real files and symbols, states the invariants the safety story depends on, and is explicit about what is reserved, unused, or aspirational rather than presenting the whole tree as finished.\nExplore Architecture\nHow the packages layer, how implementations are linked in, and the invariants that repeat everywhere\nConfiguration\nOne YAML file, three modes, and the strictness that makes a typo fail at startup\nThe agent loop\nCall the model, run the tools it asks for, journal everything, and stop for the right reason\nTools and introspection\nTurning a Fisk command tree into tools the model may call, and the tags that decide what it may not\nModel providers\nA provider-neutral message model that doubles as the on-disk format, and one package allowed to speak an SDK\nSessions and replay\nAn append-only journal, a pure fold, and a fingerprint that refuses an incoherent resume\nMemory\nSmall durable notes an agent writes for its future self, with the key charset as the security boundary\nKnowledge\nA hybrid search index in one SQLite file, with no cgo, no sidecar, and no build-tag matrix\nServing: MCP and A2A\nTwo ways to hand an application's tools to something else, and why one exposes gated tools while the other refuses\nTerminal and events\nOne typed event contract, two terminal surfaces, and a screen owned by exactly one goroutine\nReference and map\nThe command surface, the source map by package, key types, and the vocabulary {class=“children children-type-tree children-sort-”}",
    "description": "A guided deep-dive into the codebase: architecture, subsystems, and the decisions behind them",
    "tags": [],
    "title": "Code Map",
    "uri": "/fisk-ai/design/codemap/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "Instead of running an agent loop, Fisk AI can serve a Fisk application’s commands over the Model Context Protocol so another client, such as Claude Desktop or Claude Code, calls them directly as tools.\nThe tool set, the input schemas, and the tag rules are the same ones the agent uses. Only the caller changes: where the agent drives the tools with an LLM against a prompt, an MCP server hands the same tools to whatever client connects and lets it decide when to call them.\nNote Serving over MCP is opt-in. The configuration must carry an expose.agent.mcp block, otherwise fisk mcp refuses to start.\nStarting a server Serving over MCP needs less configuration than an agent: there is no agent loop, so system_prompt and llm.model are not used. A minimal config needs only the application to introspect, a tool selection, and the expose.agent.mcp block:\napplication_path: /usr/local/bin/nats include: tools: - ^stream_ expose: agent: mcp: {} Start the server with the mcp command:\n$ fisk mcp --config nats.yaml --port 8080 The transport is HTTP, the streamable MCP transport. The port is taken from --port (or FISK_AI_MCP_PORT); if unset, from expose.agent.mcp.port in the config; otherwise it defaults to 8080. All progress and logging go to stderr.\nUse fisk info to preview which tools a configuration exposes before starting the server:\n$ fisk info --config nats.yaml Connecting a client Wire an MCP client to the server by pointing it at the running URL. For Claude Code:\n$ claude mcp add --transport http nats http://127.0.0.1:8080 A client that takes a JSON server map uses:\n{ \"mcpServers\": { \"nats\": { \"type\": \"http\", \"url\": \"http://127.0.0.1:8080\" } } } Configuration MCP mode uses only the parts of the configuration that describe the application and the tool set. An identity becomes the MCP server name, defaulting to fisk when unset; system_prompt, llm.model, and the agent-only harness settings are ignored.\nField Description application_path path to the Fisk application binary to introspect and serve; optional, omit it to serve only allowlisted built-ins (today the two knowledge tools) expose.agent.mcp.builtins the built-ins this operator wants served; only knowledge_search and knowledge_enumerate are accepted. A tool must also declare MCP exposure itself, so this can narrow what is served but never widen it expose.agent.mcp the opt-in block that enables MCP serving; must be present expose.agent.mcp.port default listen port when --port and FISK_AI_MCP_PORT are unset, default 8080 expose.agent.mcp.address host or IP to bind when --address and FISK_AI_MCP_ADDRESS are unset, default 127.0.0.1 (loopback); use 0.0.0.0 to listen on all interfaces expose.agent.mcp.instructions free-text guidance sent to clients on connect expose.agent.mcp.confirm_over_mcp how confirmation-gated commands behave when a client cannot be asked expose.agent.mcp.max_concurrent_tools maximum tool calls run at once; 0 or unset uses the default 2, negative is rejected, capped at 1024 expose.agent.mcp.tool_timeout how long a single served tool call may run, for example 60s; unset uses the default 30s include / exclude select which commands become tools, matched on tool name (regex) or tag expose.agent.tools narrow the exposed set further within the include/exclude selection identity the MCP server name; optional Instructions expose.agent.mcp.instructions sets a block of free text sent to clients when they connect. A client may pass it to the model as a hint about how to use the server, which suits orientation the individual tool descriptions are too terse to carry:\nexpose: agent: mcp: instructions: | These tools wrap the NATS CLI. Prefer stream_info before stream_edit, and treat all subjects as relative to the FOO account. How tools are exposed Each command becomes an MCP tool named by its command path, for example stream_info, with its input schema and a description built from the command’s help. Both the short help and any long help are surfaced to the client, so a command that carries detailed long help gives the model richer guidance than a one-line summary alone.\nEach tool carries a readable title annotation holding the space-separated command path, so stream rm rather than the underscore tool name, and the behavioral hints its behavior tags declare:\nTag Annotation ai:read_only readOnlyHint: true ai:destructive destructiveHint: true ai:additive destructiveHint: false ai:idempotent idempotentHint: true Untagged commands send no hints at all, leaving the client on the MCP defaults, which treat a tool as destructive and open-world. A confirmation gate does not change these hints: a command tagged both ai:read_only and ai:confirm is still advertised as read-only.\nApproval itself is not expressed as an annotation, because annotations are advisory hints a client may ignore rather than a control channel.\nEvery tag a command carries, reserved and free-form alike, also reaches the client as description text, as described under Command tags over MCP below.\nThe served tools are the agent’s include/exclude selection, narrowed further by expose.agent.tools when it is set. With neither, every command is served, subject to the tag rules below. Tool selection uses the same regular expressions over the tool name as the agent. A tool call runs the command and returns its result, limited by tool_timeout per call and max_concurrent_tools in flight at once.\nCommand tags over MCP The reserved command tags are honored over MCP, differing from the agent loop where noted:\nTag Behavior over MCP ai:deny never exposed, the reliable way to keep a command off MCP entirely ai:no_defer no effect, since MCP does not defer tools behind a tool-search tool ai:confirm exposed and gated through elicitation rather than a local operator prompt behavior tags advertised as tool annotations, as described above All of a command’s tags, reserved and free-form alike, are included in the tool description as a trailing Tags: ... line, the same as in the agent, so a client’s prompt can reference them.\nConfirmation over MCP Commands tagged ai:confirm, or a configured confirm_tags tag, require approval before they run. There is no local operator on the MCP path, so Fisk AI requests approval from the calling client through MCP elicitation: before running a gated command it asks the client to approve, showing the server name, the resolved command line, and the tag that gated it, and runs the command only on an explicit approval. A refusal, a dismissal, or any elicitation error denies the call and returns an authoritative result the model is told not to retry.\nNot every client supports elicitation. expose.agent.mcp.confirm_over_mcp chooses what happens when the connected client cannot be asked:\nValue Behavior auto default; ask clients that support elicitation, run the command ungated for clients that do not always ask clients that support elicitation, refuse the command for clients that cannot be asked never never ask, run gated commands ungated regardless of client support, delegating approval to the client’s own UI expose: agent: mcp: confirm_over_mcp: always Warning Elicitation is a request, not an enforcement boundary. A client is free to auto-approve, and under auto or never a client that cannot elicit runs gated commands ungated. For a command that must never be reachable over MCP, use ai:deny rather than relying on confirmation.\nA client that already has its own approval UI may prompt twice under auto or always; set never when the client’s own gating is trusted and the second prompt is unwanted.\nWhat is not served The built-in operator tools are agent-mode only and are never exposed over MCP, since there is no local operator on the MCP path:\nthe human-in-the-loop ask_human_* tools the memory memory_* tools Safety Every served command gets the same per-command protections as the agent: it runs as an argument vector rather than through a shell, its arguments are checked against the command’s schema, its ANTHROPIC_API_KEY is stripped, its output combines stdout and stderr and is capped at 64 KiB, and LLMFORMAT=1 is set.\nThe threat model is wider than an agent run:\nAny client that can reach the server’s port can invoke every exposed tool with any schema-valid arguments. ai:deny and include/exclude are the gate on what is reachable, so scope the exposed set deliberately. There is no agent loop, prompt, or token budget limiting total use. tool_timeout and max_concurrent_tools limit a single call and how many run at once. Neither limits how many calls a client makes, so do not expose the server on an untrusted network. Command output is returned to the connected client rather than to Anthropic, so whoever connects sees whatever the commands print. Confirm-tagged commands are gated by elicitation, a request the client fulfills rather than an access control the server enforces. Use ai:deny, not confirmation, for anything that must never be reachable over MCP.",
    "description": "Serve Fisk application tools over MCP",
    "tags": [],
    "title": "MCP Servers",
    "uri": "/fisk-ai/mcp/"
  },
  {
    "breadcrumb": "Introduction \u003e Channels",
    "content": "The queued-jobs channel takes whole units of work off a Choria asyncjobs work queue, runs the agent loop against each one, and stores the answer back on the task. The submitter holds no connection to the worker: it enqueues a task, and reads the answer off the task record once a worker has written it.\nNote The channel is opt-in. The configuration must carry an expose.agent.jobs block, otherwise fisk serve has no queue to bind to.\nThe queued-jobs channel is available since Version0.0.5.\nCreating the storage A worker requires its storage to exist. Create it with ajc, version 0.4.0 or newer.\nThe task store holds every job and the answer written back to it:\n$ ajc tasks initialize The work queue holds the jobs waiting to be taken:\n$ ajc queue add FISK_AI --run-time 10m --tries 3 --concurrent 10 Run time, retry cap and concurrency are properties of the queue, not of the agent configuration. The worker reads them from the consumer at startup and prints them on the banner. The run time must be longer than a job takes, or the queue redelivers work that is still running.\nA worker started before either exists fails:\nfisk: error: building the jobs endpoint: connecting to queue \"FISK_AI\": storage not ready: stream CHORIA_AJ_TASKS does not exist, create it with 'ajc tasks initialize' Submitting work A caller enqueues a task with the queue engine’s own client. The task must name the configured queue and task type, and its payload is a v1 prompt request:\nItem Value Queue expose.agent.jobs.queue, default FISK_AI Task type expose.agent.jobs.task_type, default fisk-ai:run Payload an io.choria.fisk-ai.v1.request.prompt message A queue has nobody waiting on it, so the three other kinds of request are refused here: they act on a conversation somebody is watching.\nThe request holds the prompt and the framing every v1 message needs:\n{ \"protocol\": \"io.choria.fisk-ai.v1.request.prompt\", \"id\": \"2fT8kMxQ1aBcDeFgHiJkLmNoPqR\", \"request\": \"2fT8kMxQ1aBcDeFgHiJkLmNoPqR\", \"conversation\": \"2fT8kMxQ1aBcDeFgHiJkLmNoPqR\", \"sequence\": 0, \"time\": \"2026-08-15T11:30:00Z\", \"sender\": { \"name\": \"ops-console\" }, \"prompt\": \"Which streams have no consumers?\" } On a request, id, request and conversation all hold the same value. sender.name is limited to letters, digits, - and _.\n$ ajc tasks add fisk-ai:run --payload-file request.json --queue FISK_AI Enqueued task 3Hwxl119ZbwKHCKPzWlslXZYnB0 The submitter supplies the task id, or the engine mints one. The worker hashes it with the serving identity to get the session the run journals under, so a job creates a session or resumes one an earlier delivery of the same task made, and reaches nothing else on the worker. Every id the queue accepts works, a leading dash and a colon included.\nOptional fields narrow what one job may do:\nField Description context supporting material offered alongside the prompt budget.max_tokens lowers the token budget for this job budget.max_iterations lowers the model-call cap for this job A budget may only lower what the configuration allows. A value above the configured limit is ignored.\nThe worker refuses a payload it cannot run and does not retry it, recording the reason in the task’s LastErr. This covers:\nan oversized payload a payload that is not a valid v1 request a payload that is not an io.choria.fisk-ai.v1.request.prompt, or whose prompt is empty Reading the answer The answer is stored on the task itself as a v1 result message:\n$ ajc tasks view 3Hwxl119ZbwKHCKPzWlslXZYnB0 --json { \"protocol\": \"io.choria.fisk-ai.v1.result\", \"request\": \"2fT8kMxQ1aBcDeFgHiJkLmNoPqR\", \"sender\": { \"name\": \"worker\" }, \"recipient\": { \"name\": \"ops-console\" }, \"stop_reason\": \"end_turn\", \"text\": \"ORDERS and PAYMENTS have no consumers.\", \"usage\": { \"input_tokens\": 1608, \"cache_read_tokens\": 663, \"output_tokens\": 51, \"llm_calls\": 2, \"tool_calls\": 1 } } The request field echoes the id the caller submitted, and recipient names the caller that asked. input_tokens counts every input token the job consumed. cache_read_tokens and cache_create_tokens are subsets of that total, not additions to it.\nA failed run is still a completed job. The worker stores a v1 error message with a stop_reason and acknowledges the task. It is not retried: a model refusal or an exhausted budget fails the same way on redelivery.\nStop reason Meaning end_turn the agent finished and answered budget_exhausted the token budget ran out max_iterations the model-call cap was reached suspended the run stopped at a point it can resume from error the run failed Redelivery The worker journals every run under the session its task id derives. When a worker dies mid-job, the redelivery derives the same session and resumes that journal instead of starting again.\nA job whose session already completed is answered from the journal, without running the agent or calling the model. This is the case when a worker finished a job and died before acknowledging the task.\nNote Deploying a changed tool set while jobs are in flight fails their resume check, and those jobs are retried until the queue’s try limit and then expire. Drain a worker before replacing it.\nConfiguration Every field under expose.agent.jobs has a default, so an empty block is valid.\nexpose: agent: jobs: # The work queue to consume. It must already exist. queue: FISK_AI # The task type this worker handles. Tasks of another type on the # same queue are left alone. task_type: fisk-ai:run # How many jobs this process runs at once. The --workers flag # overrides it. workers: 1 # The NATS context the queue is reached over, defaulting to the # top-level nats_context. It is dialed separately, so the queue may # live on a different cluster from the session store. nats_context: production # Bounds a task payload in bytes before anything decodes it. max_payload: 524288 Field Description queue work queue to consume, default FISK_AI task_type asyncjobs task type handled, default fisk-ai:run workers (int) jobs run at once, default 1 nats_context NATS context for the queue, defaulting to the top-level nats_context max_payload (int) payload cap in bytes before decoding, default 524288 A worker only claims tasks of its configured task_type. Submit a different type and the task stays in the queue until it expires, with no error logged at either end.\nSafety Publish permission on the queue is the only access control. Anyone who can enqueue a task of the configured type runs the full agent loop with prompt text of their choosing, against every tool the configuration allows. Restrict publish permission on the queue’s subjects the way any other NATS resource is restricted.\nThe rest of what applies to any served run is covered in Serving.",
    "description": "take whole units of work from a Choria work queue",
    "tags": [],
    "title": "Queued jobs",
    "uri": "/fisk-ai/channels/asyncjobs/"
  },
  {
    "breadcrumb": "Introduction \u003e Channels",
    "content": "The prompts channel takes a prompt from another agent over NATS and runs the agent loop over it. The caller waits and receives an acknowledgement, then the events the run produces, then the answer or the failure.\nNote The channel is opt-in: without an expose.agent.a2a.prompts block fisk serve answers no prompts. Available since Version0.0.5.\nConfiguration identity: nats-worker application_path: /usr/local/bin/nats nats_context: production system_prompt: | You operate NATS. Answer with what you did and what you found. llm: model: claude-sonnet-5 include: tools: - ^stream_ expose: agent: a2a: serve_tools: true tool_timeout: 60s prompts: workers: 2 Answering a prompt runs the whole agent loop, so the configuration needs identity, system_prompt, llm.model and nats_context. application_path is optional: an agent with only built-in tools, or with none, still answers prompts.\nidentity must be one you wrote. It is the subject peers reach this worker on and the queue group it joins, so a name taken from the application binary or left at the default would put unrelated agents into one group, sharing each other’s work.\n$ fisk serve --config prompts.yaml Serving nats-worker/1.2.0: Endpoints: a2a/prompts a2a Model: claude-sonnet-5 Agent Context: production Sessions: file Knowledge: disabled Telemetry: disabled Tool Directory: /var/lib/fisk-ai Tool Timeout: 5m0s Answering prompts over a2a: Requests: choria.fisk-ai.task.nats-worker Cancels: choria.fisk-ai.cancel.nats-worker.* Workers: 2 Answering a prompt runs the agent loop and reaches every tool the top-level include and exclude selected. Serving tools over a2a: Discovery: choria.fisk-ai.discovery.nats-worker Tools: choria.fisk-ai.tool.nats-worker Concurrency: 4 Tool Timeout: 1m0s Exposed: stream_ls stream_info workers is how many prompts the process answers at once. --workers does not change it; that flag applies to the queued-jobs channel.\nMaking requests A caller publishes a request on choria.fisk-ai.task.\u003cidentity\u003e:\n{ \"protocol\": \"io.choria.fisk-ai.v1.request.prompt\", \"id\": \"3Hzmp2RBLG713TfOTU5aJpATRg2\", \"request\": \"docs1\", \"conversation\": \"docs1\", \"sequence\": 0, \"time\": \"2026-08-16T11:00:00Z\", \"sender\": {\"name\": \"peer1\"}, \"prompt\": \"how many streams are there\" } request names the turn. Every reply to it echoes the value, cancelling the turn addresses it by that value, and so does answering a question the run asks, so pick it before you send and keep it. It must name one turn and one only: two turns sharing a value make their replies indistinguishable, and a cancel aimed at one of them stops both. It is at most 64 characters of letters, digits, - and _, because a worker builds subjects from it.\nid names the message rather than the turn, so it is fresh on every message including a resend, and conversation is the caller’s own tag across the turns of one conversation.\nProtocol Asks for Required Also takes io.choria.fisk-ai.v1.request.prompt a turn: the agent runs the prompt prompt context, tool_hints, budget, stream, conversation_token, replay, force io.choria.fisk-ai.v1.request.answer a question answered and the run resumed conversation_token, answer budget, stream, replay, force io.choria.fisk-ai.v1.request.resume a run that stopped part way continued conversation_token budget, stream, force io.choria.fisk-ai.v1.request.read the conversation read back, no turn taken conversation_token, replay nothing The queued-jobs channel takes a request.prompt as its payload and none of the other three.\nrequest.answer is described in Answering after the run ended, and request.read in Reading a conversation.\ncontext is supporting material offered alongside the prompt, stream: false asks for the answer without the event stream, and conversation_token joins an existing conversation, see Follow-up turns.\nforce is a caller’s decision about its own conversation. Without it a worker refuses a resume across a changed model, system prompt or tool set; with it the run continues under the current configuration and drops the standing approvals it can no longer vouch for.\nA budget above the worker’s own configuration is ignored. On a conversation it limits the conversation rather than the turn, since a run measures the whole journal’s token count against it.\nThe reply set arrives on the request’s own inbox, in order:\nMessage When ack once, first, saying whether the prompt was taken event.\u003ckind\u003e zero or more, carrying the run’s output as it is produced elicit.request.\u003ckind\u003e a question the run puts to the caller, only when elicit is set result the answer, with its stop reason and token usage error instead of a result when the run did not produce one The acknowledgement comes first, so a plain nats req receives it and stops there:\n$ nats req choria.fisk-ai.task.nats-worker \"$(cat request.json)\" {\"protocol\":\"io.choria.fisk-ai.v1.ack\",\"id\":\"3Hzmp3SCMH824UgPUV6bKqBUSh3\",\"request\":\"docs1\", \"conversation\":\"docs1\",\"sequence\":1,\"time\":\"2026-08-16T11:24:10.749134Z\", \"sender\":{\"name\":\"nats-worker\"},\"recipient\":{\"name\":\"peer1\"},\"accepted\":true} Every message of the set carries sequence, numbered from the acknowledgement without gaps, so a caller can tell a lost event from a quiet run. Events are advisory. The answer is in the terminal message, and the worker’s run journal is the authoritative transcript.\nEvent blocks Each event holds one block. Where an id under io.choria.fisk-ai.v1.event. is one your client does not recognize, keep the message and render what you can rather than rejecting it: a newer worker sends kinds this one does not define.\nProtocol Fields What it is io.choria.fisk-ai.v1.event.text text, final the model’s prose io.choria.fisk-ai.v1.event.thinking text the model’s reasoning, when it produces any io.choria.fisk-ai.v1.event.tool_call id, name, input a tool the run is about to invoke io.choria.fisk-ai.v1.event.tool_result call_id, output, is_error what that call returned io.choria.fisk-ai.v1.event.agent_call id, name, task a question delegated to a peer agent io.choria.fisk-ai.v1.event.warning kind, name, count, params, error an advisory the run raised io.choria.fisk-ai.v1.event.prompt text a turn somebody asked for; sent only in a replay io.choria.fisk-ai.v1.event.status iteration, usage, phase, count, truncated progress, and the markers around a replay A text event in full:\n{\"protocol\":\"io.choria.fisk-ai.v1.event.text\",\"id\":\"3Hzmp8kRt1BqA4dQ2v9XnLcYm2T\",\"request\":\"docs1\", \"conversation\":\"docs1\",\"sequence\":2,\"time\":\"2026-08-16T11:24:11.104217Z\", \"sender\":{\"name\":\"nats-worker\"},\"block\":{\"text\":\"the stream is gone\",\"final\":true}} final marks the answer. Only the run knows which message ended the turn, so without the flag a caller cannot tell the answer from the narration on the way to it, and would render it twice when the same text arrives again in the result.\nA warning names its kind and gives you the values, not a finished sentence. Your client chooses the wording, and a client that does not recognize a kind can still display the fields.\nA tool_call is not answered twice. A call the caller was asked to approve carries the same tool_use_id as the elicit.request.approve that asked, so a caller that drew the question knows it has already shown that call.\nNot every call produces a result: a denied confirmation, a tool called without its required arguments, a tool that answers later and an aborted run each end without one, so a caller pairing the two tolerates a call that is never answered.\nA status block reports progress, and its usage is what one model call consumed. The call that ends a turn sends no status of its own, so a caller keeping a running total takes the totals from the terminal message rather than summing these. The replay markers use the same block and are described below.\nRefusals and endings The worker refuses a request it cannot parse with a NATS service error, before any acknowledgement:\n$ nats req choria.fisk-ai.task.nats-worker '{\"protocol\":\"io.choria.fisk-ai.v1.request.resume\",\"prompt\":\"...\", ...}' Nats-Service-Error: the request is not a valid v1 message: jsonschema validation failed with 'https://choria.io/schemas/io.choria.fisk-ai.v1/request.resume.json#' - at '/prompt': false schema Nats-Service-Error-Code: 400 A resume takes no prompt. Send io.choria.fisk-ai.v1.request.prompt to run one.\nEverything the worker refuses after that is an ack with accepted: false and a reason, followed by an error that closes the set. The error carries a code the caller can branch on:\nCode Meaning capacity every worker slot is busy; retry, or ask another instance duplicate_request a run with this request id is already in flight here draining the worker is shutting down and never started this run not_started the prompt was taken and the worker stopped before running it failed the run ran and failed; the message says how crashed a bug in this software; the detail stays in the worker’s log canceled the run was stopped before it finished, by the worker rather than by the caller suspended the run stopped at a resumable point, which is what a caller’s own cancel reaches deferred a tool will answer later, so the run is parked unknown_conversation the conversation_token names no conversation here; send the prompt without one conversation_busy a turn of this conversation is running here; wait for its terminal message turn_not_taken the conversation could not take the turn, and the prompt did not run budget_exhausted the conversation has used its whole token allowance and is finished unknown_call no such call is waiting for an answer already_answered the call already has an answer answer_too_large the answer is over 256KB The worker refuses at capacity rather than queueing the prompt. unknown_call, already_answered and answer_too_large are permanent: sending the same answer again reaches the same reply.\nbudget_exhausted ends the conversation, not just this request. The allowance belongs to the conversation, so every later turn is refused no matter who sends it. Your prompt did not run and was not recorded. To carry on, send a prompt with no conversation_token to start a new conversation. Only an operator on the machine running the agent can raise llm.budget.max_tokens.\nYou can also hit this straight away, on a conversation that answered a moment earlier, by lowering budget on your own request below what the conversation has already used.\nEvery error also has a stop_reason beside its code. budget_exhausted appears there when a run hit the cap part way through a turn instead of before it started.\nA deferred run is waiting for a tool answer. The error lists the calls. Answer one on a request carrying the conversation token, or with fisk session on the worker holding the journal.\nCanceling A caller cancels by publishing an io.choria.fisk-ai.v1.cancel on choria.fisk-ai.cancel.\u003cidentity\u003e.\u003crequest\u003e, where request is the correlation id it sent. The request id is part of the subject, so only the worker running that prompt is subscribed to it. It answers with an ack.\nA cancel asks the run to stop where the conversation can be continued. It does not end the run where it stands: the loop polls for it at each boundary and parks there, so the terminal message is suspended with the usage the turn spent, and the conversation takes another turn whenever the caller sends one. A run blocked on a question is included, since a cancel closes the question rather than leaving it asked with nobody to answer.\nWhat that costs is the ability to stop a model call in flight. A run inside a tool that never returns reaches no boundary, and a cancel will not move it; that escape hatch belongs to whoever operates the worker. A caller asks and an operator compels.\nBecause the id is part of the subject, a caller mints it rather than being told it: set id and request on the request before sending, so the tag is in hand before there is anything to cancel.\nA no-responder error means this instance is not running that request: it was never accepted, it already finished, or another instance took it.\nFollow-up turns A caller can send another turn of the same conversation. Every ack that accepts a prompt carries a conversation_token, and a later request carrying that token runs its prompt as the conversation’s next turn:\n{\"protocol\":\"io.choria.fisk-ai.v1.ack\",\"id\":\"3Hzmp3SCMH824UgPUV6bKqBUSh3\",\"request\":\"docs1\", \"conversation\":\"docs1\",\"sequence\":1,\"time\":\"2026-08-16T11:24:10.749134Z\", \"sender\":{\"name\":\"nats-worker\"},\"recipient\":{\"name\":\"peer1\"},\"accepted\":true, \"conversation_token\":\"3Hzmp8VqrKL42NmXcPd7bTgWfR1\"} { \"protocol\": \"io.choria.fisk-ai.v1.request.prompt\", \"id\": \"3Hzmq7WdPK628XjRVZ8cLmBUTh4\", \"request\": \"docs2\", \"conversation\": \"docs1\", \"sequence\": 0, \"time\": \"2026-08-16T11:26:00Z\", \"sender\": {\"name\": \"peer1\"}, \"prompt\": \"what is the first one called\", \"conversation_token\": \"3Hzmp8VqrKL42NmXcPd7bTgWfR1\" } A caller that asks once and stops ignores the token the worker handed it; one that wants another turn sends the token it already has. A follow-up opens a reply set of its own, with its own ack, events, cancel address and terminal message, so it is an ordinary request in every respect but which conversation it joins.\nNo worker holds a conversation between turns. Each turn loads the journal, runs, and stores the result, so any instance in the queue group serves any turn. That also means a caller sends one turn at a time: a second turn sent while the first is still running is refused with conversation_busy, and it must wait for the first turn’s terminal message rather than try another instance.\nA conversation has no end state and no expiry: the journal stays in the session store until an operator removes it.\nA turn cannot join a conversation waiting on a deferred tool result. The worker answers turn_not_taken without running the prompt. With elicit set, a human-in-the-loop question the caller neither answers nor holds open within request_timeout leaves the conversation waiting on a deferred call. Answer the question and it takes turns again. A configuration change ends a conversation. Every turn is a resume, and the worker refuses a resume when the model, the system prompt, the thinking mode or the reasoning effort has changed since the conversation started. It answers failed and the caller starts a new conversation. A changed tool set does not end it: the turn runs, and the standing approvals the conversation held are dropped, since an approval names a tool and that tool may have moved under it. The usage on a result counts the whole conversation rather than the turn, since it is read from the journal. An error that ran and stopped carries it too, so a caller can tell what it owes for a turn it is about to continue. Both also carry trace_id, the trace the worker recorded, which is empty when it exports no telemetry, and content_exported, which says whether this turn’s conversation itself reached that collector.\nAsking what an agent is Ask an agent what it is before you send it anything. Run fisk discover \u003cidentity\u003e to make that request. Every agent that answers prompts also answers discovery, whether or not it serves tools as well. An agent that serves no tools to peers answers with a card that lists none.\nTwo fields on the card describe what the agent does with a conversation:\nField Meaning telemetry the agent exports traces of what it does telemetry_content those traces carry the conversation itself, so a prompt sent here reaches the operator’s collector They are published because a caller should know before it sends a prompt, and they are read off the worker’s resolved telemetry provider rather than its configuration, so a rejected endpoint does not leave the card promising an export that will not happen. The card says what the agent is configured to do; content_exported on a terminal message says what a turn actually did.\nReading a conversation To read a conversation back, send an io.choria.fisk-ai.v1.request.read with a conversation_token and a replay count. The worker sends that many blocks of the stored conversation and ends the reply set:\n{ \"protocol\": \"io.choria.fisk-ai.v1.request.read\", \"id\": \"3Hzmr9YfRM839ZlTXb0eNoDVUj6\", \"request\": \"docs3\", \"conversation\": \"docs1\", \"sequence\": 0, \"time\": \"2026-08-16T11:30:00Z\", \"sender\": {\"name\": \"peer1\"}, \"conversation_token\": \"3Hzmp8VqrKL42NmXcPd7bTgWfR1\", \"replay\": 200 } Use this to show a conversation your client did not see live, such as one started on another machine. A finished turn leaves a completed journal, and a plain resume will not continue one, so reading it is the only request such a conversation accepts until you send the next prompt.\nYou get back the same blocks the run sent the first time, between two status blocks:\nphase: \"replay_start\" opens the history. phase: \"replay_end\" closes it, with count blocks sent, truncated when older ones were left behind, and usage for what the conversation has consumed so far. That usage is the whole conversation rather than one call, which is what lets a caller seed a running total before this turn’s own calls arrive. Set replay on each request that needs it. Leave it off a follow-up turn, which usually wants only the new blocks. The worker sends at most 200 blocks whatever you ask for, and rounds up to a whole turn so that a result never arrives without the call it answers. The largest useful value is therefore 200; ask for more and you get 200. A read asks for at least 1.\nSome of what the journal holds never leaves the worker: thinking signatures, the fingerprint, the caller, the conversation token, the standing approvals, and the notes and handles of deferred calls. Long values are trimmed to fit a block.\nio.choria.fisk-ai.v1.request.resume continues a run that stopped part way, which is what a caller sends after a suspended ending. Send it with the token and no replay.\nAnswering questions A run puts a question back to the caller when it needs a person: an approval for a confirmation-gated command, or one of the three human-in-the-loop questions. A run asks only when elicit is set.\nWarning Anyone who may answer this identity’s questions can approve a confirmation-gated command in a run. An answer carries no verified caller identity.\nexpose: agent: a2a: prompts: workers: 2 elicit: true The worker sends the question on the reply set, after the ack and before the result or error:\n{ \"protocol\": \"io.choria.fisk-ai.v1.elicit.request.approve\", \"id\": \"3Hzq6PkmVLsT9WqrChXkgF7NLwy\", \"request\": \"docs1\", \"conversation\": \"docs1\", \"sequence\": 4, \"time\": \"2026-08-16T11:24:11.912084Z\", \"sender\": {\"name\": \"nats-worker\"}, \"recipient\": {\"name\": \"peer1\"}, \"question_id\": \"3Hzq7RvnWMtU0XstDiYlhG8OMxz\", \"tool_use_id\": \"toolu_01A9bK2mNpQr\", \"command\": \"stream rm\", \"display\": \"stream rm ORDERS --force\", \"tag\": \"ai:confirm\", \"wait_ms\": 120000 } Every question has question_id, and may have tool_use_id and wait_ms:\nProtocol Asks Fields io.choria.fisk-ai.v1.elicit.request.approve whether a gated command may run command, display, and usually tag io.choria.fisk-ai.v1.elicit.request.confirm a yes or no question question io.choria.fisk-ai.v1.elicit.request.select one of a list question, options io.choria.fisk-ai.v1.elicit.request.input a free text value question, and default when it pre-fills tag is absent when the gate could name no trigger, which is a command rewritten to a tool that has none.\nThe caller answers on choria.fisk-ai.elicit.\u003cidentity\u003e.\u003crequest\u003e, where request is the correlation id it sent:\n$ nats req choria.fisk-ai.elicit.nats-worker.docs1 \"$(cat answer.json)\" { \"protocol\": \"io.choria.fisk-ai.v1.elicit.reply.approve\", \"id\": \"3Hzq8TwoXNuV1YtuEjZmiH9PNya\", \"request\": \"docs1\", \"conversation\": \"docs1\", \"sequence\": 0, \"time\": \"2026-08-16T11:24:19.310422Z\", \"sender\": {\"name\": \"peer1\"}, \"question_id\": \"3Hzq7RvnWMtU0XstDiYlhG8OMxz\", \"choice\": \"once\" } Reply under the id you were asked under, with request swapped for reply:\nProtocol Field Values io.choria.fisk-ai.v1.elicit.reply.approve choice no, once, always io.choria.fisk-ai.v1.elicit.reply.confirm confirmed true, false io.choria.fisk-ai.v1.elicit.reply.select index a position in options io.choria.fisk-ai.v1.elicit.reply.input value any string, empty included io.choria.fisk-ai.v1.elicit.reply.no_operator none no operator is available Send the field even when its value is the zero one. confirmed: false, index: 0 and value: \"\" are each an answer somebody gave.\nio.choria.fisk-ai.v1.elicit.waiting arrives on the same subject and is not an answer. It says the caller is holding the question open, see Holding a question open.\nThe worker replies with an ack. An answer to a question it is not waiting on gets a 404, as does an answer sent after the question’s window closed, and as does a waiting sent after the question was answered.\nonce runs the command that one time. always stops the worker asking about that tool for the rest of the run. no and no_operator both leave the command unrun and tell the model the refusal is final.\nThe worker holds the question for expose.agent.a2a.request_timeout, and its worker slot with it. The question’s wait_ms carries that number, so the caller knows how long it has. An unanswered question ends the run differently depending on what asked it:\nan approval leaves the command unrun, and the run ends with suspended a human-in-the-loop tool leaves its call deferred, and the run ends with deferred Both can be answered later, see Answering after the run ended. An operator on the worker holding the journal answers a deferred call with fisk session instead.\nHolding a question open A person reading a command approval can take longer than two minutes. A caller with the question in front of somebody sends an io.choria.fisk-ai.v1.elicit.waiting, and each one restarts the window:\n{ \"protocol\": \"io.choria.fisk-ai.v1.elicit.waiting\", \"id\": \"3Hzq9UxpYOvW2ZuvFk0njI0QOzb\", \"request\": \"docs1\", \"conversation\": \"docs1\", \"sequence\": 0, \"time\": \"2026-08-16T11:25:59.104812Z\", \"sender\": {\"name\": \"peer1\"}, \"question_id\": \"3Hzq7RvnWMtU0XstDiYlhG8OMxz\" } The rules a client follows:\nSend a waiting every wait_ms / 3, starting when the question goes on screen. The window restarts when the worker receives the message, so the remaining two thirds cover the round trip and one lost message. In Go, a2a.NewWaitingAck(question, sender) builds the message and question.AckInterval() is the interval. Stop before sending the answer. A waiting that arrives after the answer is refused, since the worker has finished with the question. A 404 means the question is gone: take it off the screen and send no answer, since that would be refused too. A 400, or a question with no wait_ms, comes from a worker older than this feature. Answer inside the window instead. Send no_operator when the person walks away. waiting says somebody is there to answer, and no_operator ends the question at once. Silence leaves the command unrun too, but only after a whole window. The reply set is silent while the worker holds the question, so a client learns the worker is still there only from the ack to each waiting. A caller that sends no waiting either answers within the window or answers later, on a request of its own.\nAnswering after the run ended A person closes a laptop with a question on screen. The waiting messages stop, the window runs out, and the run ends suspended or deferred. The worker unsubscribes from choria.fisk-ai.elicit.\u003cidentity\u003e.\u003crequest\u003e with the task, so an hour later their answer reaches no responder.\nThey send an io.choria.fisk-ai.v1.request.answer instead, with the conversation token:\n{ \"protocol\": \"io.choria.fisk-ai.v1.request.answer\", \"id\": \"3Hzms0ZgSN940AmUYc1fOpEWVk7\", \"request\": \"docs4\", \"conversation\": \"docs1\", \"sequence\": 0, \"time\": \"2026-08-16T12:40:03Z\", \"sender\": {\"name\": \"peer1\"}, \"conversation_token\": \"3Hzmp8VqrKL42NmXcPd7bTgWfR1\", \"answer\": { \"tool_use_id\": \"toolu_01A9bK2mNpQr\", \"kind\": \"approve\", \"answer\": \"choice\", \"choice\": \"once\" } } Copy tool_use_id and kind from the question. A resumed run mints a new question_id, so the answer names the call instead.\nThe answer object has kind and answer of its own. answer names the field holding the decision, and kind says what that decision means where the value alone cannot: no_operator looks the same whichever question was asked, and value serves both input and select.\nField Value tool_use_id the call the question named kind approve, confirm, select or input answer choice for approve, confirmed for confirm, value for select and input, or no_operator choice no, once or always confirmed true or false value the text for input, and the chosen option for select A selection names the option, not its position.\nYou get back the usual ack, events, and a result or an error. The conversation gains no turn. A deferred call takes the answer as its result; an approval is asked again by the resume and answered from the request.\nA 400 means the answer does not fit its kind, or the message has no token, or it came with a prompt.\nConcurrency and shutdown Each prompt holds a worker slot from acknowledgement until the run ends. No setting limits total run time; harness.tool_timeout limits a tool call and llm.budget.call_timeout limits a model call. With workers: 1, one long run makes the worker refuse every other caller until it finishes. A run whose caller keeps sending waiting is one such run, and it holds its slot for as long as the caller sends them.\nAn interrupt starts a drain, which takes the identity out of its queue group, so the worker accepts no further prompts. The worker waits for runs already under way and answers their callers. A prompt acknowledged but not started ends with draining. A second interrupt cancels the runs in flight and answers each caller with failed.\nA drain stops restarting the window of a question already outstanding, so it ends within one window and the runs behind it finish. A caller sending waiting at that point still gets an ack, but the ack no longer restarts the window.\nTool selection A run started this way reaches every tool the top-level include and exclude selected, exactly as a queued job does. expose.agent.tools selects what peers may invoke directly over MCP and a2a, and does not affect a run.\nA command tagged ai:confirm, or a configured confirm tag, needs an approval before it runs. Without elicit the run has no operator to ask, so the model sees the tool, calls it, and the worker refuses the call before the command runs. The worker logs how many such tools the run loaded. With elicit the question goes to the caller, as Answering questions describes.\nSessions The worker mints a conversation token and journals every run under its hash. A crash leaves a resumable run, and a deferred tool call has a journal to answer into. A caller holding the token continues that conversation; a caller that wants the work redone from scratch sends the prompt without one.\nconversation on a request is echoed on every reply and never names a journal. It is the caller’s own correlation tag, free for grouping whatever it likes, and the token names the conversation.\nJournals from this channel are named t- and a hash, so an operator reading fisk session ls can tell a prompt’s journal from a queued job’s. A session listing shows the last run’s outcome, so a conversation resting between turns reads as completed, and the prompt column shows the conversation’s first prompt.\nThe worker records the token and the caller’s claimed name with the journal, so a caller that lost a token can ask an operator for it instead of losing the conversation. Find the conversation with fisk session ls, which lists the first prompt and the time each journal was last touched, then read both values with fisk session show \u003cid\u003e. The listing has no token column, because a token is a credential and ls output is often pasted into tickets.\nSafety NATS publish permission on choria.fisk-ai.task.\u003cidentity\u003e is the only access control: anyone holding it runs this agent’s tools against a prompt of their choosing. A request carries no verified caller identity, and sender is an unverified claim the worker records and logs.\nA caller needs publish on the request subject. Canceling needs publish on choria.fisk-ai.cancel.\u003cidentity\u003e.\u003e, and answering questions needs publish on choria.fisk-ai.elicit.\u003cidentity\u003e.\u003e. The reply set arrives on the caller’s own inbox.\nAnyone holding the cancel permission who learns a request id can cancel a run they did not start. Anyone holding the answer permission who learns a request id and a question id can approve a confirmation-gated command in a run they did not start, and can hold that run’s worker slot for as long as they keep sending waiting.\nA conversation_token is a credential on the same terms: holding it is the authorization to add a turn to that conversation, and any holder can continue a conversation, whoever started it. It carries more than a fresh prompt does, because a standing approval an earlier turn recorded is restored with the conversation, so with elicit set a turn can reach a confirmation-gated command that somebody else approved. Tokens carry 128 bits of randomness and cannot be guessed, so treat one as a secret: this agent neither logs it nor puts it in an error message, and a caller should not either.\nThe session store is shared with the other channels of this identity, and each names its journals in a space of its own: a conversation here is a hash of the identity and the token, and a queued job is a hash of the identity and its task id. So a queue submitter that learns one of these journal ids and spells it as a task id gets a journal of its own rather than this conversation.\nThe worker records the token in the conversation’s journal, so anyone who can read the session store can read the token and continue that conversation. This gives away no access that reading the store did not already give, since the same access reads and writes those journals directly, but the store needs the same protection the tokens do. The caller’s name is recorded beside the token, and it is the unverified claim from the sender field.\nThe worker logs the caller, the request id and the session as a prompt is accepted, runs and ends:\nlevel=INFO msg=\"Accepted a prompt\" channel=a2a/prompts request=docs1 caller=peer1 session=3Hzq5OghUNlK8SnoBgVjfE6MKvx prompt_bytes=26 level=INFO msg=Running channel=a2a/prompts work=3Hzq5OghUNlK8SnoBgVjfE6MKvx caller=peer1 caller_verified=false resume=3Hzq5OghUNlK8SnoBgVjfE6MKvx level=INFO msg=\"Ending a run\" channel=a2a/prompts request=docs1 caller=peer1 session=3Hzq5OghUNlK8SnoBgVjfE6MKvx code=failed reason=\"llm call: 401 Unauthorized\" The worker logs the window it gave a question and, when the question closes, how long it held it and how many waiting messages the caller sent. An operator reads from these which caller is holding a worker, and for how long:\nlevel=INFO msg=\"Asked the caller a question\" channel=a2a/prompts request=docs1 caller=peer1 question=3Hzq7RvnWMtU0XstDiYlhG8OMxz kind=approve wait_ms=120000 level=INFO msg=\"A question was answered\" channel=a2a/prompts request=docs1 caller=peer1 question=3Hzq7RvnWMtU0XstDiYlhG8OMxz held=14m32s acks=21 The tool safety rules in the Reference apply here as everywhere else.",
    "description": "take prompts from other agents and stream the run back to them",
    "tags": [],
    "title": "Answering prompts",
    "uri": "/fisk-ai/channels/prompts/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "Knowledge gives an agent search tools over a locally built index of its own markdown and text documents. They run in-process and cite what they return, so it can ground its answers in project documentation rather than its training data.\nIn AI terms this is a RAG (retrieval-augmented generation) system contained entirely in a single binary and a single process. It is aimed at keeping source data local and private. It runs with or without a local embedding model; without one it uses full-text search alone.\nEverything ships in the one fisk binary. The index is a single SQLite file built and queried in-process, with no external database. A local embeddings server is the only optional external process, and only when semantic search is turned on.\nEnabling knowledge Knowledge is off by default. You get full-text search without any LLM requirements.\nharness: knowledge: enabled: true paths: - docs/ Build the index and search it from the command line, then run the agent, which now has the knowledge tools:\n$ fisk knowledge index docs/ # build the index, incremental, no embeddings needed $ fisk knowledge search \"backpressure\" $ fisk run \"how does backpressure work?\" The index is incremental. A second knowledge index re-reads only files whose content changed, detected by hash, and reconciles deletions when a full configured root is walked.\nTwo retrieval tiers The lexical tier is always on, has no dependencies, and is the default. Vector search is opt-in and requires an embeddings model.\nLexical search Lexical search finds only exact words present in the text. Synonyms and concepts do not match.\nThe lexical tier is an FTS5/BM25 full-text index. It is always active when knowledge is enabled and needs no embedding model or other dependencies. Command output calls this tier lexical.\nVector search Semantic search recalls on meaning rather than wording. A natural-language question finds the right section even when it shares no keywords with it: asking “how do I stop the agent spending too much” can surface the section on budgets though that section never says “spending”. This suits an agent, which phrases a search in its own words rather than the documents’ exact terms.\nFusing the two tiers keeps lexical’s precision on named terms while adding this semantic reach, so the hybrid result is usually better than either tier alone.\nIt needs a local embedding server, such as Ollama or LM Studio, running at both index and query time.\nAdd an embeddings block to turn on the vector tier. When it is set, each chunk is embedded through a local OpenAI-compatible embeddings server, and a query is answered by fusing the lexical and vector rankings with Reciprocal Rank Fusion behind the one search call.\nharness: knowledge: enabled: true paths: - docs/ embeddings: base_url: http://127.0.0.1:1234/v1 model: text-embedding-embeddinggemma-300m text-embedding-embeddinggemma-300m, used in the examples here, is a good default to start from for local embedding: a small (300M-parameter) Gemma-based model that runs comfortably on CPU or modest hardware, is multilingual, and is well supported by the local runtimes this feature talks to, such as Ollama and LM Studio. The feature stays model-agnostic, any OpenAI-compatible endpoint works, and it is a sound default absent a specific reason to prefer another.\nThe embedding model is user-chosen, so nothing about it is assumed. fisk knowledge doctor probes the configured server and reports the model, its vector dimension, and whether its output is normalized. After turning embeddings on, rebuild the index so the vectors are populated:\n$ fisk knowledge doctor $ fisk knowledge index --reindex $ fisk knowledge stats Changing the model, its dimension, or a prefix changes the vector identity and forces a --reindex. The index refuses a mismatched model upfront, before embedding anything, rather than silently returning wrong rankings.\nTier line All invocations of related tools will print a line indicating configuration and active state:\ntier: lexical (FTS5) - no embeddings configured tier: hybrid (FTS5 + vectors, RRF) - model=\u003cname\u003e dim=\u003cn\u003e tier: hybrid -\u003e DEGRADED to lexical (embeddings unreachable: \u003creason\u003e) A configured embeddings server that is unreachable at query time degrades to lexical-only, rather than failing the search. A configured embeddings server that is unreachable at index time errors, so an index the user asked to be semantic is never silently built lexical-only.\nWhen to enable embeddings Start with lexical search. It has nothing to run and no per-query cost, and it is often enough on its own. Add embeddings when the searches that matter are worded differently from the documents.\nAspect Lexical (default) Hybrid (with embeddings) Matches on shared words, exact terms meaning, plus shared words Best for identifiers, command names, error strings natural-language questions, paraphrased queries Needs nothing beyond the binary a local embedding model and server Per-query cost none one embedding call Index cost text index only a --reindex to embed the corpus The two are not exclusive: enabling embeddings keeps the lexical tier and fuses the two, so nothing is lost by turning it on beyond the extra model to run.\nConfiguration The harness.knowledge block mirrors harness.memory. An absent block, or enabled: false, means off.\nharness: knowledge: enabled: true paths: - docs/ directory: \"\" top_k: 5 max_injected_tokens: 6000 embeddings: base_url: http://127.0.0.1:1234/v1 model: text-embedding-embeddinggemma-300m api_key_env: RAG_EMBED_KEY timeout: 30s query_prefix: \"\" document_prefix: \"\" Field Description enabled (boolean) turns the feature on; absent or false means off paths (array) default index roots used when knowledge index is run with no path argument directory (string) store location; a relative value resolves under the store base when set, else the working directory; default knowledge/\u003cidentity\u003e top_k (integer) default retrieval count, default 5, hard ceiling 20 max_injected_tokens (integer) cap on the total retrieved text fed to the model, default 6000 embeddings optional block; its presence turns on the vector tier An absolute directory is used as-is; a relative value, including the default knowledge/\u003cidentity\u003e, resolves under the store base when one is set and against the working directory otherwise. The identity is the agent’s name, so two agents pointed at the same directory share an index and the default keeps each agent’s index its own.\nThe store base is a deployment concern for running many agents in one process, not an agent setting: a programmatic caller passes store_dir, and the knowledge command takes a matching --store-dir flag or FISK_AI_STORE_DIR environment variable. An absolute directory both the agent and the knowledge command read from the same config needs neither, and is the surest way to keep them pointed at the same index.\nEmbeddings The embeddings block is only read when the vector tier is on. It describes a local OpenAI-compatible endpoint that fisk POSTs to at \u003cbase_url\u003e/embeddings.\nField Description base_url (string) OpenAI-compatible base URL; requests go to \u003cbase_url\u003e/embeddings model (string) the embedding model name to request api_key_env (string) name of an environment variable holding the API key, never the secret itself; optional timeout (duration) per-request timeout, default 30s query_prefix (string) text prepended to a query before embedding; optional, default empty document_prefix (string) text prepended to a chunk before embedding, supports {title}; optional, default empty api_key_env names an environment variable rather than carrying the secret, so no secret lives in agent.yaml and none is logged. Prefixes default to empty because the model is user-chosen and a wrong prefix is worse than none; the models that need one document it. Run knowledge doctor to see whether a chosen model expects a prefix.\nNote A non-loopback base_url must use https. The embeddings endpoint is only ever contacted when the vector tier is on; the lexical path makes no network calls.\nEmbeddingGemma prefixes text-embedding-embeddinggemma-300m is trained with task-specific prompts, so it expects a prefix on both sides: a query is embedded under a retrieval instruction and a document under a title-and-text template. Setting them to the model’s documented values improves retrieval; leaving them empty still works but embeds text bare, the way the model was not trained to see it.\nharness: knowledge: enabled: true paths: - docs/ embeddings: base_url: http://127.0.0.1:1234/v1 model: text-embedding-embeddinggemma-300m # trailing space is required query_prefix: \"task: search result | query: \" document_prefix: \"title: {title} | text: \" The agent tools When knowledge is enabled the agent is offered these tools, along with instructions.\nknowledge_search knowledge_search runs the lexical search, adds and fuses the vector search when the vector tier is on, and returns the ranked sections.\nEach result carries a citation token of the form \u003crelpath\u003e#\u003cordinal\u003e, the file path relative to the index root and the chunk’s position in that file, alongside the human-readable heading path of the section.\nResults are returned to the model as untrusted reference data, framed as material to draw on rather than as instructions. When the store has no index yet the tool returns a soft index_not_built status rather than failing the run, so a missing index never bricks agent startup.\nknowledge_enumerate Version0.0.4 knowledge_enumerate is the tool form of knowledge match, with the same syntax. The model routes here before answering that something is absent, then reads what it needs with knowledge_search.\nCLI commands The fisk knowledge command builds and inspects the index. It is separate from the agent’s tools; the CLI never runs the agent. Every command reads --config (default agent.yaml) and prints the tier line.\nCommand Description knowledge index [paths...] incremental build; requires a path argument or a configured knowledge.paths knowledge watch [paths...] watch the configured paths and re-index on change, coalescing edit bursts knowledge search \u003cquery\u003e retrieve from the CLI for tuning; prints citation, heading, and a snippet knowledge match \u003cquery\u003e list every document containing the words, as a complete set; aliases enumerate, which Version0.0.4 knowledge words [pattern] list the words the documents actually use, with document counts; aliases vocab, terms Version0.0.4 knowledge show \u003crelpath#ordinal\u003e print one chunk verbatim, resolving a citation token knowledge sources list indexed files with chunk counts and last-indexed time knowledge doctor preflight and general consistency checks for the index and embeddings requirements knowledge rebuild rebuild the search index from the stored text, without re-embedding Version0.0.4 knowledge stats tier banner, document and chunk counts, vector count, pinned model, store size knowledge rm \u003csource...\u003e remove specific sources’ chunks by path knowledge reset wipe the index; the bare form refuses and names --force Indexing is incremental and per-file: a file whose hash is unchanged is skipped, a changed file is re-chunked, and a walk of a full configured root reconciles deletions. Indexing walks markdown and text files only, by the .md, .markdown, .txt, and .text extensions, and always excludes the store directory itself and the memory/ directory.\nWhich documents mention a word Version0.0.4 The search functions answer “what do the documents say about this” and return the sections that scored best.\nknowledge match answers what search cannot: which documents mention a word. It returns a complete list of all matching documents. An empty result means the documents do not contain the words.\n$ fisk knowledge match \"retention policy\" $ fisk knowledge match deprecated --paths-only $ fisk knowledge which api -deprecated Complete, not literal The set is complete: every indexed document containing the words is listed. It is not literal. Matching is by word stem, so deprecated also finds deprecate and deprecation, and it merges words that share a stem: universe and university both reduce to univers. Case always folds. Diacritics fold only within Latin-1, so cafe finds café, but strasse does not find Straße and viet does not find Việt. A complete answer therefore includes documents about a word that was not typed, and excludes spellings it might have been expected to reach.\nQuery syntax Form Example Matches words side by side deprecated api documents containing both, anywhere in the document a quoted phrase \"retention policy\" the words adjacent, within one section a leading minus api -deprecated documents with the first and without the second body: body:retention the section body only, not its heading heading: heading:retention the section heading breadcrumb only What words the documents use Version0.0.4 knowledge words lists the vocabulary of the index, which is every word the documents actually contain.\n$ fisk knowledge words # the whole vocabulary $ fisk knowledge words depre # only words containing \"depre\" $ fisk knowledge words '^depre' # only words starting with it The argument is a regular expression used to narrow the listing.\nA short list is shown with its counts, since a short list is there to be compared:\nWord Stem As written Any form deprecation deprec 9 17 deprecated deprec 6 17 deprecate deprec 2 17 As written counts documents holding that exact word. Any form counts documents holding any word sharing its stem, which is the number knowledge match \u003cword\u003e reports.\nA long list is shown as plain words several to a line, because a vocabulary runs to thousands of words and is scanned for one rather than read row by row.\nStore location and layout The index is project-local by default. It lives at knowledge/\u003cidentity\u003e relative to the working directory, supporting the one-project-per-directory workflow where an agent.yaml, a memory/ directory, and a knowledge/ directory sit side by side. A store base relocates that default under it, and the directory field overrides the location outright.\nWarning The store uses SQLite WAL and its shared-memory sidecar, so every process must be on the same machine. Do not place the store on a network filesystem such as NFS or SMB.\nServing over MCP Both knowledge tools can be served over MCP as well as to the agent. Exposure is off by default and enabled by naming them in an allowlist:\nexpose: agent: mcp: port: 8080 builtins: - knowledge_search - knowledge_enumerate Name both. A client that can rank but cannot enumerate cannot tell an absent term from a low-scoring one, which is the whole reason the second tool exists. See MCP for binding, ports, and the rest of the serving configuration.\nSecurity The index holds the verbatim text of every indexed document, unencrypted on disk. The file and its sidecars are created 0600 inside a 0700 directory.\nRetrieved chunks are framed as untrusted reference data and stripped of terminal control sequences before any TUI render, so indexed text cannot spoof the display or inject instructions. Embeddings secrets are supplied by environment-variable name and never logged, and are stripped from the environment of model-chosen command tools, so a tool cannot read the embeddings credential. A non-loopback embeddings base_url must use https, and the request timeout is enforced. Over MCP two gates apply, and both must pass: the tool itself declares whether it may ever be served over MCP, and the allowlist selects which of those this operator wants served. The allowlist can only narrow the tools declared servable, never widen past them, so a tool added alongside knowledge_search is not served on the strength of its neighbor’s entry. That holds between the two knowledge tools themselves: allowlisting one never serves the other. Only the two read-only knowledge tools declare MCP exposure; no index or write path is reachable over MCP, and no built-in declares a2a exposure at all. knowledge_enumerate returns a complete set rather than a ranked sample, so a client that can reach it can inventory which documents mention which terms without reading any of them. That is less text than knowledge_search discloses per call and more structure. Both matter when deciding what to bind.",
    "description": "Search local documents from an agent",
    "tags": [],
    "title": "Knowledge (RAG)",
    "uri": "/fisk-ai/knowledge/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "A channel supplies work to an agent and returns the answer. A work queue and a NATS request subject are channels today; an HTTP listener or a caller in the same process would be channels too.\nThe fisk serve command hosts an agent behind the channels. The queued-jobs channel polls a work queue. The prompts channel answers a request on a NATS subject. The agent loop is the same either way and does not see the difference.\nfisk serve also hosts endpoints that produce no work. Serving tools answers another agent’s tool call directly, running one tool. It starts no agent loop, so the behavior on this page does not apply to it.\nNote Queued jobs and prompts from other agents are the channels that ship today.\nChannels and fisk serve are available since Version0.0.5.\nChannel capabilities Channels differ in what they can offer a run:\nCapability Description Streaming whether a caller sees output as it is produced Elicitation whether a run can ask a person a question mid-run Follow-up turns whether a conversation continues after the first answer Caller identity what the channel reports about the caller What each shipped channel offers:\nChannel Streaming Elicitation Follow-up turns Caller identity Queued jobs no no no unverified sender field a2a prompts yes optional yes unverified sender field No caller waits for a queued job, so that channel does not stream output and does not take a second turn.\nThe prompts channel sends output to the caller as the worker produces it. It returns a conversation token with every prompt it accepts. Send that token on a later request to continue the conversation.\nTo let a run ask the caller a question, set expose.agent.a2a.prompts.elicit. Leave it unset and the agent asks nobody.\nConfirmation-gated tools An agent with nobody to ask still offers every tool to the model, including the confirmation-gated ones. The model can call one. The confirm gate then refuses the call and tells the model why.\nWhere to go next Serving covers the fisk serve command and the settings every channel shares Queued jobs covers the asyncjobs channel: submitting work, reading answers, and its own configuration Answering prompts covers taking prompts from other agents and streaming the run back to them Serving tools covers offering this agent’s tools to other agents without running an agent loop",
    "description": "host an agent behind one or more calling endpoints",
    "tags": [],
    "title": "Channels",
    "uri": "/fisk-ai/channels/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "Fisk AI exports OpenTelemetry traces and metrics over OTLP/HTTP, following the GenAI semantic conventions. One run is one trace: how long it took, which model calls it made, which tools it ran, and where the tokens went. It applies to fisk run, to the runs fisk serve hosts, and to knowledge searches served by fisk mcp. The a2a endpoint, which serves tools to other agents without running the loop, exports a span per served call and joins the caller’s trace.\nNote Traces and metrics go only to the collector configured below. The Fisk project receives nothing, and export is off by default. Prompts and tool results are not exported unless content capture is turned on.\nA single session with two prompts:\nTurning it on Add a telemetry block and point it at a collector:\ntelemetry: enabled: true endpoint: http://127.0.0.1:4318 This build speaks OTLP/HTTP, port 4318. Pointing it at 4317 is the OTLP/gRPC port and is rejected at startup.\nEvery setting is in the reference. Transport credentials are never in the file: the standard OTEL_EXPORTER_OTLP_HEADERS and friends configure the connection, so the same configuration sends to a collector, Grafana Tempo, Honeycomb or any OTLP/HTTP endpoint.\n--no-telemetry suppresses export: for one run on fisk run, and for the whole process on fisk serve, which reports whether telemetry is on in its startup banner.\nA local collector Save as otelcol.yaml and run otelcol-contrib --config otelcol.yaml:\nreceivers: otlp: protocols: http: endpoint: 127.0.0.1:4318 exporters: debug: verbosity: detailed service: pipelines: traces: receivers: [otlp] exporters: [debug] metrics: receivers: [otlp] exporters: [debug] Then run the agent. The run’s summary line ends with the trace id, and the full-screen UI shows it on the end card:\nRun summary: model=claude-sonnet-5 llm_calls=2 tool_calls=1 tokens=1832/241 thinking=0 latency=4.1s trace=4bf92f3577b34da6a3ce929d0e0e4736 With --verbose the run also reports what reached the collector. An export that did not arrive is always reported.\nWhat a trace looks like A one-shot run:\ninvoke_agent \u003cidentity\u003e the whole run ├── startup \u003cidentity\u003e loading tools, opening stores, importing remote tools │ └── memory_index the start-of-run memory listing ├── chat \u003cmodel\u003e one model call, one event per HTTP attempt ├── execute_tool \u003cname\u003e one tool call │ ├── retrieval a knowledge_search │ │ └── embeddings \u003cmodel\u003e one request to the embeddings server │ └── invoke_agent \u003cremote\u003e a tool served by another agent └── chat \u003cmodel\u003e retrieval covers knowledge_search. knowledge_enumerate gets its own span of that name, since it never ranks and never uses vectors.\nA full-screen run wraps the same work in a workflow, one agent invocation per turn:\ninvoke_workflow \u003cidentity\u003e ├── startup \u003cidentity\u003e ├── invoke_agent \u003cidentity\u003e turn 1 │ ├── chat \u003cmodel\u003e │ └── execute_tool \u003cname\u003e └── invoke_agent \u003cidentity\u003e turn 2 └── chat \u003cmodel\u003e A resumed session is a new trace, not a continuation. A trace spans two processes only across an a2a call, where the request carries the caller’s trace context. Group by gen_ai.conversation.id to see a session’s whole history. A resumed run’s first chat span continues the iteration numbering, so fisk.llm.iteration starting at 17 is expected.\nAttributes Standard gen_ai.* attributes carry the model, the token usage, the tool names and the stop reasons. Fisk-specific ones use a fisk. prefix.\nAttribute Where Meaning fisk.run.terminal_reason root, turn completed, max_iterations, error, budget, suspended, setup_failed fisk.run.resumed, .crashed, .interactive root how the run started and ended fisk.run.tool_calls, .remote_tool_calls root this run’s tool calls fisk.llm.uncached_input_tokens root, turn, chat input tokens billed at the uncached rate fisk.llm.thinking, .prompt_cache, .tool_search root the feature switches this run used fisk.llm.iteration, .messages, .tools chat loop index and the sizes sent fisk.session.end_id root the session the run ended on, when /clear rotated it fisk.session.usage.*, .llm_calls root session totals including the resumed prefix, resumed runs only fisk.turn.index turn one-based turn number fisk.tool.kind execute_tool which provider supplied the tool fisk.tool.outcome execute_tool executed, error, unknown_tool, capacity, policy_denied, missing_arguments, confirm_denied, confirm_unanswered, deferred fisk.tool.arg_keys execute_tool the argument key names, never their values fisk.tool.requested_name execute_tool the name the model asked for, unknown tools only fisk.tool.confirm_wait_ms execute_tool how long the call waited on the operator fisk.tool.remote, .remote_agent, .rewritten, .resumed execute_tool how the call was dispatched fisk.tool.exit_code execute_tool the exit status of the command the tool ran, absent when it ran none fisk.tools.application, .builtin, .remote, .custom, .deferred startup the resolved tool inventory fisk.remote_hosts startup configured remote tool hosts fisk.memory.backend, .location startup, memory_index, execute_tool the memory store this run used fisk.memory.entries memory_index memories the start-of-run listing returned, absent when it failed fisk.knowledge.tier.configured retrieval hybrid or lexical, as configured fisk.knowledge.tier.effective retrieval the tier that ran, absent when neither retriever did fisk.knowledge.top_k retrieval the effective result ceiling, after defaulting and clamping fisk.knowledge.search.status retrieval ok, index_not_built, index_empty fisk.knowledge.sections retrieval sections returned fisk.knowledge.indexed_chunks retrieval corpus size, absent when there is no index fisk.knowledge.degraded, .degraded_reason retrieval the fallback to lexical and why fisk.knowledge.enumerate.status knowledge_enumerate ok, index_not_built, corpus_empty, query_empty fisk.knowledge.matched, .documents, .truncated knowledge_enumerate the matched set, what was returned, and whether they differ fisk.knowledge.limit, .min_body_matches knowledge_enumerate the options that shaped those counts fisk.knowledge.indexed_documents knowledge_enumerate corpus size, absent when there is no index fisk.embeddings.inputs embeddings texts in this request fisk.embeddings.purpose embeddings query or dimension_probe Group tool calls by fisk.tool.outcome: a policy denial, an unknown tool and a failed command all return an error to the model, and only this tells them apart.\nA run with content capture on carries the gen_ai.* content attributes and fisk.content.* alongside these.\nOn execute_tool, fisk.memory.backend and .location are present on memory tool calls only, so filtering on them selects the calls that reached the store. They describe the tool that ran, which a PreToolUse hook can change, and they are span attributes rather than metric labels: on the tool duration histogram the backend would be empty for every tool that is not a memory tool. They stay on startup as well, which is where a run that binds a store but never calls a memory tool reports it.\nModel call attempts One chat span can be several HTTP requests: the Anthropic SDK retries a rate limit or a transport failure inside the call. The span covers the whole call, and each attempt is recorded as an event on it.\nEvent When fisk.llm.http_response an attempt got a response, whatever the status fisk.llm.http_error an attempt got no response at all Attribute Where Meaning fisk.llm.http_attempt both events one-based attempt number within this model call fisk.llm.http_duration_ms both events how long the attempt took http.response.status_code fisk.llm.http_response the status the attempt received error.type fisk.llm.http_error the failure class, provider unless the run was canceled or timed out http.request.resend_count the chat span retries after the first attempt, absent when there were none A call that took ten seconds with http.request.resend_count of 3 spent most of it waiting on retries, not on the model. The status code is on the events rather than the span because a span attribute would be last-attempt-wins and report 200 for a call that spent most of its time being rate limited.\nNothing about the request or the response body is recorded: not the URL, which can carry credentials in its userinfo, not the headers, and not the response body or the error text. An attempt is a status code, a duration and an ordinal.\nRemote agents A tool served by another agent gets an invoke_agent \u003cremote\u003e span inside the execute_tool span that dispatched it. It covers the a2a call.\nAttribute Meaning gen_ai.operation.name invoke_agent fisk.tool.remote_agent the agent the call was sent to gen_ai.tool.name the tool named on the wire error.type how the call ended, absent on success error.type separates the failures that look alike from the model’s side:\nValue Meaning remote_unavailable no agent answered, or the deadline passed first remote_capacity the agent answered and refused: it is at its concurrency limit and ran nothing tool_error the call was answered and the tool failed on the far side canceled, timeout this run stopped, not the peer other anything else The request carries this span’s trace context, so a peer running Fisk AI puts its own span for the call in this trace and a slow remote call shows where the time went. A peer that exports nothing, or one that is not Fisk AI, still shows only as a slow span here.\nThe two sides can still disagree about when the call ended. A served call reports that it is running every ten seconds, so a caller gives up only when those stop or when harness.tool_timeout ends the whole call, and a span closed as remote_unavailable under a server span that is still open means the caller stopped hearing from a peer that kept working.\nKnowledge fisk.knowledge.tier.configured and .tier.effective differing means the vector tier was configured and did not run. fisk.knowledge.degraded_reason says why, from a fixed set: embeddings and timeout are the embeddings server, index_meta is the index’s own metadata failing to read, canceled is the run stopping. Only index_meta is a problem with the store rather than the server, and it is the one case that opens no embeddings child span.\nfisk.knowledge.sections counts sections and fisk.knowledge.documents counts documents; several sections routinely come from one file, so the two are not comparable.\nThe embeddings span carries server.address, server.port and, when a response arrived, http.response.status_code. The dimension probe is lazy and cached per process, so the first search of a run makes two embeddings requests; fisk.embeddings.purpose tells them apart. A server that cannot be reached never lets the probe cache, so every later search makes a probe request and no query request.\nretrieval and embeddings do not add up to the execute_tool span above them: the tool renders its tier banner and trims results to the injection budget after the store has returned.\nIndexing is not instrumented. fisk knowledge index and fisk knowledge watch start no telemetry, so the knowledge spans are constructed and discarded there.\nfisk mcp does export them. A knowledge_search that arrived over MCP opens the same retrieval and embeddings spans an agent run does, and each is its own trace with retrieval at the root: there is no run above it. Everything else fisk mcp serves is uninstrumented, so a config that exposes no knowledge tools exports nothing.\nMetrics Metric Attributes gen_ai.client.token.usage operation, provider, model, gen_ai.token.type gen_ai.client.operation.duration operation, provider, model, error.type gen_ai.invoke_agent.duration agent name, terminal reason, interactive gen_ai.invoke_agent.inference_calls as above gen_ai.invoke_agent.tool_calls as above gen_ai.execute_tool.duration tool name, kind, outcome, error.type fisk.knowledge.degraded_searches fisk.knowledge.degraded_reason fisk.session.append.duration fisk.session.backend, error.type gen_ai.invoke_agent.* is recorded per turn, treating a one-shot run as one turn.\nfisk.session.append.duration times each write to the run journal, and is recorded only for a checkpointed run. It is a metric rather than a span because a run appends once per record, so a span each would outnumber every other span in the trace. The file backend writes locally and sits in the lowest buckets; the jetstream backend makes a network round trip per append, and this metric shows that difference. A failed append is recorded with its error.type, so the time spent before a failure is visible rather than missing.\nfisk.knowledge.degraded_searches counts searches that fell back to lexical. It is a metric rather than a span attribute alone because spans are sampled: with sample_ratio below 1.0 most degraded searches never reach the backend, and an embeddings outage silently costs every search its vector tier. There is no knowledge duration metric; gen_ai.execute_tool.duration filtered on gen_ai.tool.name covers it.\ngen_ai.token.type carries only input and output, so the histogram can be summed without grouping. Set no_metrics: true to export traces alone.\nWorking out cost gen_ai.usage.input_tokens includes cached tokens, and cache reads bill at roughly a tenth of the uncached rate. So:\ncost = (input_tokens - cache_read - cache_creation) x uncached rate + cache_read x cache read rate + cache_creation x cache write rate + output_tokens x output rate fisk.llm.uncached_input_tokens is that first term already worked out, and is the same number the run summary prints.\ngen_ai.usage.reasoning.output_tokens is the share of output_tokens the model spent reasoning. It is already included in output_tokens, so it does not enter the calculation above; it is there because reasoning is not displayed by default, which makes a dashboard the only place its cost is visible.\nOn a resumed run, gen_ai.usage.* on the root covers that process alone, so summing it across a session’s traces gives the session total once. fisk.session.usage.* carries the cumulative view for comparison.\nPrivacy By default spans carry structure and timing: no prompt, tool argument value or tool result is exported, and error messages are reduced to a fixed error.type class rather than their text.\nContent capture changes that. With it on, the system prompt, the conversation, the model’s replies, tool arguments and tool results are exported as span attributes. It is off unless the config turns it on.\nThe OpenTelemetry credential variables are stripped from tool subprocess environments whether or not telemetry is enabled, so --no-telemetry does not re-expose a collector token. See the reference Safety section for the full list and its limits.\nContent capture Off by default. With it on, a trace carries the conversation itself:\ntelemetry: enabled: true endpoint: https://otel.example.net:4318 capture: enabled: true messages: delta # delta | full max_bytes: 8192 # per attribute Everything the model saw is exported Whoever can read the traces can read the conversation, and an export cannot be recalled. Tool results are the verbatim output of whatever command the model ran, and the system prompt includes the memory index. Content capture bypasses the error.type reduction and every other limit described above. Use it for a short investigation against a collector you control, not as a fleet default.\nharness.pii is the one thing that reaches this: it scans the prompt and each tool result before the conversation is built, so what it removed was never exported. It does not scan the system prompt or the memory index in it, and its detection is best-effort, so treat what arrives as unredacted.\nA run with capture on shows OTEL Enabled + content on the full-screen startup card and marks its summary line:\nRun summary: model=claude-sonnet-5 llm_calls=2 tool_calls=1 tokens=1832/241 thinking=0 latency=4.1s trace=4bf92f35 content=exported fisk info shows what would be captured, including the derived export batch size. Plain http:// to a non-loopback host is rejected at startup while capture is on.\nThere is no command-line flag: capture is a config setting. --no-telemetry suppresses it along with the rest of the export.\nThe content attributes Attribute Where gen_ai.system_instructions startup, once per run gen_ai.input.messages chat gen_ai.output.messages chat gen_ai.tool.call.arguments execute_tool gen_ai.tool.call.result execute_tool fisk.content.from_index chat, where this call’s messages start fisk.content.truncated the attributes on this span that were cut fisk.content.dropped_messages messages dropped to fit Each is a JSON document in the shape the GenAI conventions define.\nNever exported: a thinking block’s provider signature, and the payload of a provider-specific block such as a server-side tool search result. Reasoning text is exported.\nA denied tool call still exports its arguments. The system prompt is on startup rather than on each model call because it does not change during a run.\nWhy gen_ai.input.messages holds only one message messages: delta, the default, exports only what each model call added to the conversation, so no single span holds the whole thing. fisk.content.from_index says where a span’s messages start; add it to the number of messages exported and you get fisk.llm.messages on the same span. Consecutive model calls chain, so a gap means a span did not arrive.\nSet capture.messages: full to put the whole conversation on every model call. That is quadratic in the length of the conversation: a thirty-iteration run exports thirty copies of a growing transcript.\nSome content reaches no message attribute at all. A run that ends at the iteration cap, on the token budget, or on a hook abort leaves its last tool results with no model call after them; those survive as gen_ai.tool.call.result on the tool spans. Do not sum message attributes across the traces of one session either: a resumed run’s first model call carries the whole restored conversation.\nSizing Each attribute is capped at capture.max_bytes (256 to 65536, default 8192), measured on the encoded JSON. Over the cap, whole messages are dropped oldest-first and then text is shortened, so the document always parses; fisk.content.truncated and fisk.content.dropped_messages say what happened.\nOTEL_SPAN_ATTRIBUTE_VALUE_LENGTH_LIMIT lowers the cap to match rather than being overridden, so the SDK never cuts a document mid-structure.\nCapture raises the size of every export, so the batch size is reduced and gzip is turned on. A collector may still need its receive limit raised:\nreceivers: otlp: protocols: http: endpoint: 127.0.0.1:4318 # Raise when content capture is on and exports are refused. max_request_body_size: 8388608 With sample_ratio below 1.0, content is exported only for sampled traces.\nWhen it does not look right What you see What it means a value ends in [truncated by fisk-ai: ...] it hit capture.max_bytes; the marker names the original size a value is cut off and the span has no fisk.content.truncated your collector or backend cut it, not Fisk AI gen_ai.input.messages holds one message the delta; see above no traces, and an export warning after the run the batch was refused, usually on size with capture on spans missing from a trace, and no warning at all the span queue overflowed; the delivery line cannot see this The other run outputs Output Scope Contents Use when --trace FILE one run, local exact request and response bodies, including retries debugging Fisk AI or the provider --http-debug one run, local raw bodies to a fixed file, a subset of --trace prefer --trace run summary one run, local counters and latency the receipt for the run that just finished telemetry many runs, many processes structure and timing; the conversation only with capture on which tool fails, where time goes, across a fleet",
    "description": "Export traces and metrics with OpenTelemetry",
    "tags": [],
    "title": "Telemetry",
    "uri": "/fisk-ai/telemetry/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "A Fisk AI agent is one YAML file. The same file drives run, mcp, and a2a, and each command validates only the parts it needs. The design goal throughout is that an operator mistake fails at startup with a message naming the fix, never at the first tool call.\nWhere it lives config is a single file, config.go. It is pure data with no IO beyond reading the file, and it imports no internal package. That is deliberate: config is the lowest layer, so main and every internal package can import it.\nOne file, three modes agent.yaml literal, no interpolation parse, prepare, validate unknown keys are fatal ModeAgent run: model and prompt ModeMCP mcp, info: tools only ModeServer a2a: app and nats The mode selects which required fields apply. Structural checks run in all three. ModeMCP is the most permissive: it serves tools and needs neither a prompt nor a model. That is exactly why fisk info parses in it. Requiring a model would reject a valid MCP-only config that info exists to inspect.\nLoading is strict on purpose Parsing is one call with three consequential options:\nyaml.UnmarshalWithOptions(data, cfg, yaml.DisallowUnknownField(), yaml.UseJSONUnmarshaler()) DisallowUnknownField makes every unknown key a hard parse error, including a harness setting mistakenly left at the top level. That case has its own test.\nUseJSONUnmarshaler makes the parser populate the json.RawMessage fields, harness.memory.options and harness.sessions.options, with canonical JSON. A backend can then decode its own sub-block against a typed schema regardless of the source format. It is also why an unknown key inside an options block fails just as loudly, only at store construction time rather than at parse time.\nEvery field carries both json and yaml tags even though only YAML is read today, and the parse path is routed through canonical JSON deliberately, so a JSON config or a schema-driven editor is a small step rather than a rewrite.\nDefaulting and normalization prepare() runs before validation, in a fixed order.\nIdentity fallback Explicit value, else the basename of application_path, else fisk-ai. Normalize confirm tags Trim, drop empties, de-duplicate, preserve first-seen order. Trimming is a safety fix: a trailing space would silently fail to match a real command tag. Normalize global flags Trim, strip leading dashes so --context and context both work, drop empties, de-duplicate. Prepare the MCP block Validate confirm_over_mcp, validate the builtins allowlist, parse the tool timeout. Prepare the a2a block The same limits pass, with its own path in every error message. Prepare the budget Reject negatives, then default max tokens to 200000, iterations to 50, and the call timeout to 120s. Prepare embeddings Default the timeout to 30s, else parse it and require a positive value. Duration strings always come in pairs: a string field from YAML and a parsed time.Duration twin tagged json:\"-\" and yaml:\"-\". Parsing happens once, in prepare.\nDefaults that do not live in the config package This matters when reading config.go and finding no default for something the documentation promises.\ntop_kDefaults to 5 with a ceiling of 20, in internal/rag/store.go. max_injected_tokensDefaults to 6000, also in the rag store. knowledge.directoryDefaults to knowledge/\u003cidentity\u003e, resolved by the rag store. memory directoryDefaults to memory/\u003cidentity\u003e, resolved by the file backend. max_output_tokensDefaults to 8192, raised to 16384 when thinking is on, in internal/agent. MCP port and addressDefault to 8080 and 127.0.0.1, resolved in mcp_command.go. Validation Structural checks run in every mode. Mode-specific required fields come after.\nRule Reason global_flags with no application_path is an error Global flags belong to the wrapped application and have nothing to attach to A non-empty identity must match ^[a-zA-Z0-9_-]+$ It doubles as a NATS queue group and appears in subjects, so whitespace, ., *, or \u003e would form an invalid or wildcard-bearing subject A remote tool host needs a name matching the same pattern The name keys the NATS subjects A remote host alias must match the pattern too It prefixes imported tool names A remote host exclude.tags filter is rejected outright Discovery carries no tags, so the exclude could never be honored and would silently leave an unwanted tool imported The identity pattern is checked in every mode whenever the value is non-empty, not only where the field is required, because the value may have been derived from a binary basename carrying a dot or a space.\nAn include-by-tag on a remote host is treated more gently than an exclude-by-tag: the import path warns and ignores it rather than refusing, since over-inclusion is visible and under-exclusion is not.\nMode-specific requirements:\nMode Requires ModeAgent llm.model always; identity and system_prompt unless also exposed over MCP; nats_context when remote_tools is set ModeMCP nothing beyond the structural checks ModeServer application_path and nats_context A2A requires an application because no built-in declares a2a exposure, so an application-less a2a server would start with an empty tool set. The server itself can carry any tool kind; the requirement expires when a built-in first opts in.\nSeveral rejections happen during prepare rather than validation, so they fire in all modes:\nconfirm_over_mcp must be auto, always, or never after trimming and lowercasing. A typo must not silently select a weaker gate than intended. expose.agent.mcp.builtins may contain only knowledge_search and knowledge_enumerate, and the error names the accepted set and why the others are excluded. This is the selection, not the capability: the tool’s own declaration is the ceiling and this can only narrow it, and naming one knowledge tool never selects the other. A non-empty allowlist with knowledge disabled is also rejected, since there would be nothing to serve. max_concurrent_tools rejects a negative value and anything above 1024. Zero is treated as unset rather than rejected, because an omitted YAML key unmarshals to zero and the server applies its own default. tool_timeout must parse and be non-negative, and the error names which block it came from since MCP and a2a share the helper. Overrides and precedence There is no environment interpolation inside the YAML. The file is literal. Overrides are layered by the CLI.\nSetting Precedence MCP port and address flag, then config, then built-in default TUI Config no_tui is an absolute veto. The flag can only turn the TUI off, never on --state-dir Folded into the config object after parsing, so it wins over a configured directory --state-dir is the one flag written back into the config. Against a non-file session backend it is a hard error rather than a silently ignored flag.\nFlag-or-environment bindings such as ANTHROPIC_API_KEY, FISK_AI_MCP_PORT, and FISK_AI_STORE_DIR never enter the config object at all. They travel alongside it into the agent’s options or are resolved in the command.\nWhere the config does name an environment variable, it names it rather than holding a value. api_key_env is a variable name, and CredentialEnvNames() returns those names so a tool subprocess’s environment can be scrubbed of them.\nWhat identity silently controls identity is more load-bearing than its one-line description suggests. It is the discovery name, the NATS queue group, so multiple agents sharing it share work, and the namespace for on-disk state: memory/\u003cidentity\u003e and knowledge/\u003cidentity\u003e.\nChanging it orphans an existing memory store and knowledge index. Nothing warns about that, because nothing can tell an intentional rename from a typo.\nFrom config to a running agent fisk run does the following before agent.Run sees anything:\nValidate flag combinations --resume with --checkpoint, --resume with a query, --name without --checkpoint, and --force without --resume are all refused before any work happens. Resolve whether the run is checkpointed This changes the interrupt contract, so it is decided first. Parse in ModeAgent Then fold in --state-dir. Open the HTTP debug file The CLI owns it: mode 0600, removed then reopened with O_EXCL to defeat a symlink planted at the fixed name. Peek the session's chat flag when resuming So --chat need not be re-passed. Pick a UI Chat with --no-tui is refused loudly rather than silently degraded. The naming split in the knowledge feature is intentional and worth stating rather than fixing. The YAML key and the user-facing noun are knowledge; the Go type is RAGConfig, the field is Harness.RAG, and the package is internal/rag. Knowledge is the feature, RAG is the technique. The CLI mirrors it: the command is knowledge with rag and k as aliases.\nThe command tree main.go is 78 lines and loads no configuration. Every command parses the file itself, in the mode it needs.\nCommand Mode Purpose run ModeAgent Runs the agent. Owns the largest flag set session ls, show, rm ModeMCP, or none Inspects checkpointed journals. --config is optional info ModeMCP Explains a config without contacting a model knowledge and its thirteen subcommands reads harness.knowledge Builds and inspects the local index mcp ModeMCP Serves tools over MCP a2a ModeServer Serves tools to other agents over NATS discover reads nats_context only Prints a remote agent’s card interruptContext is the shared one-shot contract: SIGINT plus SIGTERM, so a server stops cleanly under systemd or a container stop, and a second signal falls through to the default disposition. run deliberately does not use it, because it layers a graceful-suspend contract on top.\nmain.go blank-imports the file session backend so the session subcommands, which construct a store directly, are self-sufficient. The run path picks it up transitively through the agent package.\nReserved and unused remote_agents is entirely unused. The field and its type are referenced nowhere outside config.go and its test. Nothing reads them and nothing validates them, unlike remote_tools, which is checked in every mode. remote_tools is the working feature. A2ATransport() is a stub returning the constant \"nats\". A transport config field is deferred until a second transport exists, but the value is still routed through the transport registry, so adding one is a config field plus a blank import. ThinkingConfig is a one-field struct on purpose, so an effort knob can land without a breaking config change. Config.Harness carries omitempty on a non-pointer struct, which is a no-op for struct values. It is cosmetic. MCP and a2a each get their own concurrency and timeout knobs rather than sharing one, because the two bound different trust boundaries: anything that can reach a TCP port, possibly on a non-loopback address, versus NATS peers. Both are config-only, with no flag or environment override. tool_timeout is named to avoid colliding with llm.budget.call_timeout, which bounds a different unit of work. llm.budget.max_tokens is a soft, deliberately over-counting cap. It sums uncached input, cache reads, cache writes, and output, so with prompt caching on it over-states dollar cost by design. A cost-weighted budget is named as separate future work. It is also soft in timing, since the total is checked after each call. Next The agent loop picks up where parsing ends, and turns the validated config into behavior.",
    "description": "One YAML file, three modes, and the strictness that makes a typo fail at startup",
    "tags": [],
    "title": "Configuration",
    "uri": "/fisk-ai/design/codemap/configuration/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "A Fisk AI agent is described by a single YAML configuration file. It has the path to the application, selects which of its commands become tools, and sets the model, the prompt, and how the harness behaves. The run, mcp, and serve commands all read the same file; each uses the parts it needs and ignores the rest.\nThe --config flag selects the file, defaulting to agent.yaml in the working directory:\n$ fisk run --config nats.yaml \"report on the ORDERS stream\" Each section below is a slice of that file: read it top to bottom and you have seen every setting Fisk AI understands. Fields that are required are called out as such; everything else has a working default and can be left out.\nNote Most agents need only a handful of these settings. The Agents guide walks through building one from scratch; this page is the exhaustive list to reach for when you want to know exactly what a field does.\nA minimal file The smallest useful agent names the application, sets a model, and gives a prompt:\n# agent.yaml - drive the NATS CLI as an agent application_path: /usr/local/bin/nats llm: model: claude-sonnet-4-6 budget: max_tokens: 500000 max_iterations: 50 call_timeout: 120s system_prompt: | You manage NATS JetStream Streams using tools. Everything after this point expands on those blocks and adds the optional ones.\nA knowledge-only agent application_path is optional. Leave it out to run an agent with no wrapped application, on the built-in tools alone. This is useful for a knowledge agent that answers from an indexed corpus over knowledge:\n# agent.yaml - answer questions from a local knowledge base, no wrapped app llm: model: claude-sonnet-4-6 system_prompt: | You answer questions using the knowledge_search tool over the indexed docs. harness: knowledge: enabled: true With no application_path, the identity defaults to fisk; set an explicit identity to keep the knowledge/\u003cidentity\u003e and memory/\u003cidentity\u003e stores separate when you run more than one such agent in a directory.\nIdentity and application # The name of the agent. Used in discovery and reused as a NATS queue # group, so it must contain only letters, digits, \"-\" or \"_\". If you # leave it out it defaults to the application binary's base name, or to # \"fisk-ai\" when no application_path is set; set it explicitly when the # derived name carries a dot, a space, or other characters, which are # rejected, or to keep memory/knowledge stores separate between agents. identity: nats # Path to the Fisk application binary to introspect and run. OPTIONAL. # When set, the binary is introspected once at startup to obtain its # command tree and per-command JSON schemas. Leave it out to run an agent # on the built-in tools (knowledge, memory, human_in_the_loop) and any # remote tools alone, with no wrapped application. Required only when # expose.agent.a2a.serve_tools exposes the wrapped application's tools. application_path: /usr/local/bin/nats # The system prompt describing what the agent should do. REQUIRED for a # \"run\" and for a channel that runs one, ignored by \"mcp\" mode and by the # a2a endpoint. Think of it as a one-file SKILL: describe the goals and # give broad guidance. system_prompt: | You manage NATS JetStream Streams using tools. identity is load-bearing beyond a label: it is the NATS subject key when the agent serves or is discovered over agent-to-agent, and the default memory directory is memory/\u003cidentity\u003e. Keep it to the safe character set so those uses stay valid.\napplication_path is optional for run and mcp modes and required only when expose.agent.a2a.serve_tools is set, because no built-in is offered over a2a today and such an endpoint would have nothing to serve. When set, the target must be built with a current Fisk (v0.9.0 or newer) that supports --fisk-introspect and precomputed per-command schemas. When it is left out, Fisk AI skips introspection entirely and the agent runs on its built-in and remote tools alone; see a knowledge-only agent below.\nTool selection include and exclude choose which of the application’s commands become tools. Each takes a list of regular expressions matched against the tool name, and a list of fisk tags:\n# Keep only the commands whose tool name or tag matches. When \"include\" is # present, a command must match it to be exposed. include: # Regular expressions matched against the tool name: the command path # joined with underscores, so the \"stream info\" command is \"stream_info\". tools: - ^stream_ - ^consumer_info$ # Match commands by fisk tag. An empty string \"\" matches untagged # commands. The reserved ai:deny tag is always active and can never be # included back in. tags: - scope:read # Remove matching commands. Applied as a filter: a command that matches # \"exclude\" is dropped even if \"include\" allowed it. exclude: tools: - ^stream_rm$ tags: - scope:system A tool’s name is its command path joined with underscores, so a nested command like stream info becomes stream_info. Grouping commands and hidden commands are skipped and never become tools. include and exclude can be used together: for example include ^stream_ but exclude ^stream_rm$. Commands tagged ai:deny are dropped before any of this runs and can never be added back.\nRun fisk info to preview the resulting tool set before a run.\nModel and budget The llm block selects the model and limits what the loop may do. llm.model is the only required field in it:\nllm: # The model identifier. REQUIRED. Accepts any value the Anthropic API # accepts; the well-known identifiers are listed under \"Models\" below. model: claude-sonnet-4-6 # The model backend. Defaults to \"anthropic\" when unset, so most agents # never set it. Set it only to target a different backend that has been # built in; naming one that is not available fails at run start with the # list of providers that are. provider: anthropic # Limits on the agent loop so it cannot run without end. The two caps # have different scopes: max_iterations is per turn, max_tokens is # cumulative over a conversation. budget: # Tokens a whole conversation may process, counted across every turn # of it. Default 500000. A conversation that reaches it takes no # further turn; start a new conversation or raise this. It counts # tokens rather than money: cache reads weigh the same as uncached # input here and are priced at a fraction of it. max_tokens: 500000 # Cap on the tokens a single response may generate, distinct from the # cumulative max_tokens. Left unset it uses a built-in default that is # raised when thinking is on. Set it only to fit an endpoint whose # per-response limit is lower than that default; an explicit value wins. max_output_tokens: 0 # Agent loop iterations one turn may take, a fresh allowance per # turn. Default 50. max_iterations: 50 # Per-call timeout as a Go duration string, for example \"60s\" or # \"2m\". Default \"120s\". call_timeout: 120s # Controls whether the model exposes its reasoning, which some providers # call reasoning rather than thinking. The whole block is optional and # leaving it out is the default: nothing is sent and the model uses its own # behavior. Including it states a preference either way, so omitting it and # setting enabled false are different requests. # # Older models that predate adaptive thinking (Sonnet 4.5, Haiku 4.5) reject # the parameter, and so may a proxy behind ANTHROPIC_BASE_URL. Both explicit # states send one, so remove the block for those rather than setting false. thinking: # true asks the model to think and surfaces its reasoning separately from # the answer (thought-bubble lines on stderr in shell mode, folding blocks # in the TUI). false asks it not to think, which changes only a model that # would otherwise reason unaided. enabled: true # How hard the model works, which governs how deeply it reasons and how many # tokens it spends overall. Unset asks for nothing and the model uses its own # default. It sits beside the thinking block rather than inside it, so an # effort can be set without sending a thinking parameter. # # The value is passed to the provider as written and is not checked against a # list of levels, because the levels belong to the model: Anthropic takes low, # medium, high, xhigh and max, other providers name their own, and a model # released after this build may take one Fisk AI has never heard of. A level # the model does not take is refused at the first model call, naming it. reasoning_effort: high # When true, disables Anthropic prompt caching for the run. Left off, # Fisk AI caches the stable prefix of each request to lower cost and # latency on multi-turn runs. no_prompt_cache: false # When true, disables server-side tool search: every tool is sent to the # model directly instead of being deferred behind a search tool at ten or # more tools. Left off, tool search is used automatically when the provider # supports it and the tool count crosses the threshold. Set true only for an # endpoint that does not implement it. no_tool_search: false Larger models reason better on complex, long-horizon tasks; smaller models like Haiku are faster and cheaper for narrow ones. When the agent exposes ten or more tools it relies on the model’s server-side tool search, which recent models support and older ones do not; see Models.\nHarness The harness block governs how the agent harness behaves during a run, as distinct from the model (llm) or the tool selection. Everything in it is optional and the whole block can be omitted to leave every setting at its default. These settings apply to the agent loop only; mcp mode and the a2a endpoint ignore them.\nharness: # Opt-in built-in tools that let the model ask the operator a question # at the terminal (agent mode only). human_in_the_loop: # When true, offers the model the ask_human_confirm, ask_human_select, # and ask_human_input tools. Off by default. enabled: true # Command tags that, in addition to the always-on ai:confirm tag, gate a # command behind operator approval before it runs. Matching is exact, not # a regex, and additive to ai:confirm. An entry that matches no loaded # command is reported as a warning at startup. confirm_tags: - ai:destructive - impact:rw # Limits a single tool call, at a terminal and on a worker alike. # Unset uses the default of 5m; set 0s for no limit at all, which is # what a command that legitimately runs for hours needs. # # The timeout cancels the call. A command is killed along with its # process group; an in-process tool stops only if it checks. A call # waiting on your answer runs as long as it needs. Separate from # expose.agent.mcp.tool_timeout and expose.agent.a2a.tool_timeout, # which limit a served call. tool_timeout: 5m # A hard off switch for the full-screen terminal UI: the agent always # uses line-by-line output, even on an interactive terminal, and the # command line cannot turn the UI back on. Use the --no-tui flag for a # one-off run instead. Negative switch, no effect in the line UI. no_tui: false # The full-screen UI rings the terminal bell each time a run blocks # waiting on you (an approval gate or an ask_human_* prompt). On by # default; set true to silence it. Negative switch, no effect in the # line UI. no_bell: false # Opt-in built-in key/value store that survives across runs (agent mode # only). memory: # When true, offers the model the memory_list, memory_read, # memory_write, and memory_delete tools. Off by default. enabled: true # The store implementation. Defaults to \"file\", which keeps each # memory in a markdown file under a directory; it is the only backend # today. backend: file # By default the stored keys and descriptions are injected into the # system prompt at run start so the model knows what it has saved. Set # true to keep the store's contents out of the prompt. Negative switch. no_index: false # Serve memory_list and memory_read only, withholding memory_write and # memory_delete, so a run uses what earlier runs saved without changing # it. The store is untouched: anything else writing to it still does. read_only: false # Backend-specific settings. For the \"file\" backend: \"directory\", the # path memory files live under, defaulting to \"memory/\u003cidentity\u003e\". A # relative directory resolves under the store base when a deployment # sets one and against the working directory otherwise; an absolute # directory is used as-is. options: directory: memory # Scans prompts and tool results for personal data before the model, the # session store or telemetry sees them. Alone in this block it acts # without being asked for: leave it out and it redacts. pii: # redact replaces each value found with a placeholder naming its type # and the run continues. reject refuses the text instead: a prompt is # denied and a tool result is withheld from the model. off scans # nothing. Defaults to redact. mode: redact # Where run journals are stored, which is what --resume continues. # Optional; absent it uses the \"file\" backend under the XDG state # directory. Sessions cannot be disabled: every run is a conversation. sessions: # The store implementation. \"file\" (the default) keeps each session as a # JSON-lines journal under a directory. \"jetstream\" keeps them on a NATS # JetStream stream shared over a broker, using the nats_context above. backend: file # Backend-specific settings. For \"file\": \"directory\", the path journals # live under, defaulting to the XDG state directory; --state-dir # overrides it. For \"jetstream\": \"stream\", the name of an # operator-created stream to bind, for which --state-dir does not apply. options: directory: /var/lib/fisk-ai/runs human_in_the_loop lets the model decide when to ask; the ai:confirm tag and confirm_tags gate a command the model wanted to run anyway. The two are compared in detail under Command tags and in the Agents guide.\nPoint two agents at the same memory directory and they share a memory; leave the default and each keeps its own. Treat what a memory contains as data the model saved, not as trusted instructions.\nCommand tags Fisk commands can carry tags, set in their fisk definition or, for App Builder applications, in YAML. Any tag can be matched by include/exclude. The ai: prefix is reserved for the tags Fisk AI interprets; a tag under that prefix that is not one of the tags below does nothing and is reported as a warning at startup, by fisk info, by the MCP server and by the a2a endpoint.\nControl tags These change what Fisk AI does with a command.\nTag Meaning ai:deny Never expose the command; dropped before include/exclude and can never be added back. The reliable off switch. ai:no_defer Always send the command directly instead of deferring it behind the tool-search tool. ai:confirm Require the operator to approve the command at the terminal before it runs; always active, no config flag. ai:confirm denies by default: no interactive terminal, or a prompt that cannot be shown, declines rather than runs. An interrupt or an end-of-input at the prompt ends the run instead of declining, since the operator did not answer; on a the conversation survives and fisk run --resume puts the question again. An “allow for the conversation” answer is remembered by command regardless of its arguments: the conversation records it and honors it on every resume. /clear and a --force resume across a changed configuration drop it, and a resume with no terminal attached declines a gated command rather than honoring it. harness.confirm_tags extends the same gate to any other tag your application already uses. Over MCP these gates are requested through elicitation instead of a local operator prompt; over agent-to-agent, confirmation-gated commands are not served at all. The full behavior is documented under Command Tags in the Agents guide.\nBehavior tags These describe what the command does. They enforce nothing: they are advice, carried to the model, to MCP clients as tool annotations, and to peer agents. Use ai:deny and ai:confirm for control.\nTag Meaning ai:read_only The command does not modify anything. ai:destructive The command may destroy or overwrite existing state. ai:additive The command changes state but only adds to it. ai:idempotent Running the command again with the same arguments has no further effect. Most commands need one tag: ai:read_only for a read, ai:destructive for a delete. Leave a command untagged and clients fall back to the MCP defaults, which assume the worst and treat it as destructive.\nEach tag sets only what it names. ai:read_only does not imply ai:idempotent, and MCP clients ignore the destructive and idempotent hints for a read-only tool. A command tagged both ai:read_only and ai:destructive is used as destructive and the contradiction is reported as a warning.\nBecause these are ordinary tags, harness.confirm_tags: [ai:destructive] gates every destructive command behind approval, and include: {tags: [ai:read_only]} serves a read-only tool set. Both select on what the command author remembered to tag, so they are a convenience rather than a boundary; ai:deny and name-based include/exclude remain the reliable controls.\nAll of a command’s tags, reserved and free-form alike, are appended to the tool description Fisk AI sends the model as a trailing Tags: ... line, so a prompt can reference them. Adding or changing a tag changes that description, which changes the tool-set fingerprint a conversation is keyed on: one stopped before the change refuses to continue after it.\nServing over MCP To serve the same tools over the Model Context Protocol instead of running the agent loop, add an expose.agent.mcp block. It is opt-in: without this block, fisk mcp refuses to start. MCP mode uses only the fields that describe the application and the tool set; system_prompt, llm.model, and the harness settings are ignored.\nexpose: agent: # The opt-in block that enables MCP serving. Must be present, even if # empty ({}), for \"fisk mcp\" to start. mcp: # Default listen port, used when neither --port nor the # FISK_AI_MCP_PORT environment variable is set. Default 8080. port: 8080 # Host or IP to bind to, used when neither --address nor the # FISK_AI_MCP_ADDRESS environment variable is set. Defaults to the # loopback address 127.0.0.1, so the server serves only local clients # unless you set this; use 0.0.0.0 to listen on all interfaces. address: 127.0.0.1 # Free-text guidance sent to clients when they connect. A client may # pass it to the model as a hint about how to use the server, a good # place for orientation the terse per-tool descriptions cannot carry. instructions: | These tools wrap the NATS CLI. Prefer stream_info before stream_edit, and treat all subjects as relative to the FOO account. # How confirmation-gated commands (ai:confirm or a confirm_tags tag) # behave when the connected client cannot be asked through # elicitation: # auto - default; ask clients that can elicit, run ungated for # clients that cannot # always - ask clients that can elicit, refuse for clients that # cannot be asked # never - never ask, run gated commands ungated, delegating # approval to the client's own UI confirm_over_mcp: auto # Maximum tool calls run at once. 0 or unset uses the default 2, a # negative value is rejected, and the ceiling is 1024. It is separate # from the a2a knob because the MCP port can be network-reachable # (address 0.0.0.0), a wider trust boundary than a2a's NATS peers. max_concurrent_tools: 2 # How long a single served tool call may run, e.g. 60s. Unset uses # the default 30s. Named tool_timeout, not call_timeout, to avoid # colliding with llm.budget.call_timeout, which limits a different # unit of work. Config-only; there is no flag or environment override. tool_timeout: 30s # Optional: narrow the served set further, within the top-level # include/exclude selection. With neither, every selected command is # served (subject to the tag rules). Same regex-over-tool-name and tag # matching as the top-level filters. tools: include: tools: - ^stream_ exclude: tools: - ^stream_rm$ The served tools are the agent’s top-level include/exclude selection, narrowed further by expose.agent.tools when set. identity, if set, becomes the MCP server name. Elicitation is a request the client fulfills, not an enforcement boundary; for a command that must never be reachable over MCP, use ai:deny rather than confirmation. The MCP Servers guide covers this mode end to end.\nAgent-to-agent Fisk AI agents can also serve tools to, and import tools from, one another over NATS with no LLM on the serving side. Both sides use a named NATS context, given as nats_context. Serving is an endpoint of fisk serve; the Serving tools guide covers it end to end.\nNote A2A capabilities are under development, this is included here for completeness but subject to radical change\n# Name of a NATS context (as managed by \"nats context\" and resolved by # jsm.go) used to connect to NATS. REQUIRED when remote_tools is set or # when serving tools to other agents. nats_context: ngs expose: agent: # What this agent answers for other agents over NATS, and how long it # waits on the calls it makes to them. Opt-in: without the block nothing # answers, and a block must set serve_tools or prompts unless it holds # request_timeout and nothing else. Both endpoints use one connection under one # identity. Its knobs are separate from the mcp block's because the two # servers sit on different trust boundaries (NATS peers vs anything # reaching a TCP port). a2a: # When true, \"fisk serve\" answers tool calls from peers, serving one # tool per call and running no agent loop. It needs only # application_path, identity, nats_context and the tool selection: no # prompt and no model. Confirmation-gated commands are never served, # since there is no operator to approve them. serve_tools: true # Maximum tool calls run at once. A call arriving with every slot in # use is refused at once with a \"capacity\" code and no command is # started for it. 0 or unset uses the machine's CPU count clamped to # between 2 and 8 (a container's own limit, not the host's), a # negative value is rejected, and the ceiling is 1024. max_concurrent_tools: 4 # How long a single served tool call may run, e.g. 60s. Unset uses # the default 30s. Config-only, no flag or environment override. tool_timeout: 30s # How long this agent waits for a peer to say anything, e.g. 30s, # where tool_timeout limits a call it answers. A served call is # answered with an acknowledgement, a message every ten seconds # while the tool runs, and then the reply, so this applies to the # gap between messages while harness.tool_timeout limits the call. A # card fetch is one message, so for discovery the # two are the same number. Unset uses the default 120s; 0s and a # negative are rejected, and a value under 30s is raised to it. An # agent that only calls other agents sets this with nothing else in # the block, which is valid. request_timeout: 120s # Answers prompts from peers by running the agent loop over each one # and streaming the run back. Its presence enables the endpoint and an # empty block works. Answering a prompt runs the whole loop, so # identity, system_prompt and llm.model are all required, and the run # reaches every tool include and exclude selected, not the served set. prompts: # How many prompts this process answers at once, and the number # above which a caller is refused rather than made to wait. # Default 1. The --workers flag does not reach it: that sizes the # queue. workers: 2 # Lets a run put its questions to the caller that sent the prompt: # an approval for a confirmation-gated command, or a # human-in-the-loop question. Default false: the worker refuses # every gated command. Anyone permitted to answer this identity's # questions can approve a gated command, and an answer carries no # verified caller identity. The worker holds a question for # request_timeout, and its worker slot with it. A caller answers # waiting before the window runs out to restart it, for as long as # somebody is reading the question. elicit: true # Import tools from one or more remote fisk agents over NATS and expose # them to this agent alongside its local tools. remote_tools: - # The remote agent's identity (also the NATS subject key). REQUIRED. name: nats # A prefix for the imported tool names. Applied only when a bare name # would clash with a local tool or another remote's tool. Defaults to # \"name\". alias: nats # Select which of the remote agent's tools to import, matched against # the tool name only. A \"tags\" filter cannot be honored, since # discovery does not carry tags, and an exclude-by-tag is rejected at # startup. include: tools: - ^stream_ exclude: tools: - ^stream_rm$ Imported tools keep their own name where it is unambiguous, and take the \u003calias\u003e_\u003cname\u003e form only when the bare name would collide. A run is strict: an unreachable or unimportable remote agent fails the run. fisk info is lenient and reports each remote host’s reachability instead.\nThe timeouts around a2a each cover one thing. expose.agent.a2a.tool_timeout limits a call this agent answers for a peer. expose.agent.a2a.request_timeout sets how long it waits for a peer to say anything before treating it as gone. harness.tool_timeout limits any tool call the loop makes, remote ones included, so it is how long a remote call may take in total. llm.budget.call_timeout limits a model call and reaches nothing on the network.\nQueued jobs expose.agent.jobs opts the agent in to taking whole units of work off a Choria asyncjobs work queue. Its presence is the switch for fisk serve: without the block, the command refuses to start. Every field under it defaults, so an empty block is a working worker.\nexpose: agent: jobs: # The work queue to consume. It must already exist: the worker binds # to it and creates nothing, so its run time, retry cap and # concurrency stay with whoever owns the queue. Default \"FISK_AI\". queue: FISK_AI # The asyncjobs task type this worker handles. A task of another # type on the same queue is left alone, so a task submitted under a # type no worker handles stays queued until it expires, with no # error logged at either end. Default \"fisk-ai:run\". task_type: fisk-ai:run # How many jobs this process runs at once, default 1. The --workers # flag overrides it. It cannot raise throughput past the queue's own # concurrency, which limits every worker on that queue together. workers: 1 # The NATS context the queue is reached over, defaulting to the # top-level nats_context. It is dialed separately from the shared # connection, so the queue may live on a different cluster from the # session store and remote tools. nats_context: production # Caps a task payload in bytes before anything decodes it, default # 524288. It is the only limit on a third party's input to an endpoint # whose sole access control is permission to write to the queue. max_payload: 524288 A job runs the whole agent loop, so it uses the agent’s own include and exclude rather than expose.agent.tools, which selects only what is served over MCP and a2a. It needs identity, system_prompt and llm.model like any other run, where a worker serving only tools needs none of them. The Queued jobs guide covers submitting work and reading answers.\nTelemetry telemetry exports OpenTelemetry traces and metrics over OTLP/HTTP. It applies to fisk run, to the runs fisk serve hosts, and to knowledge searches served by fisk mcp. The a2a endpoint exports nothing. Nothing is exported unless enabled is true.\ntelemetry: enabled: true endpoint: http://127.0.0.1:4318 service_name: \"\" sample_ratio: 1.0 no_metrics: false capture: enabled: false messages: delta max_bytes: 8192 Setting Description enabled Turns export on. Default false. endpoint OTLP/HTTP base URL; /v1/traces and /v1/metrics are appended. Unset, the standard OTEL_EXPORTER_OTLP_* variables apply, defaulting to http://localhost:4318. service_name Service name reported to the backend. Unset, it falls back to OTEL_SERVICE_NAME, then identity, then fisk-ai. sample_ratio Head sampling ratio from 0.0 to 1.0. Default 1.0. An explicit 0 samples nothing. no_metrics Exports traces only. Metrics are on with telemetry. capture Exports the conversation itself, not only structure and timing. Off by default; see below. Content capture Setting Description capture.enabled Exports the system prompt, the conversation, model replies, tool arguments and tool results. Default false. capture.messages delta (default) exports what each model call added; full exports the whole conversation on every call. capture.max_bytes Cap per content attribute, measured on the encoded JSON. Default 8192, from 256 to 65536. Everything the model saw and everything the tools returned reaches the collector, including the verbatim output of commands the model ran, and an export cannot be recalled. There is no command-line flag; only --no-telemetry, which suppresses the whole export.\nWhat harness.pii removes is gone before this sees it, since the scan runs as the text enters the conversation. That is the only redaction on this path: everything harness.pii does not scan or does not detect is exported verbatim.\nPlain http:// to a non-loopback host is rejected at startup while capture is on. The settings under capture are ignored and unvalidated while capture.enabled is false. See the telemetry guide for the attributes, sizing and collector limits.\nTransport credentials are never written in the file. OTEL_EXPORTER_OTLP_HEADERS and the other standard OTEL_* variables configure the connection, so the same configuration sends to a collector, Grafana Tempo, Honeycomb, or any OTLP/HTTP endpoint without a change here. This build speaks OTLP/HTTP only: port 4318, not 4317.\nWhether a run exports is decided in this order:\nCondition Result --no-telemetry, NO_TELEMETRY, or OTEL_SDK_DISABLED=true Off telemetry.enabled: true On Otherwise Off Setting OTEL_EXPORTER_OTLP_* does not enable export on its own; a host-wide collector endpoint does not turn every agent on the machine into an exporter. A run that finds those variables set while telemetry is off prints a note saying so.\nAn invalid configuration fails at startup rather than exporting nowhere: an endpoint that is not an http or https URL, an endpoint on port 4317, OTEL_EXPORTER_OTLP_PROTOCOL=grpc, a sample_ratio outside 0.0 to 1.0, or plain http to a non-loopback host while an OTEL_EXPORTER_OTLP_*_HEADERS variable is set, which would send the credential in the clear.\nfisk info shows the resolved settings and where each came from. After a run, an export that did not reach the collector is reported; --verbose also reports a successful one.\nA run exports one trace covering the whole run, with spans for setup, each turn, each model call, each tool call, each knowledge search, each request to the embeddings server and each tool served by a remote agent, plus the GenAI metric instruments. A model call carries one event per HTTP attempt, so a retried call reports what it spent waiting. Indexing is not instrumented. The Telemetry guide has a local collector to try it against, the span tree to expect, and the attribute reference.\nModels Well-known Anthropic model identifiers are available as constants in the config package; any value the Anthropic API accepts may be used in llm.model, local LLMs will have their own convention. fisk does not restrict what you enter here.\nConstant Identifier Notes ModelClaudeFable5 claude-fable-5 Most capable overall, for demanding reasoning and long-horizon agentic work; highest cost. ModelClaudeOpus48 claude-opus-4-8 Most capable Opus tier; slowest and most expensive Opus. ModelClaudeOpus47 claude-opus-4-7 Prior Opus release. ModelClaudeOpus46 claude-opus-4-6 Earlier Opus release. ModelClaudeOpus45 claude-opus-4-5-20251101 Earlier Opus release. ModelClaudeSonnet5 claude-sonnet-5 Balanced capability, speed, and cost; good default. ModelClaudeSonnet46 claude-sonnet-4-6 Prior Sonnet release. ModelClaudeSonnet45 claude-sonnet-4-5-20250929 Earlier Sonnet release. ModelClaudeHaiku45 claude-haiku-4-5-20251001 Fastest and cheapest; best for simpler tasks. Every model in the table supports the server-side tool-search tool that deferred tool discovery relies on. Anthropic’s tool search is generally available on Claude Opus 4.5, Sonnet 4.5, Haiku 4.5, and later; Claude Opus 4.1 and earlier, and local models, do not support it. If you point llm.model at an older identifier or a local model while exposing ten or more tools, the model is left holding only the tool-search tool with no way to reach the deferred commands and the run stalls. With such a model, keep the exposed set below ten tools (around 15 for local runners) so every tool is sent directly.\nCommand-line flags and environment Some behavior is set per run on the command line rather than in the file. The flags override the file where they overlap, except for the hard off switches (harness.no_tui), which the command line cannot re-enable.\nFlag Environment variable Description --config Path to the configuration file. Default agent.yaml. --api-key ANTHROPIC_API_KEY Anthropic API key. Required. --base-url ANTHROPIC_BASE_URL Anthropic API base URL to use, for example a local Anthropic-compatible runner. A non-loopback host must use https; plain http is allowed only for a loopback address. --http-debug HTTP_DEBUG Dump Anthropic API request and response bodies to http-debug.log. The file holds the full conversation and is created mode 0600. --no-color NO_COLOR Disable markdown rendering of the final answer, emitting raw text. --no-tui NO_TUI Disable the full-screen terminal UI and answer one prompt with line-by-line output. The full-screen view holds a conversation of many turns; this answers one. --verbose VERBOSE Show more verbose output. --thinking THINKING Show the model’s reasoning, which is hidden by default. On fisk session show --transcript it includes reasoning in the transcript. The thinking=N token counter is reported either way. --trace Write a JSON-lines trace of every LLM request and response to a file. --resume Continue a stored conversation, by the session id fisk session ls shows or by a conversation token. --force Continue a stored conversation whose configuration has changed since it started. Standing approvals are dropped. --state-dir Override where the sessions of the agent this process hosts are stored, default $XDG_STATE_HOME/fisk-ai/runs. Refused with --nats-context, where the agent is elsewhere and keeps its own. --nats-context On fisk run, talk to an agent on this NATS context instead of running one in this process. The configuration’s identity names the agent and must be set. --a2a-debug Dump every a2a message between this terminal and the agent to a2a-debug.log. The file holds the conversation token, your prompts and all tool output; it is created mode 0600. --no-telemetry NO_TELEMETRY Suppress OpenTelemetry export, whatever telemetry.enabled says. On fisk run it covers the run, on fisk serve the whole worker. The credential scrub still applies. --workers On fisk serve, how many jobs to run at once, overriding expose.agent.jobs.workers. --work-dir On fisk serve, the directory command tools run in. Must be an absolute path that exists. Defaults to the worker’s own working directory. The MCP server port also reads FISK_AI_MCP_PORT, which --port overrides and which in turn overrides expose.agent.mcp.port. Sessions, chat, and their durability semantics are covered in the Agents guide.\n--workers overriding the file is the opposite of how harness.tool_timeout works, where a configured value beats the built-in default. The worker count is a property of the process; the tool timeout is a property of the agent.\nSafety The configuration is the boundary on what the model can reach: application_path fixes the one binary it can drive (and with no application_path set the agent can drive no external binary at all), include/exclude and ai:deny fix which of its commands become tools, and nothing outside that set is callable. Commands run as an argument vector rather than through a shell, each argument is checked against the command’s schema, the ANTHROPIC_API_KEY is stripped from their environment, output is capped at 64 KiB, and LLMFORMAT=1 is set. The Agents and MCP guides describe the full threat model for each mode.\nThe OpenTelemetry export credentials are stripped from tool environments too: OTEL_EXPORTER_OTLP_HEADERS and its per-signal forms, and the mTLS variables OTEL_EXPORTER_OTLP_CLIENT_KEY, OTEL_EXPORTER_OTLP_CERTIFICATE and OTEL_EXPORTER_OTLP_CLIENT_CERTIFICATE with their per-signal forms. This happens whether or not telemetry is enabled, so --no-telemetry does not re-expose a collector token. The mTLS variables name a file path rather than holding a secret, so removing them from the environment hides the location of the key, not the key: a tool running as the same user can still read that file if it knows where to look.\nSpans carry structure and timing, tool names and argument key names, and no prompts, tool arguments or results. Setting telemetry.capture.enabled reverses that for every one of them, and for the error.type reduction as well, since a tool’s error text is part of its result.\nPersonal data harness.pii scans the prompt and each tool result as it enters the conversation, before the model, the session store or a telemetry collector sees it, and either replaces what it finds or refuses the text. It redacts unless configured otherwise. fisk info reports the mode in effect, and a run says so the first time it acts.\nDetection is pattern matching and is best-effort in both directions: it misses real values (a valid US social security number went undetected in testing) and it flags text that is no such thing. It lowers what leaks; it does not gate it, and no decision to send data somewhere should rest on it.\nCredentials are scanned alongside personal data: API keys with a recognizable prefix (sk-…, sk-or-v1-…, sk-ant-…, xoxb-…, ghp_…), bearer tokens, and NATS credentials and nkey seeds. These are matched by their own shape, so a key pasted into a prompt or printed by a tool is found wherever it appears, not only where it is assigned to a name. A credential with no distinctive shape is not found: an opaque value is indistinguishable from a git SHA or a base64 blob, and a rule wide enough to catch it redacts those too.\nFour limits are worth knowing before relying on it:\nIt does not see the system prompt, the memory index that prompt carries, the model’s own replies, the arguments the model writes for a tool call, a result a caller supplies for a deferred call, or the history a resume restores. A session journaled before the feature was turned on keeps what it recorded. Redaction is one-way. A placeholder the model reads out of a file goes back into that file through the next tool call, since there is no restore path. Asking an agent to edit a file whose contents are redacted will corrupt it. The scan is the last point the text passes, not the first. A prompt sent to an agent over a2a is already on the broker, in the task record and in any wire log before the agent that answers scans it. What the operator sees is what the model was given. A redacted tool result reads as redacted in your own terminal, for data on your own machine, because the same trace reaches a caller who is not at it.",
    "description": "Configuration Reference",
    "tags": [],
    "title": "Reference",
    "uri": "/fisk-ai/reference/"
  },
  {
    "breadcrumb": "Introduction \u003e Channels",
    "content": "The a2a endpoint exposes the wrapped application’s commands to other agents over NATS as callable tools. A peer reads the agent’s card and invokes one of them. No prompt is sent and the agent loop does not run, so the model, budget and session settings do not apply.\nNote The endpoint is opt-in. The configuration must set expose.agent.a2a.serve_tools: true, otherwise fisk serve exposes no tools over a2a.\nServing tools is a endpoint of fisk serve since Version0.0.5. The fisk a2a command is gone, and a configuration carrying the old expose.agent.agent_to_agent key is refused at startup.\nServing a set of tools identity: nats-tools application_path: /usr/local/bin/nats nats_context: production include: tools: - ^stream_ expose: agent: a2a: serve_tools: true A worker serving only tools needs no system_prompt and no llm.model. It does need application_path: built-in tools are never served, so an agent with no wrapped application serves nothing.\n$ fisk serve --config tools.yaml Serving nats-tools/1.2.0: Endpoints: a2a Agent Context: production Telemetry: disabled Serving tools over a2a: Discovery: choria.fisk-ai.discovery.nats-tools Tools: choria.fisk-ai.tool.nats-tools Concurrency: 4 Tool Timeout: 30s Exposed: stream_add stream_ls stream_info Reading the card fisk discover fetches the card the worker answers discovery with:\n$ fisk discover nats-tools --config peer.yaml Agent Card for nats-tools: Agent: nats-tools Version: 1.2.0 Protocols: io.choria.fisk-ai.v1 Importing those tools into another agent is remote_tools in that agent’s configuration, covered in the Reference.\nTool selection expose.agent.tools applies on top of the agent’s include and exclude. One file can run every stream_ tool in a job and serve two of them to peers.\nA tool carrying ai:confirm or a configured confirm tag is left off the card, because no operator is behind a served call to approve it. Use ai:deny to keep a command out entirely.\nBuilt-in tools are never served. Knowledge, memory and the human-in-the-loop tools declare no a2a exposure, and the startup banner lists them as withheld when the configuration enables them.\nLimits expose: agent: a2a: serve_tools: true max_concurrent_tools: 4 tool_timeout: 30s request_timeout: 120s Setting Description max_concurrent_tools (int) tool calls run at once; default is the CPU count clamped to 2 to 8 tool_timeout (duration) limit on one call this agent answers; default 30s request_timeout (duration) wait for a peer’s next message; default 2m, minimum 30s In a container the concurrency default reads the container’s CPU limit, not the host’s, and the banner prints the concurrency in use. --workers sizes the queued-jobs intake and does not reach these. harness.tool_timeout limits a tool call inside the agent loop.\nrequest_timeout limits a call this agent makes to a peer. The peer answers with a set of messages: an acknowledgement, a keepalive every ten seconds while the tool runs, then the reply. The timeout applies to the gap between those messages, so this agent waits for as long as the keepalives arrive, and harness.tool_timeout ends the call. A card fetch is a single message, so the same value covers a whole card fetch.\nThe same value is how long the prompts endpoint holds a question it put to a caller, see Answering questions. Raising it for a slow peer raises that window too.\nAn agent that only calls other agents still needs request_timeout, so a block holding nothing else is valid:\nexpose: agent: a2a: request_timeout: 30s Any other expose.agent.a2a block must set serve_tools: true or a prompts block, or fisk serve exits with an error.\nThe server refuses a call that arrives with every slot in use rather than queueing it. The ack carries accepted: false, and the tool.reply carries is_error: true with code: capacity. The identity is a NATS queue group, so a retry is delivered to whichever member is free next.\nRunning with other endpoints A single fisk serve process runs a channel and this endpoint on one NATS connection:\nServing nats-worker/1.2.0: Endpoints: asyncjobs/FISK_AI a2a Model: claude-sonnet-5 Queue Context: production Sessions: file Knowledge: disabled Telemetry: disabled Tool Directory: /var/lib/fisk-ai Tool Timeout: 5m0s Queue Workers: 1 (config) Serving tools over a2a: Discovery: choria.fisk-ai.discovery.nats-worker Tools: choria.fisk-ai.tool.nats-worker Concurrency: 4 Tool Timeout: 30s Exposed: stream_ls stream_info Adding a prompts block to the same a2a block also answers prompts, covered in Answering prompts.\nShutdown and faults A drain stops the endpoint answering and removes the identity from its queue group, so the worker takes no further tool calls. Prompts stop with it, both endpoints using one transport and one identity.\nA drain does not wait for a call that is already running. The call runs to completion with nowhere to reply to, and a command it started may outlive the worker. tool_timeout stops a call that does not finish.\nAn error on any of the service’s subscriptions stops the whole micro service, taking discovery, tools and prompts for that identity down together. fisk serve logs it, drains the runs in flight and exits non-zero, so a supervisor restarts the worker. A drain stops the service by the same path, and is logged rather than reported as a fault.\nSafety Whoever can publish to choria.fisk-ai.tool.\u003cidentity\u003e can run every tool on the card. NATS permissions are the whole of the access control: a served call carries no verified caller, so nothing can distinguish one peer from another.\nThe tool safety rules described in the Reference hold here as everywhere else: commands run as an argument vector rather than through a shell, each argument is checked against the command’s schema, and credentials are stripped from tool environments.",
    "description": "serve this agent's tools to other agents over NATS",
    "tags": [],
    "title": "Serving tools",
    "uri": "/fisk-ai/channels/a2a/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "The rest of the documentation describes what Fisk AI does. This section describes how it is built and why the design turned out the way it did.\nThe material here targets contributors, reviewers, and anyone auditing the harness before trusting it with a production tool. It names real packages, files, and symbols, and it states the invariants the safety story depends on.\nRoadmap: Long term development goals Code Map: A guided deep-dive into the codebase: architecture, subsystems, and the decisions behind them",
    "description": "How Fisk AI is built and why",
    "tags": [],
    "title": "Design",
    "uri": "/fisk-ai/design/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "The agent loop is where a configuration becomes behavior. It calls the model, executes the tools the model asks for, feeds the results back, and stops. Nearly all of the interesting design work is in what it refuses to do: run a truncated tool call, spend past a budget, suspend mid-tool, or let one tool source shadow another.\nWhere it lives internal/agent owns the run. Key files: agent.go for setup, runner.go for the loop, hooks.go for the extension points, events.go for the display sink, slog_events.go for a structured sink. The package doc states the boundary explicitly: it owns no CLI concerns, so flags, signals, and terminal rendering stay with the caller.\nTwo halves agent.go is setup. It resolves every tool source, dials at most one NATS connection, builds the provider, computes the resume fingerprint, opens or creates a session, and constructs a private runner.\nrunner.go is the loop. The runner struct is deliberately split into two groups of fields, and that split is the reason resume works at all:\ninfrastructureRebuilt from configuration on every start or resume: provider, system prompt, tool definitions, budgets, confirm gate, prompter, events sink. run stateWhat a snapshot has to carry: messages, journal sequence, iteration counters, pending tool calls, session id, and the deferred-reset flags. Everything the model sees is rebuilt from the config file. Only the conversation itself is restored. That is what makes a resumed run verifiable rather than merely restarted.\nStartup order is a safety property Register the panic barrier first Deferred functions run last-in-first-out, so registering the barrier before the journal, store, and tracer closers means it also catches a panic in cleanup. Validate caller-supplied directories ToolWorkDir and StoreDir must be absolute and must exist, and the error names the option. Load application tools fisk.LoadTools introspects the binary, strips ai:deny, then applies include and exclude. Claim names into one set Every later source must claim into taken. A clash aborts the run. Add the built-ins Human-in-the-loop tools first, then memory, then knowledge. All agent-mode only. Dial NATS at most once Decided per subsystem, after resolving whether the run is checkpointed at all. Import remote tools, strictly An unreachable named agent aborts the run. Register custom tools last So a collision message can name the clashing kind. Require at least one callable tool With a different message depending on whether application_path is set. Build the confirm gate and warn about dead tags Every configured confirm tag matching no loaded tool is reported. Load-bearing decision Tool names form one flat namespace across application, built-in, remote, and custom tools, because the model addresses every tool by a single flat name. A collision aborts the run rather than silently shadowing. The rationale in internal/agent/agent.go:644 is security, not tidiness: shadowing a confirm-gated command would strip its gate. Any change that makes a collision non-fatal reopens that hole.\nTwo warnings exist purely because silence would be misleading. WarnConfirmNoTerminal fires when gated tools exist but nothing can prompt. WarnConfirmTagUnmatched fires for each configured tag that matches no loaded tool, because, in the source’s own words, left unreported it gives a false sense of safety.\nThe NATS dial is a single connection shared by JetStream memory, JetStream sessions, and remote tools. The session gate also checks whether the run is checkpointed at all, so a run that never writes a session does not fail on NATS it would never use.\nOne iteration suspend poll the only such point provider.Call under call_timeout journal turn before any tool runs tool_use? terminal turn completed, or refusal no budget check stop before tool spend yes execute tool batch journal each result while iter \u003c max_iterations The assistant turn is journaled before any tool runs, and the budget is checked after the terminal branch but before tool execution. Three placements in that flow carry most of the weight.\nThe suspend poll is at the top, and nowhere else. It runs at a boundary where the conversation is coherent and nothing is in flight, and it runs before the iteration index is consumed, so a suspend does not burn an iteration number. A run that is not checkpointed has no suspend function wired at all, so it cannot suspend.\nThe assistant turn is journaled before any tool executes. A crash partway through a batch therefore resumes without re-paying for the model call that produced it.\nThe budget is checked after the terminal branch and before tool execution. A terminal answer is delivered regardless of remaining budget, since no further spend follows it. An over-budget turn that wanted tools stops without incurring their side effects.\nWhy a truncated reply is an error A reply that stopped at the output token cap may carry a partial tool_use block whose input is incomplete. Executing it would mean running malformed arguments. The loop treats StopMaxTokens as ReasonError with an explicit message rather than running the call or silently reporting completion.\nThe terminal flag is computed with a StopMaxTokens term that is a no-op on every path past the truncation branch. It exists only so that PostModelCall, which observes every reply including a truncated one, sees the correct value.\nTerminal reasons Reason Cause Continues a chat? ReasonCompleted A terminal turn with no tool calls yes ReasonMaxIterations The per-run iteration cap was reached yes ReasonError Provider failure, hook error, refusal, or truncation yes ReasonBudget The token budget was exhausted no ReasonSuspended A graceful suspend was requested no A transient model failure must not stall an interactive session, which is why ReasonError is continuable. An aborted context is filtered out separately by the loop condition rather than by the reason.\nTool dispatch Dispatch is an ordered pipeline, and the order is the safety argument.\nregistry lookup PreToolUse hook resolve rewrite count by kind validate arguments confirm gate ExecuteUse PostToolUse hook WarnUnknownTool deny result WarnMissingRequired ConfirmDeniedResult counted once, after rewrite is resolved gated on the union of original and effective Four exits return a result to the model without running the tool. None of them is silent. The details that matter:\nThe original call is described before any mutation. PreToolUse sees exactly what the model asked for, and a rewrite can still be gated against the original. A denied call is counted under the original kind, once. The accounting invariant is that every call counted in ToolCalls is also counted in ToolCallsByKind, including rejected ones, so on a fresh run the buckets partition the total. A rewrite that names an unregistered tool, or supplies invalid JSON, aborts the run rather than dispatching a malformed call. RewriteInput replaces the whole argument object, so {} clears every argument. The hook does not re-fire on the rewritten call. Argument validation runs before the gate, so an operator is never asked to approve a structurally invalid call. The outcome is a warning, not a call-and-result pair. The gate fires on origGated || effGated. This is the point of the whole ordering. Load-bearing decision The confirm gate is evaluated on the union of the original and the effective tool, so a hook cannot strip a gate by redirecting a gated call to an ungated tool. The operator is shown the effective command, because that is what actually runs, but the trigger tag falls back to the original tool’s when the effective tool is not Confirmable. See Tools and introspection for the gate itself.\nA denial returns util.ConfirmDeniedResult, which is deliberately not an error result. The model treats it as authoritative rather than as something to retry.\nTools describe themselves rather than having the runner switch on concrete types. A tool that does not implement Describer degrades safely: unknown kind, name-only trace, no dependencies, not remote. Dependencies follow least privilege, so a kind receives only what it asked for through NeedsPrompter and NeedsWorkDir.\nThe panic barrier The barrier is registered first so it unwinds last, which means it also covers a panic during cleanup. Its scope is documented honestly: it catches only the run goroutine, and it cannot catch a fatal runtime error such as a concurrent map write, an out-of-memory kill, or runtime.Goexit. It is not a substitute for process isolation.\nThere are three nested recovers, each for a different reason.\nThe barrier itself. An inner recover around Events.Panicked, so a misbehaving sink cannot crash the process the barrier just protected. fireSessionEnd has its own recover, because by the time it runs the barrier’s recover has already completed. The stack trace never crosses a trust boundary. PanicError carries a fixed generic message and exposes only the recovered value; the stack goes to Events.Panicked alone, since it leaks absolute paths and frame arguments. The stack is captured before any caller code runs, while the panicking frames are still live, because SessionEnd may itself panic.\nHooks Eight optional callbacks, all on the single run goroutine, in loop order. A nil hook is a no-op. There is one callback per point; composing several behaviors means wrapping them in one function.\nHook Fires Can change On failure SessionStart Once, before any session is created or opened nothing aborts the run, leaving no orphan session UserPromptSubmit Initial prompt, and each interactive follow-up Deny with a reason initial deny ends the run; a follow-up deny reopens the input PreModelCall Before each provider.Call nothing, counts only ReasonError PostModelCall After every reply, including a truncated one nothing, gets a deep copy ReasonError, but not durable across resume PreToolUse Ahead of validation, gate, trace, and execution deny, or rewrite the tool or its input aborts the run PostToolUse After execution, before trace and journal replace the output and error flag aborts the run TurnEnd At each interactive continuation boundary nothing ReasonError SessionEnd Inside the panic barrier, after everything is closed nothing downgraded to a warning The contract bounds hook power deliberately: a hook may observe, terminate, or adjust tool data. It never injects prompts, continues or extends a turn, or changes token or tool accounting, budgets, or iteration caps.\nPayloads are snapshot-isolated so a mutating hook cannot corrupt the live conversation. Tool input arrives as a bytes.Clone, the model reply as a JSON round-trip deep copy built only when a hook is actually set, and the per-kind tool counts as a maps.Clone.\nLifecycle facts worth knowing:\nSessionStart fires before the initial UserPromptSubmit. A resume fires SessionStart with Resumed: true and does not re-fire UserPromptSubmit for reconstructed history. A context reset does not re-fire SessionStart. That rotation is reported through Events.SessionRotated. SessionEnd fires from exactly one place, exactly once, for every run that reached the runner, whether it completed, ran out of budget, suspended, errored, or crashed. A setup failure before the runner exists never fires it. On a crash, Err is nil and Reason is unset, so a hook must key off Crashed. PreToolUse is the only reliable way to block a tool durably, because PostModelCall runs after the turn is already journaled. PreToolUse and PreModelCall sit above the provider, so they fire even for an injected provider, unlike an llm.Middleware. PostToolUseResult.Replace is an explicit boolean rather than an empty-value sentinel, because an empty output is a valid replacement.\nInteractive continuation A nil NextPrompt makes the run one-shot. Otherwise the outer loop gathers a Continuation at each boundary.\nContinuation has three fields. Continue false ends the session. Reset clears the conversation but keeps the system prompt, the tools, and the confirm gate’s approvals. Reset with empty text reopens the input without running a turn.\nThe follow-up UserPromptSubmit hook fires before any reset, rotation, append, or journal write, so a denial reopens the input without clearing context or journaling a rejected turn. The stale terminal reason is reset so the previous advisory does not fire twice.\nA reset on a checkpointed run is deferred rather than immediate. Clearing the conversation would leave an empty prompt in the session metadata, which would fail to resume, so the reset waits for the next real prompt and then rotates to a new session. If rotation fails the reset is abandoned and the turn continues in the current session, which stays consistent because the messages were never cleared.\nEach accepted follow-up adds the configured iteration budget to the cap, so one long turn cannot starve the next.\nA journal write failure at a follow-up boundary ends the session before the turn runs, so the journal stops at the last coherent boundary rather than recording a turn whose result was never written.\nWhat the loop does not promise Being explicit about this is part of the design.\nToolWorkDir is collision avoidance, not confinement. It sets a per-run working directory; it sandboxes nothing. CustomTools run in process with the agent’s own privileges and an unscrubbed environment. An injected Provider bypasses the registry, so its credentials are not in llm.CredentialEnvNames and are therefore not scrubbed from tool subprocesses. Middlewares, including HTTP debug and tracing, are also not applied to it. RunStats.ToolCallsByKind is live-only. It stays nil-seeded across a resume, so the buckets partition the total only on a fresh run. A confirm-denied or missing-argument result is journaled with remote=false regardless of the tool’s actual kind. The token budget is soft in two ways. It over-counts by design, summing uncached input, cache reads, cache writes, and output, and it is checked after each call, so a single call can overshoot. Reserved and unused llm.Caps.MaxOutputTokens is declared but read nowhere. resolveMaxOutputTokens uses two package constants and the config override only, so a provider’s declared ceiling does not currently clamp the per-call cap. SlogEvents has no in-tree consumer outside its own test. It exists for a server or job runner that does not yet live in this repository, and its doc comment prescribes NewSlogEvents(base.With(\"run_id\", id), verbose) as the attribution pattern. The shared-Events contract for concurrent runs is explicitly deferred to whenever a job system arrives. Most of Options is embedder-only. The CLI sets fewer than half its fields; Provider, ToolWorkDir, StoreDir, Conns, RAGStore, MemoryStore, SessionStore, A2ATransport, CustomTools, and Hooks are exercised only by tests today. resumeHazards returns a slice but has one hazard kind, and Warning.Params is used by one warning. Only StopMaxTokens, StopPauseTurn, and StopRefusal are branched on. A stop_sequence reply with no tool call therefore reads as a completed answer. Next Tools and introspection covers where the tools the loop dispatches come from, and what the tag system does to them.",
    "description": "Call the model, run the tools it asks for, journal everything, and stop for the right reason",
    "tags": [],
    "title": "The agent loop",
    "uri": "/fisk-ai/design/codemap/agent-loop/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "Fisk AI writes no glue code for the application it drives. It runs the binary once with --fisk-introspect, reads the command tree, and turns leaf commands into tools. Everything that makes this safe rather than merely convenient happens in the filtering, the gating, and the execution.\nWhere it lives internal/toolkit holds the neutral contracts and imports no tool kind. Key files: tool.go, kind.go, describer.go, confirm.go, prompter.go. internal/toolkit/fisk is the application-command kind, internal/toolkit/functool the generic function kind, and internal/toolkit/builtin the harness-owned tools.\nOne interface, narrow capabilities toolkit.Tool is four methods: Name, Description, InputSchema, Definition(deferLoading bool), and ExecuteUse. The runner never type-switches on a tool. Policy is opted into through three narrow interfaces:\nDescriberSupplies the trace line, the provider kind, and which dependencies the tool needs. Must be side-effect free, because it runs before execution and before hooks. ConfirmableReports whether a call needs approval, which tag triggered it, and how to render the command. ArgumentValidatorReports missing required arguments so the runner can reject a malformed call before an operator ever sees it. A tool implementing none of these is still runnable. It traces by name, accounts as KindUnknown, receives no dependencies, and is not treated as remote.\nTwo axes that are never derived from each other Axis Values Used for Kind unknown, application, builtin, remote, custom Accounting and the kind= log token Presentation PresentCommand, PresentRemote, PresentSelfRendered, PresentTraced Whether and how a renderer shows the call One kind spans two presentations: the human-in-the-loop tools are self-rendered, while the memory and knowledge tools are traced. Remoteness is read from Present == PresentRemote explicitly, never inferred from a possibly-empty agent name.\nFrom a command tree to a tool set fisk binary, --fisk-introspect leaf commands become tools ai:deny dropped include filter exclude filter expose.agent.tools unconditional optional optional MCP and a2a only every stage can only narrow, never re-add the survivors share one flat namespace, and a collision aborts the run The deny pass runs with a nil filter before include and exclude, which is what makes it unconditional. Introspect FetchFiskAppModel runs the binary as a subprocess with a 30 second deadline when the caller supplies none, in its own process group, with credentials scrubbed from the environment. Walk the tree ApplicationTools skips hidden commands and their whole subtree. A node with subcommands is a grouping node and produces no tool. Every leaf becomes one tool whose name is its path joined with underscores. Require real schemas A leaf with no precomputed restricted schema is an error telling the operator to introspect with a current fisk, so an empty schema is never shipped silently. Drop ai:deny LoadTools calls FilterTools with a nil filter first. That pass exists solely to enforce the deny tag. Apply include, then exclude Each is honored only if it carries patterns or tags. Patterns are regexes over the underscore name; tags are exact matches, where an empty string matches an untagged command. Narrow for serving ServedTools applies expose.agent.tools on top for MCP and a2a. Introspection deliberately does not run in the per-run working directory. A binary that reads a relative config at introspect time therefore sees the same one the operator would, so the exposed tool set cannot differ between runs.\nAn application_path that is unset is not an error. An agent may run on built-ins and remote tools alone.\nSchemas The per-command JSON schema is computed by fisk during introspection, not here. Fisk emits an additionalProperties: false object schema and a restricted variant trimmed to the strict subset the model API accepts. InputSchema returns that schema as-is, unless global flags are exposed.\nmergeGlobalFlags clones the schema, the properties map, and the required list before touching anything. The model schema is shared and reused on every request, so mutating it in place would leak across calls and, worse, would break the resume fingerprint. A global flag’s description is prefixed with Global flag: so the model knows the setting is application-wide, and string-typed completions become an enum.\nOptional parameters are annotated on the wire rather than in the neutral schema. toolkit.AnnotateOptional copies the property map and appends (optional) to each description, because models under-weight mere absence from required and will interrogate the operator for a parameter the command would have defaulted.\nglobal_flags failures are loud rather than silent. An unknown, framework, or hidden entry is an error, and the two cases are distinguished so an operator is not hunting for a typo that does not exist. A framework flag deny-list keeps help, completion, fisk-introspect, and version from ever becoming parameters, since exposing them would make a tool print usage instead of running. A required global is exposed whether or not it was allowlisted, because the command cannot run without it. A global that collides with a command’s own flag is skipped for that command, so a call is never ambiguous.\nTags Tags come from the Fisk command definition and survive introspection. Three are interpreted; everything else is free-form and matched only by the filters and by confirm_tags.\nTag Effect ai:deny Never exposed to any model over any transport. Dropped before include and exclude, so no selection can re-add it. It also beats ai:confirm: a doubly-tagged command is never exposed at all. ai:no_defer Always sent directly rather than hidden behind tool search, even inside an otherwise deferred set. Inert over MCP and a2a, which do not defer. ai:confirm Always requires operator approval. Matched unconditionally, so leaving it out of confirm_tags cannot weaken it. harness.confirm_tags lists additional tags that gate exactly as ai:confirm does: exact match, no regex, additive. The common use is an existing application convention such as impact:rw. That is an example of an operator-supplied tag, not something Fisk AI understands.\nConfirmTrigger prefers ai:confirm when present, since it is the strongest and mode-independent signal, and otherwise takes the first matching tag in the tool’s own tag order, so prompt wording is deterministic.\nAll surviving tags are appended to the model-facing description as a single Tags: line. That is deliberate: operator prompts can key off tag names, and the terms stay searchable when the tool set is deferred behind tool search.\nLoad-bearing decision Approval is never expressed as an MCP annotation, because annotations are advisory hints a client may ignore rather than a control channel. That keeps the exclusion story single-valued: for anything that must be unreachable the answer is ai:deny, which drops the command before any filter or gate can reach it, not ai:confirm, which depends on a client choosing to honor it. See Serving: MCP and A2A.\nNothing rejects or warns about an unrecognized ai:* tag. The prefix reads as reserved but is not enforced, and such a tag reaches the model in the Tags: line like any other.\nTwo ways a human enters the loop These are distinct mechanisms and are worth keeping apart.\nThe confirm gate puts the operator on the path of a command the model chose. util.ConfirmGate.Approve checks the per-run allow list, then default-denies if no operator is reachable, then default-denies on a canceled context, all before any prompt is drawn. Only then does it call the prompter. An “always” answer is remembered for the run, keyed by tool name, so it covers that command with any arguments.\nA denial returns a normal result, not an error, whose reason states that the decision is final and not to retry. That wording is the point: the model treats the refusal as authoritative rather than as a transport failure to route around.\nThe human-in-the-loop tools let the model ask its own question: ask_human_confirm, ask_human_select, and ask_human_input. Each handler decodes, sanitizes, validates non-empty, checks that an operator is reachable, checks the context, and only then prompts. Every non-affirmative path is a normal result carrying a reason. Only malformed input is an error.\nask_human_input treats an empty answer as a real value, distinguishing “typed nothing” from “no answer”. ask_human_select starts at no selection so a cancel never silently picks the first option, and ask_human_confirm lists the safe option first so a reflexive Enter declines.\nThese tools exist because the loop ends on a text-only turn. A model that asks a question in prose silently ends the run. HITLSystemNote names the enabled tools in the system prompt so the capability is discoverable and the note cannot drift from what is actually wired.\nThree startup advisories cover the ways this can be misconfigured: HITL enabled with no reachable operator, gated tools with no reachable operator, and each confirm_tags entry that matches no loaded tool.\nExecution No shell, ever. Model arguments become argv and nothing else. Model.ArgsFromJSON is the trust boundary: it bounds arguments to the command’s schema and rejects unknown properties. Exposed globals are placed after the command path and before the -- separator so they always parse as flags.\nCredentials are scrubbed by name. The environment handed to a command drops the union of every linked provider’s credential variables and the operator’s own api_key_env names. The union covers all linked providers, not just the active one. The limit is stated honestly in the source: it cannot catch the same secret exported under a second, unnamed variable.\nPWD is rewritten so the child never sees two. HOME and TMPDIR are deliberately left inherited: repointing TMPDIR into the work directory would collide with the tool’s own output there and let a cleaning tool delete it, and repointing HOME breaks tools that read credentials or config from it.\nThe whole process group is killed on cancel. exec.CommandContext signals only the direct child, and WaitDelay bounds only the parent’s wait for I/O, so a wrapped CLI that forks would leak orphaned grandchildren. That is invisible in a short-lived CLI and cumulative in a long-lived server, so both execution paths set their own process group and kill the group. On non-Unix platforms this is an explicit documented no-op.\nOutput is bounded, differently on each path, for a reason. Tool output keeps the first 64 KiB and the most recent 32 KiB with a truncation marker in between, because a head-and-tail view is usually enough to act on. Introspection output is capped at 16 MiB and then rejected rather than truncated, because the document has to parse whole. Both writers report full consumption so the os/exec copier keeps draining the pipe.\nA single writer is set on both stdout and stderr, so os/exec serializes them and the interleaving order a terminal would show is preserved without buffering the whole stream.\nA canceled or timed-out context is an error. A non-zero exit is a normal result carrying exit_code and output. A failure to start the binary is an error.\nEverything an operator sees is sanitized before truncation, so a cut can never leave a dangling escape sequence. Command lines get a 2000-rune cap, because truncating shorter could hide the very argument being approved. Short trace lines elide argument values only, never the command path or flag names, and only for display: the gate and the executor always see the full value.\nValidation before approval Fisk silently drops a missing required flag and fails only on the command’s own exit. So MissingRequired runs on the effective call before the gate. The operator is never asked to approve a structurally invalid call, and nothing runs.\n“Supplied” means the key is present regardless of value, so false, 0, \"\", and [] all count. A non-object input passes through for fisk’s own error message. The invariant relied on is that fisk forbids a parameter that is both required and defaulted.\nMissingRequiredMessage returns the full required and optional roster, with optional parameters sorted since properties are an unordered map, so the model can fix the call in a single turn.\nLoad-bearing decision A tool’s Definition JSON must be byte-stable across process restarts, because the tool set is hashed into the resume fingerprint. That is why AnnotateOptional copies rather than mutates, mergeGlobalFlags clones, parameters are sorted, custom tools are appended in name order, and defer_loading is emitted unconditionally including a present false. Any non-determinism here turns into a spurious resume refusal. See Sessions and replay.\nThe built-in tools All in-process, all KindBuiltin, all never deferred.\nEach declares the serving surfaces that may ever carry it. The declaration is the ceiling; an operator’s allowlist narrows it further and can never widen past it, and the zero value serves nowhere.\nTool Group MCP A2A Notes ask_human_confirm HITL no no Yes or no, deny by default ask_human_select HITL no no Up to 25 options, labels sanitized, never auto-picks ask_human_input HITL no no Free text with an editable default; the description says explicitly it is not for secrets memory_list memory no no The live view, contrasted with the start-of-run index memory_read memory no no A miss is a soft result, not an error memory_write memory no no Create by default; an existing-key reply names the colliding description memory_delete memory no no Idempotent knowledge_search knowledge yes no Read-only ranked retrieval knowledge_enumerate knowledge yes no Read-only complete-set matching; returns paths and counts, no text The HITL tools need an operator at a terminal and the memory tools carry operator state, so neither is offered on a served surface. Both knowledge tools are read-only and need no prompt, so both declare MCP. Neither declares A2A exposure because there is no a2a builtins allowlist: without a selection mechanism, declaring it would serve it the moment an operator enables a2a, with nothing to narrow it.\nThe two knowledge tools are meant to be served together, since a client that can rank but cannot enumerate has the defect enumeration exists to fix. That is carried by a note from notePartialKnowledgeSet, never by one allowlist entry selecting the other: selection stays strictly per tool, so naming knowledge_search serves exactly knowledge_search. The per-tool filter now has two genuinely servable tools to keep apart, which tests it harder than a tool that could not be served either way.\nmustNew is the single chokepoint that stamps KindBuiltin, and it panics on a bad spec. A compiled-in static spec is correct by construction, so a failure there is a programming error, surfaced the way regexp.MustCompile does rather than threaded through every factory.\nEvery group ships a system-prompt note, on the stated theory that without discovery text a model under-reaches for the capability. Every group can also be enumerated with a nil store so fisk info can list names offline; the handler then returns an error rather than panicking.\nWhat functool provides functool is the generic backend. Declare a Spec with a name, description, JSON schema, and a Go handler, plus optional Confirm, ValidateRequired, Trace, NoDefer, Remote, and Kind, and get a full Tool that also satisfies Describer, Confirmable, and ArgumentValidator, with each capability inert unless the spec enables it.\nPresentation is derived rather than declared: Remote implies remote presentation and kind, a Trace function implies traced presentation, and neither implies self-rendered. Kind defaults to KindCustom, so an embedder’s tool is accounted correctly with no extra work.\nNew refuses incoherent specs: a missing name, description, schema, or handler; ValidateRequired with no required parameters, which would silently validate nothing; and remote combined with confirm-gating, since a remote tool is gated by the agent serving it and never locally.\nThree consumers exist today: the harness built-ins, a2a remote tools, and caller-injected custom tools.\nWhat is not a sandbox The source is repeatedly explicit about this, and the code map should be too.\nThe work directory is collision avoidance, not confinement. A functool handler is trusted code running with the agent’s own privileges and an unscrubbed ambient environment. It must never read a secret from the environment or pass that environment to a subprocess. Over MCP there is no local operator, so confirm_over_mcp: never and clients that do not support elicitation run ungated. Credential scrubbing is name-based, so it cannot catch a secret exported under a name nobody declared. Reserved and unused ConfirmSpec and ValidateRequired on functool have no production users. No shipped built-in or remote tool sets them; they exist for embedder-supplied tools and are exercised only in tests and the documentation examples. Spec.NoDefer is likewise unused, since the fisk path uses the tag instead. Presentation has no unknown member. Its zero value is PresentCommand, so a tool without a Describer gets a real KindUnknown sentinel but command presentation. The asymmetry is worth knowing when reading trace output. The unrestricted Schema from introspection is populated and never consumed; only RestrictedSchema is used. The same goes for LLMExtraInfo, Cheats, CheatTags, and per-command Cheat. functool.Result is used only by the remote-tool constructor; the built-ins use a local helper for the same job. The survey prompter ignores its context argument in all four methods. Survey cannot select on a context, which is exactly why the authoritative checks happen before the prompt and why the full-screen prompter exists. The introspection timeout, the wait delay, and the output cap are fixed constants rather than config. The escape hatch for the timeout is a caller-supplied deadline. Next Model providers covers the other side of the loop: how a request reaches a model, and what makes a local Anthropic-compatible endpoint work.",
    "description": "Turning a Fisk command tree into tools the model may call, and the tags that decide what it may not",
    "tags": [],
    "title": "Tools and introspection",
    "uri": "/fisk-ai/design/codemap/tools/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "internal/llm is the domain model for talking to a model, and it imports no vendor SDK anywhere. Exactly one package, internal/llm/anthropic, translates that model to and from the Anthropic SDK. Everything else in the tree, including the durable journal, speaks the neutral types.\nWhere it lives internal/llm holds the contracts. Key files: types.go for the message model, provider.go for the interface, request.go, response.go, middleware.go, registry.go. internal/llm/anthropic holds the only provider: provider.go, codec.go, tools.go. internal/llm/README.md is the normative contract document and the best source for the reasoning.\nan SDK is spoken only right of this line agent loop builds one request llm.Request provider-neutral anthropic codec the only SDK importer middlewares http debug, then tracer Messages API no streaming path runstate journal stores llm.Message The neutral model is not only an abstraction over vendors. It is also the durable format, which is why lossless round-tripping is a hard requirement. The interface type Provider interface { Call(ctx context.Context, req Request) (*Response, error) Capabilities() Caps } Call owns the wire call end to end, including the per-call timeout. Caps reports the neutral provider id, whether tool search is supported, and a declared maximum output.\nCapabilities are declared rather than discovered, because neither Anthropic nor OpenAI expose capability flags at runtime. The set is deliberately minimal and grows when a second provider makes a real difference concrete rather than predicted.\nThe neutral model is also the on-disk format runstate records store llm.Message and llm.ToolResultBlock directly. An earlier version stored the Anthropic wire format and did not round-trip, which is why the record version refuses both older and newer snapshots.\nThat makes lossless preservation a hard requirement rather than a nicety:\nThinkingBlock.Signature is []byte. The neutral model never inspects or renders it; it only preserves it. The model rejects a turn whose thinking signature was dropped or altered. ToolUseBlock.Input is json.RawMessage, so arguments survive byte-for-byte with no schema-shaped intermediate. ProviderBlock{Kind, Raw} is the escape hatch for a server-side block the neutral model does not name. Kind is the provider’s own discriminator and Raw its faithful JSON. A golden test asserts a byte-identical round-trip across thinking, redacted thinking, text, server tool use, web search results, tool use, and tool results. That test is the tripwire for the whole scheme.\nOne codec subtlety worth knowing providerBlockToNeutral reads the type discriminator out of the marshaled JSON rather than calling GetType(), because the SDK leaves each block’s Type field at its zero value and fills the default only on marshal. A block with no discriminator is an error rather than a silently untyped passthrough.\nComing back the other way, repairToolSearchResult rebuilds the entire content union from Raw. The SDK decoder drops tool_references on a successful tool-search result and mis-selects the error variant, so a plain unmarshal would lose data. The error variant already round-trips and is left alone.\nA response is routed through the same block codec as a stored message, so a reply and a journaled turn share one representation.\nThe registry, and credentials as a boundary Registration is by import side effect. The agent package blank-imports the provider, and a second provider is a second blank import there.\nfunc Register(name string, factory Factory, credentialEnvNames []string) The credential list is a required positional argument, not an option. A provider cannot be registered without declaring its secrets.\nLoad-bearing decision llm.CredentialEnvNames() is a security boundary, not documentation. Its union is stripped from the environment of every model-chosen tool subprocess, and it covers every linked provider rather than only the active one. Selector variables such as ANTHROPIC_PROFILE and XDG_CONFIG_HOME are deliberately excluded, since they hold no secret and stripping them buys nothing a tool could not rediscover. An injected agent.Options.Provider bypasses the registry, so its credentials are not in this union and are therefore not scrubbed.\nRegister panics on an empty name, a nil factory, or a duplicate, mirroring database/sql.Register. Each is a programming error resolvable at compile time.\nThe request Request carries no client, no credential, and no timeout, so it is a plain value a test can build and assert on.\nTwo fields deserve a note. SystemBlocks is a slice rather than a string because Anthropic sends separate system blocks and places the cache breakpoint on the last one; a string-system provider would join them. Interactive exists only to select a longer cache TTL.\nThe SDK system slice is rebuilt fresh on every call, specifically so marking its last element for caching cannot write through to any value the caller hashed into the run fingerprint. That has its own test.\nTool declaration defer_loading is emitted unconditionally, including a present false, so the rendered tool is a pure function of the neutral value rather than varying with its zero state.\nSchema rendering maps properties and required to dedicated SDK fields and forwards every other schema key verbatim through ExtraFields, notably additionalProperties. The type key is dropped because the SDK fixes it to object.\nStrict mode is deliberately not used: its grammar compilation caps the total optional parameters across all tools, which a broad command tree exceeds.\nDeferral itself is decided in util.BuildToolParams over the combined local, remote, custom, and built-in count against a threshold of 10. It reports back whether anything actually deferred, so the tool-search tool is only requested when there is something to find.\nMiddleware Middleware and MiddlewareNext are type aliases rather than defined types. That is what lets an llm.Middleware pass straight into the SDK’s option.WithMiddleware unchanged.\nMiddlewares are assembled in the agent, where their lifecycle lives, not in the provider. The SDK wraps from last to first, so the first appended is outermost. With the current order the HTTP debug dump is outermost and the tracer sits closest to the wire.\nHttpDebugMiddlewareDumps the request via GetBody and the response by buffering and replacing it, so the SDK still parses normally. The sink is injectable so it cannot corrupt the full-screen UI. Tracer.MiddlewareJSON-lines trace of every request, response, and error. Nil-safe, and tracing never changes the call's outcome. Middleware is HTTP-level and therefore invisible to an injected provider. The PreModelCall and PostModelCall hooks are the provider-agnostic complement: they sit above the provider and fire either way.\nNo streaming, and what compensates There is no streaming path. Call uses the non-streaming Messages API only.\nThe accommodation is on the output cap: the thinking-mode default of 16384 stays within the non-streaming ceiling that keeps responses clear of SDK HTTP timeouts. Streaming-shaped feedback is achieved at a coarser grain through per-turn events and the verbose request summary.\nRetries Retries are delegated entirely to the SDK default of two retries, three attempts. Two consequences follow.\nThe retry loop lives inside the middleware-wrapped handler’s caller, so middlewares are re-entered per attempt. The tracer relies on exactly that: it reads the SDK’s retry-count header to set attempt, giving each retry a new trace id while reusing the iteration number.\nAll attempts share the one per-call deadline, because Call wraps the context before dispatching. So llm.budget.call_timeout bounds the attempt series, not each attempt.\nCall adds one error-shaping rule: a 400 while thinking is enabled is annotated with the suggestion to set llm.thinking.enabled to false, because older and compatibility models reject adaptive thinking.\nToken accounting Usage has exactly four tiers: input, output, cache read, and cache create. The run sums all four, and the same four are journaled on each assistant record so a resume can reseed the totals.\nInTokens is the uncached remainder, which makes the summary line diagnostic. A healthy multi-iteration run shows a small input count and a climbing cache-read count. A silent cache miss shows cache reads stuck at zero against a large input count.\nThe budget check sums all four tiers, so the cap measures total throughput and keeps its magnitude comparable to the pre-cache world.\nLocal and compatible endpoints Running against ollama, llama.cpp, or LM Studio is a first-class deployment, and four knobs exist mostly for it.\n--base-url or ANTHROPIC_BASE_URL is applied only when non-empty, so the SDK default endpoint is used otherwise. util.ValidateBaseURL requires http or https, rejects embedded userinfo, and requires https for any non-loopback host. Plain http is allowed only for 127.0.0.1, ::1, and localhost, so a local server keeps working. It does not resolve names, so a hostname that happens to resolve to loopback is not treated as loopback.\nValidation runs twice on purpose: once at the CLI boundary, so a bad base URL fails on a normal terminal before the HTTP debug file is created or the full-screen UI starts, and again inside agent.Run. That is what lets the provider factory declare construction infallible.\nllm.no_tool_searchThe manual complement to the capability flag. The flag says tool search is possible; this switch turns it off for an endpoint where it is possible but unwanted, such as a proxy that does not implement the tool-search tool. llm.no_prompt_cacheFor a proxy that rejects or ignores cache_control. Disabling only raises cost; it never changes output. llm.budget.max_output_tokensSet it to fit an endpoint whose per-response limit is below the default. llm.thinking.enabledOff by default, because older and compatibility models reject adaptive thinking. A future anthropic-compat selector is anticipated, and the contract document is explicit that it must still report the same provider id when the backend semantics are identical, so it does not break resumes.\nProvider identity gates a resume Capabilities().Provider is stamped into the run fingerprint, and it is the resolved provider’s own id rather than the config selector. A provider change is a hard resume refusal that --force cannot cross, which is why Provider is excluded from the fingerprint’s Equal and Diff: those govern only forceable drift.\nPrompt caching is deliberately outside the fingerprint, so toggling it never refuses a resume.\nReserved and unused Three exported codec functions have no non-test callers: ToolUseToNeutral, ToolResultToAnthropic, and ToolResultFromAnthropic. Their comments reference boundaries that now use neutral types end to end. They are migration residue. Caps.MaxOutputTokens is declared but never read. Nothing clamps or validates the per-call cap against a provider’s stated ceiling, and the Anthropic provider leaves it zero. Providers() has no caller beyond the unknown-provider error message. The factory’s error return is always nil today. It exists for a future provider that can fail to construct. StopStopSequence is never branched on. A stop-sequence reply with no tool call reads as a completed answer. Only one provider exists. The declared next target is OpenAI, explicitly with no new SDK dependency, following the hand-rolled client in the rag embedder rather than taking on an SDK whose types would leak back through this layer. The known hard spots are enumerated in the README: per-tool-result messages versus Anthropic’s batched synthetic user turn, a string system prompt which voids the cache-breakpoint mechanism, and a thinking round-trip that may need more than a single opaque field. Credential selection is not yet provider-conditional. --api-key is hardwired to ANTHROPIC_API_KEY and unconditionally required. internal/util/anthropic.go is named for the provider but contains no SDK reference and is fully neutral. The name is misleading rather than meaningful. A latent trap, not a bug today: Call unconditionally applies its timeout, so a provider built with a zero timeout would fail instantly. The run path always supplies a resolved value, and the one zero-timeout construction only reads capabilities offline. Next Sessions and replay covers what the journal does with these neutral messages, and why a provider change is the one refusal --force cannot override.",
    "description": "A provider-neutral message model that doubles as the on-disk format, and one package allowed to speak an SDK",
    "tags": [],
    "title": "Model providers",
    "uri": "/fisk-ai/design/codemap/providers/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "A checkpointed run writes an append-only journal. Nothing about the run’s configuration is stored: the fold reconstructs the conversation, and everything else is rebuilt from the config file. That is what makes a resumed run verifiable rather than merely restarted.\nWhere it lives internal/runstate holds the format and the fold. Key files: record.go, state.go, fingerprint.go, store.go, validate.go, schemas.go. Backends are internal/runstate/file and internal/runstate/jetstream. The CLI surfaces are session_command.go and resume_replay.go.\nRecords A record is a sequence number, a protocol id, and exactly one payload. Sequence starts at 1 on the meta record and is the sole ordering authority.\nThe protocol ids sit under io.choria.fisk-ai.v1.session.*. The namespace constant is spelled out by hand rather than imported from the A2A package, so storage does not depend on the protocol package, with a comment noting it must track the other. The intent is stated plainly: the record stream maps one to one onto the durability journal today and onto the A2A event stream later, so one model backs both local resume and remote streaming.\nMeta seq 1, fingerprint Assistant before any tool ToolResult one per tool User chat follow-up Terminal once, at the end strictly increasing seq, fsynced or write-once per subject Fold(records) pure, no IO, derives everything a crash loses at most one tool result The assistant turn becomes durable before any tool runs, and each tool result as it completes. Write order is the durability argument Every append goes through one helper that is a no-op when the journal is nil, so an unchecked run costs nothing.\nThe assistant record is written before any tool executes, so a crash midway through a batch resumes without re-paying for that model call. One tool-result record is written as each tool completes, so a crash loses at most one tool. A user record is written only for a free-standing interactive follow-up. The initial prompt lives in the meta record and tool results in their own records. A journal failure here ends the session before the turn, so the journal stops at the last coherent boundary rather than recording assistant turns with no preceding user message. The terminal record is written once, at the true end, carrying the reason and any error text. A failed terminal write is a warning, not a failure. CheckAppend decides whether an append is a duplicate, a gap, or valid, but the backend advances its own last-sequence counter. The doc comment explicitly forbids folding the advance into the helper: the writer must advance only after the record is durably stored, which is what makes a torn or failed write re-append the same sequence instead of losing it.\nThe fold Fold is pure and does no IO. Counters and the resume position are derived from the records rather than stored, so they cannot drift from the recorded events.\nTwo invariants define the output:\nMessagesAlways ends on a boundary the model API would accept. An in-flight tool batch never appears here. PendingHolds the partly-answered assistant turn: the message, its iteration, its stop reason, the results so far, and the set of answered tool-use ids. Role alternation is reconstructed rather than stored. A user record holds only the newly typed blocks, never a merged view, and the fold re-derives the same merge the live path performs. Recording the merged message instead would double the tool-result blocks the fold already appended. A test asserts both paths reconstruct an identical conversation.\nOn resume, the loop first completes the pending turn: it reuses journaled results for answered tool-use ids, runs only the unanswered ones, journals each, and then commits the assistant turn together with the full result set.\nThe fingerprint The fingerprint records the configuration the journal was written against: provider, model, a hash of the system prompt, a hash of the tool set, thinking mode, max tokens, and max iterations.\nContinuing a conversation against a changed configuration can be genuinely incoherent. A stored tool call may reference a tool that no longer exists, or a thinking signature may be rejected outright. So the gates run in a fixed order.\nGate Behavior Already completed Refused. Only ReasonCompleted blocks a resume Interactivity mismatch Refused in both directions, as defense in depth behind the CLI’s own reconciliation Provider changed Refused unconditionally. --force does not apply Anything else changed Refused unless --force, with a per-field diff Provider is checked before drift so the message is unambiguous, and it is deliberately excluded from Equal and Diff so those govern only forceable drift.\nLoad-bearing decision The system prompt is only ever hashed, never stored verbatim, and there is a test named for exactly that: it must not leak a sensitive prompt into the fingerprint on disk. Three things are deliberately outside the fingerprint so that data drift never blocks a resume: the memory index, the resume reminder, and the prompt-cache setting. All three are appended after the fingerprint is computed and none is persisted.\nA resume seeds the sequence from the journal’s last, the iteration from the recorded next, plus the pending turn, the messages, and the six counters.\nAn interactive resume gets a fresh per-turn iteration budget from where it left off, because a chat’s grown cap is not stored, only the position. A one-shot resume keeps the absolute cumulative cap.\nresumeAtInputBoundary skips the first loop entirely when the session is interactive, has no pending turn, ends on an assistant message, and did not stop on a paused turn. The operator lands at the input bar rather than watching a redundant model call.\nA run resting on a paused-turn boundary raises WarnResumePausedTurn, since the server-side state it depends on may have expired.\nThe resume reminder is advisory: it tells the model that tool results may be stale and to re-verify before taking state-changing actions. It is not enforcement.\nTwo backends Both store byte-identical record bodies, so a run migrates between them unchanged. There is a test asserting the two fold identically.\nfile jetstream Unit One line in one JSON-lines file One message on one subject Write-once Append-only plus fsync MaxMsgsPerSubject=1 with discard-new-per-subject Mutual exclusion Advisory flock on a per-run lock file Per-run tail fence on the expected last sequence Retry safety Last sequence advances only after fsync A nonce message id lets a lost ack be adopted Torn tail Dropped as the final line only Impossible; a message is atomic Crash recovery The kernel releases the flock Nothing to release Requires NATS No Yes The file backend syncs the line, and on the first write of a new journal also syncs the directory so the entry survives a crash, and only then advances its counter. Reads drop an unparsable final line as a torn tail but treat an interior parse failure as corruption, which is valid only because the file is append-only and fsynced.\nThe JetStream backend binds a stream and never creates one; the operator owns durability. The subject prefix is derived from the stream’s single wildcard subject rather than configured, and zero or multiple wildcards are construction failures. checkStreamConfig rejects, at construction, a stream whose per-subject message limit is not 1, whose discard policy is not discard-new per subject, that has any max age, or whose max message size is below the record floor. Each error names the exact nats stream fix, and a missing stream produces a ready-to-run nats stream add line.\nIts exclusion is not a lock. Appends are fenced against the expected last sequence for the run’s subject, so a stale writer is rejected rather than interleaving. A wrong-last-sequence error is disambiguated by reading the target subject back: a matching message id means this writer’s own ack was lost and the record can be adopted, and anything else is a genuine conflict.\nNon-Unix platforms get a stub file lock that does not exclude. The comment is explicit that those platforms rely on the operator not resuming a run twice.\nWhere a run is stored Runs are never stored in the working directory, so state does not leak into repositories, and DefaultDir lives in the core rather than the file backend precisely so that contract stays visible to every backend.\nSessions are also not namespaced by identity, unlike memory. A resume must find its run whatever identity is active, so the registry passes no identity to a session backend at all.\nValidateID is both a format rule and a path-traversal defense: a single safe path component that is also a valid NATS subject token, bounded to 128 characters so the same ids work in both backends without producing an oversized filename. Both stores validate before an id becomes a key or a path, and listings skip ids that fail it.\nLoad reads and folds without locking; Open takes the lock for appending. Resume reads and gates with Load, then takes the lock with Open, so inspection never blocks a running session. The ErrLocked message names neither a process nor a run, because a flock is per open file description and a caller genuinely cannot tell which holder it hit.\nVersion 3 is refused in both directions A newer snapshot may carry record shapes this build cannot fold. An older one predates the provider-neutral record format and does not round-trip. Refusing beats silently mis-folding, so neither is accepted.\nReplay Three renderings share one folded state: live narration to stderr on resume, a plain-text dump for inspection, and structured lines for the full-screen transcript viewer.\ntranscriptLines pairs each tool call with its result by tool-use id across the whole conversation, so calls and results interleave as they did live. An unanswered call in a suspended turn simply has no result. All three walks handle the pending turn explicitly after the committed messages, since the in-flight turn lives outside the conversation.\nA read-only view renders tool calls without loading the tool registry, while the live resume path renders the resolved command line when the tool is still known.\nReserved and unused The JSON schema validator is not wired into any write or read path. Five schemas exist, are embedded, and are compiled, and nothing in production calls them; only tests do. It exists as the published contract for external and future consumers, and for parity with A2A, where the equivalent validator is on the live path. The message and result objects in those schemas are intentionally opaque, so provider-specific blocks round-trip verbatim without the schema constraining them. Journal.Records() has no production caller. The resume path uses the unlocked load followed by a locked open. Backends() has no CLI surface, despite its doc comment; it only feeds the unknown-backend error message. RunInfo.Created is populated and never displayed. session ls shows only the updated time. SessionInteractive double-dials NATS on a JetStream resume, once for the pre-flight meta read and once for the resume itself. That is accepted for simplicity. RemoteToolCalls and the per-record remote flag are counted and displayed, but resume logic does not act on them. internal/agenttest.FakeSessionStore is an in-memory implementation written in a separate package to prove the interface is implementable from outside. It reuses the shared id and append validation. Next Memory covers the other durable store, and why it is deliberately kept out of the fingerprint.",
    "description": "An append-only journal, a pure fold, and a fingerprint that refuses an incoherent resume",
    "tags": [],
    "title": "Sessions and replay",
    "uri": "/fisk-ai/design/codemap/state/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "Memory gives an agent four tools for storing short markdown notes that survive the end of a run. The design problem is not storage. It is that both the key and the body are written by a model, so every value read back is untrusted input that will be placed into a later prompt.\nWhere it lives internal/memory holds the contract and the shared rules. Key files: store.go, key.go, write.go, frontmatter.go, registry.go. The backends are internal/memory/file and internal/memory/jetstream. The model-facing tools are in internal/toolkit/builtin/builtin_memory.go.\nThe contract memory.Store (internal/memory/store.go:105) is four methods.\ntype Store interface { List(ctx context.Context) ([]Item, error) Read(ctx context.Context, key string) (description, content string, err error) Write(ctx context.Context, key, description, content string, overwrite bool) error Delete(ctx context.Context, key string) (existed bool, err error) } Item (store.go:96) carries only Key and Description, never the body. That is what makes the index cheap enough to inject into every prompt. Reading a body always costs a tool call.\nThe interface documents a stronger concurrency requirement than usual: an implementation must be safe for use by independent processes sharing one backing store, not merely by goroutines in one process. Two agents pointed at the same directory or the same NATS bucket are the expected case.\nThree sentinel errors carry the outcomes the tool layer needs to distinguish: ErrExists, ErrNotExist, and ErrStale.\nThe limits are constants in one file, and the two exported ones leak into operator-facing documentation.\nmaxKeyRunes200, sized so key + \".md\" fits on every filesystem. maxDescriptionRunes500 after normalization to a single line. MaxContentBytes64 KB. The cap shown by fisk info. MaxEntries1024 memories per store. MaxEntryBytes69600. Content plus a deliberately over-estimated frontmatter overhead. This is the number an operator passes to nats kv add --max-value-size. The two byte caps are close enough to read as a contradiction. MaxContentBytes bounds what the model may write. MaxEntryBytes bounds what the backend stores, because the stored value includes the frontmatter header.\nThe key charset is the whole traversal defense ValidateKey (internal/memory/key.go:25) enforces five rules in order: non-empty, at most 200 runes, matching ^[A-Za-z0-9._=-]+$, no leading or trailing ., and no .. substring.\nThe charset is the intersection of legal NATS KV keys and safe filenames, with / excluded even though KV permits it. That exclusion is the point. A key maps one to one onto a flat filename with no path separator to escape.\nLoad-bearing decision Because a validated key provably carries no separator, filepath.Join(s.dir, key+\".md\") cannot leave the memory directory. Every Store method validates before touching the backing store, and the file backend re-validates on the way out: keyFiles (internal/memory/file/file.go:196) drops any filename stem that fails ValidateKey, so a name planted by hand can never appear in a listing. Loosening the charset to allow / would silently remove the traversal guarantee from both directions.\nNamespacing happens per backend rather than per key. The file backend uses a directory named memory/\u003cidentity\u003e. The JetStream backend prefixes keys with \u003cidentity\u003e., joined with a dot because a dot is a legal key character, so a prefixed key is still a legal key on both sides.\nThe write path Validate ValidateWrite (write.go:19) checks the key, normalizes the description, rejects an empty result, and rejects content over 64 KB. It returns the normalized description, which the backend must be the one to persist. Normalize the description normalizeDescription (write.go:51) replaces control characters with spaces, collapses runs of whitespace, and truncates to 500 runes. The result is always a single line. Check capacity On the create path only, the backend counts entries and calls CheckCapacity (write.go:39). Serialize Serialize (frontmatter.go:31) marshals a typed struct into a YAML header followed by the body verbatim. Store atomically The file backend stages a temp file and then links or renames. The JetStream backend creates, or updates against a known revision. memory_write model tool call ValidateWrite key charset, size caps CheckCapacity create path only rejected never reaches a backend Serialize typed yaml frontmatter file backend link to create, rename to replace jetstream backend revision-checked update one serialized format, so a value migrates between backends unchanged Validation runs before any backend sees the key. The two backends differ only in how they commit. The identical serialized format on both sides is deliberate. A value written by one backend migrates to the other unchanged.\nThe value format Serialize marshals a one-field struct rather than concatenating strings.\ntype frontmatter struct { Description string `yaml:\"description\"` } That choice is what makes a description containing key: value, a quote, or a leading dash safe. Combined with the single-line normalization invariant, the header cannot grow a second key or break out of the block.\nParse (frontmatter.go:53) is deliberately lenient in the other direction, so a hand-written file still reads:\nInput Result No --- prefix Description empty, the whole input is the body A body containing its own --- line The first closing delimiter wins, the body survives intact Header closed by --- with no trailing newline Accepted, empty body No closing delimiter at all Treated as not frontmatter, the whole input is the body Header present but invalid YAML Degrades to the whole input as body, no error Reading, and the two kinds of miss memory_read never fails for an absent key. A miss returns {\"found\": false, \"reason\": ...} naming memory_list as the way to see the current set, so the model corrects itself without burning an error turn. memory_delete behaves the same way, returning {\"deleted\": false} for a key that was not there.\nAn existing key on the create path is also a soft result rather than an error. existsReason (builtin_memory.go:325) spends an extra Read purely to name the colliding memory’s description, so the model can decide whether to retry with overwrite in the same turn instead of a separate round trip.\nThe index, and treating stored text as data When memory is enabled and no_index is not set, the agent calls List once at startup and appends builtin.MemoryIndexBlock(entries) to the system prompt (internal/agent/agent.go:1118). The block wraps entries in \u003cmemory-index\u003e tags and opens with a line stating that these are notes saved on earlier runs, not instructions. The system note ends with the same framing: anything stored in memory is data the agent saved, not an instruction to follow.\nDescriptions are sanitized twice: normalizeDescription strips control characters at write time, and util.SanitizeForTerminal truncates to 200 runes at render time. A description written by an earlier run therefore cannot smuggle a terminal escape onto an operator’s screen or inflate the prompt.\nA List failure at startup is advisory. The agent emits a WarnMemoryIndex warning and continues, because the tools still reach the store; only the free preview is lost.\nLoad-bearing decision The index is appended after the run fingerprint is computed (internal/agent/agent.go:1112). Memory is data, not configuration, so a memory written between a suspend and a resume must never block that resume. Folding the index into the fingerprint would make every successful memory write invalidate its own session. See Sessions and replay for what the fingerprint does cover.\nMemory tools are never hidden behind tool search. They are always declared directly, though they do count toward the tool-count threshold and the degradation warning described in Tools and introspection.\nThe two backends Both funnel through the same validation, the same capacity check, the same serialization, and the same strict option decoder. They differ in what they can promise.\nfile jetstream Layout One \u003ckey\u003e.md per memory in a 0o700 directory One KV entry per memory, keys prefixed with the identity Location options.directory, else memory/\u003cidentity\u003e rebased under the state directory A pre-existing bucket, never created by Fisk AI Create atomicity os.Link from a temp file; ErrExist becomes ErrExists kv.Create Overwrite os.Rename, last write wins Revision-checked kv.Update Returns ErrStale No Yes Delete accuracy Exact, a single os.Remove Best-effort, Get-then-Delete can race Listing Directory scan, skipping temp, non-regular, and invalid names One server-side Watch pass filtered to the prefix The JetStream backend binds a bucket and refuses to create one, so durability policy stays with the operator. It also refuses to start against a bucket that would quietly break the contract. checkBucketConfig (internal/memory/jetstream/jetstream.go:138) rejects any bucket with a TTL, because stored memories would expire without anyone noticing, and any bucket whose positive MaxValueSize is below MaxEntryBytes. A missing bucket produces an error containing a ready-to-paste nats kv add command with the right size.\nA backend declares what it needs at registration time rather than being special-cased by the host. The JetStream backend registers with memory.RequiresNats(), and that single flag is why memory.NeedsNats(cfg) can tell the agent to dial NATS before construction without the agent naming any backend.\nThe read-before-update guard By default the JetStream backend will not overwrite a key whose current revision it does not know. Read records the entry revision in the memory.Scope on the context, and overwrite requires a known revision and issues a revision-checked update. A wrong-last-sequence error maps to ErrStale, and the stale revision is dropped so a retry is forced to re-read.\nA scope belongs to a run, and a turn of a checkpointed conversation is a run. The agent journals the scope as each run ends and seeds a fresh one from RunState.MemoryRevisions on resume, so the revisions follow the conversation across turns, processes and a week of wall clock.\nList deliberately does not grant that authority. Seeing a key in the index is not the same as having read it. A successful create or overwrite does carry authority forward, so a sequence of edits within one conversation costs a single read.\nThe switch to disable this is spelled no_require_read_before_update, one of two negative switches in the memory config. Both are phrased as opt-outs so that omitting them leaves the safe behavior in place.\nThe tools Four tools, all pure. They receive a Prompter and ignore it, because none of them needs a terminal.\nTool Parameters Success Miss memory_list none {\"memories\": [{\"key\", \"description\"}]} sorted by key error only memory_read key {\"found\": true, \"key\", \"description\", \"content\"} {\"found\": false, \"reason\"} memory_write key, description, content, optional overwrite {\"written\": true} {\"written\": false, \"reason\"} memory_delete key {\"deleted\": true} {\"deleted\": false} memory_list is described to the model as the live view, in contrast to the index captured in its instructions at the start of the run. memory_write explicitly tells the model not to write frontmatter itself and restates the key charset, so the common failure is prevented by the description rather than caught by validation.\ncontent is a *string in the read result so that an empty body still serializes as \"content\": \"\" instead of vanishing from the object.\nEvery handler tolerates a nil store and returns an error rather than panicking. That is what makes MemoryTools(cfg, nil) safe for fisk info to call when it only needs to enumerate names offline.\nTraced output for the three key-taking tools runs the model-supplied key through util.SanitizeForTerminal before it reaches the screen, falling back to the bare tool name on a decode failure.\nNone of these tools can be served over MCP or a2a. They declare no serving exposure, which the serving surfaces apply per tool, and the configuration validator independently rejects any name but the two knowledge tools in the MCP allowlist. Both gates must pass, so neither one alone is load-bearing.\nFailing at construction Every memory misconfiguration is a startup error, never a surprise at the first tool call. Unknown backend, unknown option key, unwritable directory, missing bucket, bucket with a TTL, undersized bucket, illegal prefix, and a missing NATS connection all fail before the agent runs. Option decoding uses DisallowUnknownFields through a single shared DecodeOptions helper, so a typo fails as loudly as a bad top-level YAML key and the strict rule cannot drift between backends.\nGrowth points Several parts are generalized slightly ahead of need, and the comments say so.\nErrStale is optional in the contract and unused by the file backend, but the tool layer already handles it. The file backend can gain the guard with no tool change. frontmatter has one field today. The typed struct plus the lenient Parse is the growth path for more header keys. RuntimeEnv is a struct rather than two parameters so a future per-run value is a new field instead of a signature change across every backend. Today each backend ignores the other’s field. RegisterOption exists for one option. internal/agenttest.FakeMemoryStore is a third implementation of Store, written in a separate package to prove the interface is implementable from outside using only exported identifiers. It is test-only and enforces neither validation nor caps.\nNext Knowledge covers the other store an agent reads from, where the content is operator-owned rather than model-written.",
    "description": "Small durable notes an agent writes for its future self, with the key charset as the security boundary",
    "tags": [],
    "title": "Memory",
    "uri": "/fisk-ai/design/codemap/memory/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "Knowledge gives an agent two tools over a locally built index of its own markdown: knowledge_search, which ranks, and knowledge_enumerate, which does not. The design constraint that shapes everything else is that the whole feature ships inside the single Fisk AI binary: no C toolchain at build time, no shared library at runtime, and no external database.\nWhere it lives internal/rag holds the store. Key files: store.go for the handle and schema, index.go for the write path, search.go for the ranked read path, enumerate.go and enumerate_query.go for the set-valued one, words.go for the vocabulary, integrity.go for the write-locked check and repair, chunk.go for chunking, embed.go for the embeddings client, watch.go, doctor.go. The tools are in internal/toolkit/builtin/builtin_rag.go and builtin_rag_enumerate.go, and the CLI in rag_command.go, rag_match.go and rag_words.go.\nThe YAML key and user-facing noun are knowledge; the Go identifiers keep the rag prefix. Knowledge is the feature, RAG is the technique.\nWhy pure-Go SQLite is load-bearing store.go blank-imports two packages: the modernc.org/sqlite driver and its vec subpackage, which registers the vec0 virtual-table module. Both come from one module that is a transpilation of upstream SQLite and sqlite-vec into Go.\nLoad-bearing decision Because there is no cgo, CGO_ENABLED=0 static builds and cross-compilation keep working, and sqlite-vec is linked in rather than loaded as an extension. That removes an entire class of problems: no extension path to configure, no enable_load_extension, and no version skew between binary and extension. Both tiers link in regardless of configuration, so there is no build-tag matrix and no separate “vector build”. A lexical-only deployment still links vec0; it simply never creates the table.\nFTS5 is compiled in for the same reason, which makes the verifyFTS5 check a defensive guard against a surprising build rather than an expected failure. It is nonetheless the one fatal check the doctor reports.\nIndexing Open a writer Build the embedder from config with no network call, create the directory at 0700, take the advisory lock, create the database at 0600 with no-follow, and enforce permissions on the database and both WAL sidecars. Preview the cost On a first build or a reindex with the vector tier on, a dry pass reports how many chunks across how many files are about to be embedded. Nothing is embedded yet. Prepare the vector tier Probe the embedding dimension once, reconcile against the stored manifest, and only then create the vector table and its delete trigger. This runs before any embedding spend. Walk each root Checking the context between files so an interrupt is prompt across CPU-bound chunking. Classify by content hash No row means add, an equal hash means skip, a different hash means update. Chunk, embed, then commit The network call happens outside the transaction; only the upsert, purge, and insert are transactional. Reconcile orphans Only on a full-corpus walk, and only when the walk saw at least one file. The walk skips the store’s own directory, any dotdir, and a sibling memory directory, through a predicate shared with the watcher so the two agree on what the corpus is. Symlinks are skipped entirely, files over 512 KiB are skipped with a note, and non-UTF-8 files are skipped.\nAn interrupted index is not an error. Already-embedded files are committed and skipped on the next run.\nAn equal-hash skip still adds the existing chunk count to the statistics, so the totals stay truthful. A reindex deliberately bypasses the equal-hash short-circuit so a dry-run reindex reports the real work.\nLoad-bearing decision Reconcile is double-guarded: deletion happens only on a full-corpus walk and only when that walk saw at least one file. A walk that errored early can therefore never wipe the index.\nChunking The chunker is heading-aware and size-packed, and it is pure: no database, no IO.\nA fenced code block is collected whole and marked indivisible. Code is never split, even past the maximum chunk size, because splitting code hurts retrieval more than an oversized chunk does. A heading flushes the current section, updates the breadcrumb stack, and starts a fresh packer. The heading line itself is not added to the body; the breadcrumb carries it. A paragraph is the contiguous run of non-blank lines up to the next blank line, fence, or heading, which is what keeps a markdown table’s rows together. Blocks accumulate to a 1200-byte target and a 1500-byte maximum, and a divisible block over the maximum is hard-split. A finished chunk carries its body and its breadcrumb as two separate fields and never folds one into the other. The two are folded in exactly one place, where the chunk is handed to the embedder, so the section title still travels into the vector and pulls the on-topic chunk up; the lexical index keeps them apart.\nThe chunk ordinal is its index within the document. That is exactly why citations shift when a file is edited.\nThe schema documents(id, path UNIQUE, title, mtime, hash) chunks(id, document_id REFERENCES documents ON DELETE CASCADE, heading_path, ordinal, body) chunks_fts VIRTUAL USING fts5(body, heading_path, content='chunks', content_rowid='id', tokenize='porter unicode61') chunks_fts_exact VIRTUAL USING fts5(body, heading_path, content='chunks', content_rowid='id', tokenize='unicode61') chunks_vocab VIRTUAL USING fts5vocab('chunks_fts_exact', 'row') chunks_vec VIRTUAL USING vec0(chunk_id INTEGER PRIMARY KEY, embedding FLOAT[\u003cdim\u003e]) rag_meta(key PRIMARY KEY, value) chunks_fts is an external-content table, so chunk text is stored exactly once and FTS5 holds only the index. heading_path gets its own FTS column because a section title is often the most search-relevant phrase in a chunk.\nchunks.body is the body alone. That is worth stating because it was not always so: the breadcrumb used to be stored folded into the same column, which put the heading text in two columns at once and cost three things. No body-only question could be asked, since every heading term was also a body term. A phrase could match across the join between a heading and the body under it, matching text no document contains. And the breadcrumb was rendered twice by every surface that prints both, and billed twice against the injected-token budget.\nLoad-bearing decision Separating the columns removed an implicit ranking weight. While the breadcrumb sat inside the indexed body, heading tokens were counted twice and BM25 weighted headings without anyone choosing to. bm25(chunks_fts, 1.0, 2.0) puts that back as a decision rather than a side effect. Both bm25() calls in the search statement carry the weights, or the ORDER BY and the returned score disagree, and a spec pins the result against rankings recorded before the split.\nForeign keys are on for every connection, so deleting a document cascades to chunks and, through triggers, to both the full-text and vector indexes. The vector table is created only when the vector tier is on and only once the dimension is known, so a lexical-only index has no vector table at all.\nThree rules govern the sync triggers, and two of the three ways to break them corrupt silently. The hidden first column of a command insert is the target table’s own name, so a copy-paste that leaves the wrong name there writes into the wrong index. Every indexed column must be supplied on a delete, with the old value, or terms are left behind against a rowid that no longer exists and every later delete wedges SQLITE_CORRUPT; search hides exactly that, because hydration drops rows it cannot join. And the FTS5 column names must equal the content-table column names: a mismatch still answers MATCH and still passes a bare integrity check, failing only at rebuild.\nThat last one is why a bare integrity-check is not evidence. Only ('integrity-check', 1), the rank form, compares the index against its content table, and that is the form the specs use, after an insert, an update and a delete.\nNeither form is available to knowledge doctor. Both are command inserts, so both are writes, and every read-only handle carries query_only(1); a reader gets attempt to write a readonly database. Verifying an index against its content table therefore needs the writer and its advisory lock, which doctor does not take.\nTwo tokenizers over the same rows FTS5 sets the tokenizer per table rather than per column, so a second tokenizer means a second table, not a third column. chunks_fts_exact indexes the same rows through the same triggers and stems nothing.\nEvery query runs against the stemmed table. That is what makes an empty result mean “no document holds this word in any form” rather than “no document spells it the way you typed it”. The unstemmed table never answers a search; it earns its size three other ways: it holds real words, so a vocabulary dump returns deprecation rather than deprec; it is the only table where a prefix search grows monotonically; and it lets a stemmed count say how many of those documents contain the word as it was written.\nLoad-bearing decision The unstemmed table must not become the matcher. The idea is attractive, since it would make matching literal and would let a prefix operator ship, and the cost is invisible without measurement. Measured over this repository’s own docs/content, 19 files and 389 chunks: of 20 word forms present in the index, every one loses documents without the stemmer. Two drop to zero, and 17 more return strictly fewer. tooling matches all 19 documents under the stemmer and none without it. A command whose output says “this is the complete answer, not a ranking cutoff” cannot be built on that.\nPrefix behavior is the other half, and it is why no * operator is offered against the stemmed table. Against an index of stems, a prefix longer than the stem but shorter than the word matches nothing, so lengthening a query walks a result set from non-empty to empty and back. Measured against a document containing deprecated:\nPrefix chunks_fts (stemmed) chunks_fts_exact deprec* matches matches depreca* empty matches deprecat* empty matches deprecate* matches matches chunks_vocab is an fts5vocab table over the unstemmed index, exposing every term with the number of chunks holding it. It costs no write time and no bytes of its own. It has to be created by the writer, because a read-only connection cannot create a virtual table at all, in main or in temp; once created it is fully readable through every read-only handle.\nLoad-bearing decision Its doc column counts chunks, not documents. A word in five chunks of one document and a word in one chunk of each of five documents both report doc=5. knowledge words therefore never displays it, never sorts on it and never filters on it: every document count it shows comes from a count(DISTINCT document_id) query per word. Filtering or ordering on the cheap number while displaying the expensive one produces a list whose own column contradicts the flags that built it, and it is the kind of wrong that looks like a rendering bug rather than a counting one.\nThe exact per-word count is the only option on the read path, which was established by exhausting the alternatives. An instance-form fts5vocab would give every word’s true document frequency in one GROUP BY, but it cannot be created on the read-only connection, nor in temp, because query_only blocks the temp database too, and the schema-qualified three-argument form does not exist in this SQLite build, so the scratch-database trick stemSurfaces uses cannot be pointed at an attached index either. Adding it to the schema would be a format generation bump and a reindex, for a scan proportional to total tokens rather than to distinct words.\nNarrowing the vocabulary scan is done in Go, against the whole list. SQLite has no REGEXP, and the driver’s function registration is process-global, so registering one would put it on the agent’s connection too. Taking a literal prefix from the pattern and range-scanning on it is worse than useless: LiteralPrefix describes a prefix of the match, not of the subject, so under unanchored matching a scan from ing silently drops testing. Measured, the alternatives it would have optimized cost about a third of a second across a five thousand word vocabulary, which is not worth a silently incomplete answer from a command whose whole promise is completeness.\nThe format gate An index carries a format generation, and every open refuses any generation but this build’s: a newer one so an older binary never misreads a future layout, an older one because nothing migrates it today. The documents on disk are the source of truth, so the index is rebuilt from them rather than upgraded in place.\nBoth refusals are the same shape, and the gate is deliberately the whole of the mechanism. Nothing drops and recreates inside an open, and no writer destroys an index because a flag implied consent. The operator discards the index with knowledge reset --force and rebuilds it with knowledge index, in that order, which is what the refusal says.\nRebuilding rather than migrating is a decision the absence of users pays for, not a permanent one. The gate is the single place that would change: it is the one point that knows a generation is behind, so a migration added later hooks in there and every open inherits it.\nSituation Where it is refused What the message names Older generation Every reader and every writer reset --force, then index Newer generation Every reader and every writer Upgrade fisk-ai; the index is not the thing that is wrong reset --force is the only path that can act on an index nothing can open, so it removes the file and its WAL sidecars rather than clearing rows: with no handle there are no rows to clear. That path takes the same advisory write lock as any writer, so it cannot race a live indexer, and refuses a symlink at any of the three paths rather than deleting through it.\nLoad-bearing decision Two checks, not one. CREATE TABLE IF NOT EXISTS silently no-ops against a table from an older layout, leaving the stored schema untouched and failing much later as no such column, so the gate runs before it. The pinned generation alone is not enough to catch that: a reset performed by an older build cleared the manifest but kept the table shape, so there is no generation left to compare, and an unpinned manifest is also what a freshly created schema has. The column shape of chunks settles those cases.\nA spec asserts the named commands actually succeed against the state they are named for, since a message naming a fix that also refuses is worse than one naming none.\nThe generation also covers changes that are invisible to every other check. Nothing in the manifest is a function of the text handed to the embedder, so a chunker change combined with the equal-hash skip would otherwise leave unchanged files’ vectors computed from the old text and touched files’ from the new, silently. Bumping the generation is what forces the full re-embed.\nThe pinned vector identity rag_meta holds a manifest: format version, model, dimension, whether vectors are normalized, and both prefixes. Together those are the vector identity, and mismatches are hard failures rather than degradations.\nThe invariant is enforced in three places for three reasons. On the write path before any embedding spend, so a changed model does not cost money before it is refused. At open time for everything except dimension, so opening never contacts the embeddings server. At query time for dimension only, against a live probe.\nAdding the vector tier to a non-empty lexical-only index is also refused, rather than silently searching lexically forever after an operator asked for hybrid.\nEvery mismatch message ends by naming the fix: reindex. A stale manifest yields garbage rankings, which is worse than a refusal.\nNormalization lives in exactly one place. The package L2-normalizes every vector regardless of which embedder produced it, which is what lets the manifest pin normalization to true and lets the default L2 distance stand in for cosine similarity.\nSearch query sanitized FTS5 BM25 always on, fanout 50 vector KNN optional, fanout 50 RRF fusion by rank, not by score truncate to top_k, max 20 then hydrate into citations an unreachable embeddings server degrades to lexical, and always says so Fusion is on rank, so BM25 scores and L2 distances never have to be normalized against each other. The query is split on non-alphanumeric runs, terms under two runes are dropped, each term is double-quoted with embedded quotes doubled, the list is clamped to 40 terms, and they are joined with OR. The quoting is both a syntax fix and an injection guard. The OR is deliberate: the lexical tier supplies recall and the vector tier supplies precision.\nBoth retrievers over-fetch 50 results before truncation to at most 20, so a chunk that is strong in one list and deep in the other still gets its rank boost before the cut.\nHydration is a single join re-ordered to the fused order. A row that vanished mid-flight, because a reindex was running, is skipped rather than failing the search.\nWhen the vector tier is off or degraded, no fusion runs at all and the result is straight BM25 order.\nThe active tier is never ambiguous. One canonical tier line is rendered by every ranking surface and included in the tool’s JSON, and a degraded query renders a distinct line naming the reason. Enumeration overrides it with a line of its own and its machine-readable modes print none, which is covered below.\nEnumeration Search answers “what do the documents say about this” and ranks. Enumeration answers “which documents mention this” and does not rank: it returns a set. The two are different questions, and the second is the one a search cannot answer, because a search that returns nothing means nothing scored well, which is not the same as nothing being there.\nStore.Enumerate is the engine. Two surfaces reach it: the knowledge match CLI verb in rag_match.go, and the knowledge_enumerate tool in builtin_rag_enumerate.go.\nLoad-bearing decision MATCH is a predicate, so the set is complete by construction. No bm25() call takes part, nothing is truncated by score, and the only limit is a display budget applied after the total is known. That is what lets an empty result be reported as an answer rather than as a cutoff, and it is the property the whole feature is for.\nLoad-bearing decision FTS5 booleans evaluate within one row, which is one chunk. A document with “retention” in one chunk and “policy” in the next is found by each term alone and missed entirely by \"retention\" AND \"policy\". Composition therefore happens in Go over document-id sets, one query per term, intersected and subtracted. This is not an optimization detail: a command that claims completeness cannot be built on chunk-scoped booleans, and a spec asserts the single-MATCH form misses the document that the composed form finds.\nThe syntax is small and closed. Every form composes over sets rather than inside one expression:\nForm Example Matches terms side by side deprecated api documents holding both, anywhere in the document a quoted phrase \"retention policy\" the words adjacent, within one section a leading minus api -deprecated documents holding the first and not the second a field prefix heading:retention the section breadcrumb only a field prefix body:retention the body only What is missing is missing deliberately, and each absence is enforced by name rather than passed through to FTS5:\nTyped What happens Instead OR, AND, NOT, NEAR rejected, naming the working form terms side by side; a leading minus to exclude * anywhere rejected matching is by word stem already only exclusions rejected add a term to match an unknown field rejected, naming the two that exist body: or heading: OR is the one that shows why rejecting by name matters. It is two runes, so it survives the minimum-term-length drop, and an unguarded compiler turns foo OR bar into \"foo\" AND \"or\" AND \"bar\", intersecting with every document containing the word “or”. A wrong answer, not an empty one, from the command whose contract is completeness. * is refused for the reason in the tokenizer section: against an index of stems it is not monotonic.\nEvery token emitted into MATCH is double-quoted with internal quotes doubled, and the only unquoted characters this package emits are its own operators and the two column names, which are constants it chooses rather than user text. The doubling is load-bearing here rather than defensive, because a doubled quote inside a phrase is how FTS5 spells a literal quote, so a term can legitimately contain the character that delimits it.\nPer-document body and heading counts are reported separately, which the column split made computable. They are counted as the OR of the query’s terms within each column, not the AND: they are a statistic about how much a document is about the query, so a document holding one term in one chunk and another in the next has to report both chunks rather than the zero an AND would give it. A dropped term, one below the length floor, is named in the result rather than discarded, since a term that was never queried makes the answer complete about a different question.\nEach term is also reported against both indexes: how many documents hold it in any form, and how many hold it as written. The stem is named too, and it comes from SQLite’s own porter tokenizer, run over a scratch in-memory FTS5 table rather than from a second implementation of the algorithm that could drift from the index it describes. The reader cannot compute it in place, since it is query-only and cannot create a table; a failure there leaves the stem empty rather than failing the query, because the stem explains the answer and the answer does not depend on it.\nThe tool surface knowledge_enumerate returns citations and counts, never document text. Reading is a separate call the model makes with the citation, which keeps the decision to spend context on text apart from the decision to find out where the text is. The list is bounded by a quarter of max_injected_tokens rather than a constant, because an operator who raised the budget should get a longer list, and because enumeration precedes the retrieval it exists to inform: spending the whole budget on filenames would crowd out the text the model reasons over. The budget floors at one document, since rounding a real match down to an empty list would read as absence.\nLoad-bearing decision The note field is never omitted. A model does not read the absence of a warning as a signal, so a complete set has to say it is complete as loudly as a truncated one says it is not. The note also names any gap between what a term matched and what is written, in both directions: a count above the literal one because stemming reached other forms, and a zero that is a genuine absence. Without it the model has counts and no reading of them.\nThe tool declares Expose: \u0026functool.ExposeSpec{MCP: true}, on the same terms as knowledge_search: read-only over the operator’s own index, needing no operator at a terminal. config.mcpExposableBuiltins names both, so an operator can allowlist either or both.\nLoad-bearing decision They are meant to be served together, and that is a note rather than a mechanism. notePartialKnowledgeSet tells an operator who exposed one what the missing half costs their clients; it does not select the other for them. Making one allowlist entry serve two tools would break the property the whole exposure design rests on, that selection narrows capability and never widens it, and would silently invalidate the spec that guards it. A client served only knowledge_search is exactly the case enumeration exists to fix, so the note is worth printing, but not at that price.\nHaving two servable tools in one group is what finally tests the per-tool filter properly. mcpSelectedBuiltins applies the allowlist per tool rather than once as a boolean, so naming one knowledge tool serves precisely that one. Before this the guard could only be exercised with a tool that was unservable anyway, which could not distinguish a working filter from a filter that never mattered.\nRAGSystemNote gains one routing sentence, not a syntax lesson: the model is told which question each tool answers, and the tool’s own description carries the syntax. Without the routing the model has two tools and no rule for choosing, and the one it under-reaches for is the one that makes “no” safe to say.\nSoft states and hard failures The split is consistent and worth stating plainly.\nSituation Behavior No index file yet A store opens with a nil database; searches report a status, not an error, so a first agent run still starts Index exists but is empty A status, not an error Index built by an older or newer generation A hard error on every path but the two that declare destruction Embeddings server unreachable at query time Degrade to lexical, set the reason, and report it Embeddings dimension differs from the manifest A hard error Model or prefixes differ from the manifest A hard error naming the reindex Embeddings server unreachable at index time A hard error, because that is when spend was about to happen A silent lexical fallback would be indistinguishable from “vectors did not help”, which is why the reason is always surfaced.\nEnumeration carries its own status enum rather than reusing the search one, and the reason is the feature itself. SearchStatus folds “the index holds nothing” and “the query reduced to nothing” into a single member, and those are exactly the two states enumeration exists to tell apart: one says nothing about the documents, the other says nothing about the query.\nEnumerate status Means ok The set is complete, and may be empty. An empty ok is the answer the feature exists to give index_not_built No index file yet, which is the ordinary first-run state for an agent corpus_empty The index holds no documents, so a zero says nothing about any document query_empty Every term was dropped before anything ran, so a zero is about the query A malformed query is an error rather than a status. It carries a fix, there is nothing to report about the index, and the error is the compiler’s own rather than a wrapped FTS5 one, which would quote the user’s text back inside a parser’s wording.\nEmbedding The client is hand-rolled against an OpenAI-compatible endpoint, batching 64 inputs at a time. On any batch failure it binary-splits down to single inputs while preserving order, so a server with a lower batch cap still succeeds.\nVectors are placed by each response object’s index field, and the batch fails on an out-of-range index, a duplicate index, an empty vector, or a count mismatch. That is what guarantees a vector can never land on the wrong chunk. An error-shaped body returned with HTTP 200 is also treated as a failure.\nInputs are validated before sending: non-empty, valid UTF-8, and under 128 KiB. Responses are read through a 64 MiB limit. A query is truncated to 8192 characters, backing off any partial trailing rune.\nThe dimension probe runs once per process under a mutex, because one embedder is shared across concurrent MCP calls, and it refuses to cache an empty result so a broken server never pins a bogus dimension.\nA non-loopback base URL must be https, so a query is never sent in cleartext. The API key is configured as the name of an environment variable, never as the secret.\nCitations The canonical form is \u003crelpath\u003e#\u003cordinal\u003e, produced by one function and set during hydration. The path is exactly as relative or absolute as the root that was indexed.\nknowledge show parses a citation back and resolves it, and a miss produces a purpose-written hint that citations shift after a reindex.\nWatching The watcher prints its tier line from a read-only store first, deliberately, so it never contends with the writer locks it is about to take.\nMissing or unreadable roots are warned about and dropped, and it errors only if none remain. Every eligible directory is watched, with a file root watched through its parent, and the same skip rules as the index walk apply. A watch-limit error is translated into a single actionable warning about the inotify limit and the watcher keeps going.\nThe event loop is a single-timer debounce with a 100 ms floor. Indexing runs in its own goroutine so a long pass never blocks event draining, which would overflow the kernel queue, and a dirty flag coalesces changes arriving mid-pass into exactly one follow-up pass. A lock held by a competing writer becomes a warning and a retry.\nEvents under the store’s own directory are rejected outright, which is what breaks the WAL feedback loop.\nDeletions are applied stat-guarded: if the path exists again, the delete is skipped. That is what makes an editor’s atomic-save rename harmless.\nOne subtlety worth knowing: a reactive pass is not a targeted per-file ingest. It re-walks every root and relies on the content-hash skip for cheapness. The collected event paths are used only for reporting and for the guarded deletes.\nLocking and platform splits Two independent mechanisms solve two different problems. A single open connection serializes writes within the process. An advisory file lock serializes writers across processes, which the connection limit cannot do because WAL lets multiple processes open the same file.\nOn Unix the lock is a non-blocking flock, and a busy lock fails fast rather than interleaving writes under a timeout. Because it is an flock, the operating system releases it if the process dies, so a crashed writer never wedges future indexing. On Windows it is an exclusive-create lock file, and the comment states the trade-off plainly: it does not auto-release on crash, so a stale lock may need removing by hand.\nNo-follow open is used at exactly two sites, the database and the lock file, so a symlink planted at either path cannot redirect a write. Permissions are enforced on the database and both WAL sidecars, with any symlink among them refused, because SQLite creates the sidecars honoring the umask and could leave them world-readable.\nReader connections are opened read-only by the driver and additionally set query-only as defense in depth.\nPerformance choices Embedding happens outside the transaction, so the slow network call never holds the single writer slot. Hash-based incremental indexing means unchanged files cost nothing, which is also what makes an interrupted index cheap to resume and the watcher’s full re-walk affordable. Purge-then-insert per document covers first ingest and update in one path, and the triggers clear ghost chunks when a file shrinks. The reader pool is kept tiny and short-lived so no pooled connection pins a WAL snapshot long enough to block checkpointing and grow the WAL unbounded across a long session. Auto-vacuum returns freed pages to the operating system after a removal or reindex. It only takes effect on a freshly created empty database, which is precisely why the writer must create the file itself and why that pragma is ordered before the journal mode. Everything is bounded: 512 KiB source files, 1200 and 1500 byte chunks, 40 query terms, 8192-character queries, 128 KiB embed inputs, 64 MiB responses, at most 20 results, and a default 6000-token injection budget. A second full-text index is the largest single line item in the store, and it buys tooling around retrieval rather than retrieval itself. It is paid at index time and on disk, never per query, since no search reads it. prefix='2 3' was measured and rejected: it helps only the two-to-three character band, does nothing at four or more, and costs again in both size and index time, where a compiler-enforced minimum prefix length costs nothing. Reset drops the schema and recreates it rather than deleting rows. It is the cheaper operation, it is the only one that works against a corrupt index, and it means the schema has exactly one definition instead of a per-table rebuild list that has to be kept in step with the table list forever. The tool’s own budget converts injected tokens to characters at four per token and stops adding hits once the budget would be exceeded, but always includes at least the first hit so a large first chunk is not silently dropped to nothing.\nThe doctor fisk knowledge doctor renders the tier line as its header and then runs a series of checks, of which two are fatal: FTS5 compiled in, and the search index matching the stored text. Store presence, journal mode, index writability, each configured path, and the embeddings checks are all reported without failing the command.\nThe policy is deliberate: an absent or unreachable embeddings server is never fatal, so a lexical-only operator is never told their setup is broken.\nA check is three-valued rather than a boolean, because “did not run” is a real answer that neither of the other two can carry. Reporting an unrun check as passing is the dishonesty the whole report exists to avoid; reporting it as failing says something is broken when nothing is. HasFatal ignores an unrun check, since not knowing is not the same as knowing something is wrong, and the report says in words that it verified less than it lists.\nLoad-bearing decision The integrity check is the one that needs the write lock, and it does not go through OpenWriter. That function is the index constructor: it creates the directory, creates the database file, sets persistent journal and vacuum modes, and creates the whole schema. A diagnostic built on it would manufacture the index it was asked to inspect, and the very check reporting “no index file” would report a built store on the next run. withWriteAccess takes the advisory lock directly and opens a writable handle on a file that already exists, refusing when it does not.\nOnly the rank form of integrity-check detects the failure worth detecting. The bare form and the rank-0 form both pass on an index that has drifted from its content table, and a drifted index answers MATCH with fewer rows than the corpus holds, so every search silently under-reports while knowledge match keeps promising a complete set. That is why it is worth taking a write lock for, and why it runs on every doctor invocation rather than behind a flag: a check that guards the completeness contract and is off by default protects nobody.\nIt is cheap enough to be unconditional. Measured at 0.2s over 600 documents, 4,727 chunks and 11 MiB, against 0.02s for the rest of a doctor run, and it leaves the database file’s mtime alone, touching only the WAL sidecar, so what knowledge stats reports as Modified does not move.\nEverything that can prevent the check from running is reported as skipped, not failed: a read-only index file and a read-only store directory are both supported deployments, a concurrent writer is ordinary, and none of them is a finding about the index. SQLite reports the real failure as database disk image is malformed, which reads as a dying disk and is not what happened, so it is translated into a sentence naming the stale table and the repair.\nknowledge rebuild is that repair, and it is a separate verb rather than something the doctor offers. It rebuilds both FTS indexes from the chunk text, leaving documents, text and vectors untouched, so nothing is re-embedded. The reason it is not automatic is that it repairs a derived index over intact text and nothing else: given a damaged chunks table it builds a consistent index over the damage, after which the check passes and reports nothing.\nStore directory resolution is a documented footgun The directory is knowledge.directory or, by default, knowledge/\u003cidentity\u003e. A relative result is rebased under the store directory when one is set, and an absolute one is honored verbatim.\nThe agent and the CLI must pass the same base or they resolve to different directories. That is why --store-dir and FISK_AI_STORE_DIR exist and are documented as distinct from --state-dir, and why the agent raises WarnKnowledgeIndexAbsent when a base was given but no index is there.\nSecurity posture Text is stored unencrypted at mode 0600 inside a 0700 directory, the same posture as memory. The package documentation says outright not to index secrets.\nRetrieved text is framed to the model as untrusted reference data rather than instructions, in the system note and in the tool description alike. The model-supplied query is sanitized before it reaches an operator’s terminal.\nchunks_vocab deserves its own sentence, because it is the one object whose contents are broader than any query result. It makes the full vocabulary of the corpus, with per-term frequencies, readable through every read-only handle, including the agent’s. That is acceptable only as long as nothing serves it: knowledge words is a CLI verb, which the agent cannot reach, and that is the control rather than the table’s own permissions.\nThe reason to keep it that way is not confidentiality. The same read-only handle already returns verbatim document text through knowledge_search, so a vocabulary adds no new class of secret. What it adds is a discovery primitive. Retrieval today requires guessing a word: * is refused outright, and the related-forms explanation names at most five words sharing a stem, and only when the counts disagree. A vocabulary tool removes both limits at once and hands any caller, including one following injected instructions, the identifiers that make the rest of the corpus searchable. It is also, more prosaically, thousands of tokens of low signal for a model that asked a question.\nWhat is deliberately absent Stating the scope matters as much as stating the features. Hybrid here means BM25 plus KNN plus reciprocal rank fusion, and nothing else:\nNo cross-encoder re-ranking. No diversification or per-document capping. No neighborhood expansion around a hit. No query expansion or rewriting. No snippet highlighting; the CLI simply truncates the line. No relevance threshold. Scores are internal and never exposed in the tool JSON or the CLI, and truncation to top_k is the only gate. Embedding is per file rather than per chunk. Any content change re-embeds every chunk of that file; there is no chunk-level embedding cache. Reserved and unused IndexOptions.Extensions is a real extension point that no caller sets, so the indexable extension set is not configurable from YAML or flags today. documents.title is computed and written on every upsert and read on no path at all, which stays true only because the enumerate result carries no title either: adding one would turn a written-and-never-read column into a read one, for output nothing renders. Chunk-level heading paths do all the work. IndexStats.FirstBuild is computed and read by nobody; both first-build previews check the document count themselves. Hit.ChunkID is populated and never read outside the package. rag.Destroy has one caller, knowledge reset --force, and only on the branch where the index could not be opened. Meta.Normalized is always written true and any index with it false is a mismatch. It documents the invariant rather than selecting a behavior. Store.VectorEnabled() and Store.Dir() have no in-repository callers. The search status enum has no degraded member; degradation is a separate flag and the status stays fine. Next Serving: MCP and A2A covers knowledge_search in its other role, as the only built-in a Fisk AI process will hand to an outside client.",
    "description": "A hybrid search index in one SQLite file, with no cgo, no sidecar, and no build-tag matrix",
    "tags": [],
    "title": "Knowledge",
    "uri": "/fisk-ai/design/codemap/knowledge/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "Instead of running a loop, Fisk AI can serve a Fisk application’s commands to something else: over the Model Context Protocol to a client such as Claude Code, or over NATS to another Fisk AI agent. Both modes reuse the same tool selection. They diverge on one question: whether there is a human on the other end.\nWhere it lives internal/mcpserver is the whole MCP mode in one file. internal/a2a is the A2A protocol engine, split into protocol machinery and a pluggable transport, with internal/a2a/nats as the one live binding. internal/remotetools is the policy layer for importing another agent’s tools, and internal/conns owns connection establishment.\nBoth modes are opt-in, and the switch is config presence The presence of expose.agent.mcp enables MCP. expose.agent.a2a.serve_tools: true enables A2A tool serving. An agent that says nothing serves nothing, and each command refuses to start with an error naming the exact key to add.\nServedTools one filtering path mcpserver streamable HTTP, no stdio elicitation gate gated tools are exposed any client on the port the wider trust boundary a2a server core NATS request-reply gated tools dropped no operator to ask NATS peers subject permissions apply The same selection, two confirmation policies, decided by whether a human can be reached. Both servers cap concurrency at 2 and per-call time at 30 seconds by default, for the same stated reason: an external caller has no iteration budget the way the agent loop does, so an ungated path could spawn unbounded concurrent commands.\nThe knobs are nonetheless separate, because they bound different trust boundaries. MCP bounds calls from anything that reaches the TCP port, and the address may be set to a non-loopback interface. A2A bounds NATS peers, which already passed account authentication.\nMCP The transport is streamable HTTP, and only that. There is no stdio serving mode anywhere in the tree; clients connect with an HTTP transport, and the server prints a copy-pasteable claude mcp add --transport http hint on startup.\nAddress resolution is three-tier: flag or environment, then expose.agent.mcp port and address, then 8080 on 127.0.0.1. The loopback default is deliberate.\nThree settings are warned about because they have no effect over MCP: a missing application_path, human_in_the_loop, and memory. Warning beats silently ignoring an operator’s intent.\nShutdown ordering http.Server.Shutdown neither cancels request contexts nor closes hanging server-sent-event streams, so a single Ctrl-C appeared to hang. The fix is a cancelable connection context used as the server’s BaseContext: shutdown cancels that first to unblock held-open GETs, then calls Shutdown with a five second budget.\nThe elicitation gate There is no local operator, so approval is requested from the client through MCP elicitation with a single-required-boolean form.\nThe session’s negotiated capability is read at call time rather than cached, so a client that cannot be elicited is detected per call. The two minute elicitation timeout is applied outside the run timeout and before the semaphore is acquired, so a human deliberating holds no execution slot.\nEverything except an explicit approval under an accept action fails closed: a decline, a dismissal, a non-boolean answer, or an elicitation error all deny.\nLoad-bearing decision confirm_over_mcp: never, and any client that does not support elicitation, runs gated tools ungated. Both serving modes say the same thing in their source comments: ai:deny is the reliable exclusion, and confirmation is not an enforcement boundary. Anything that must be unreachable needs the tag, not the gate. See Tools and introspection.\nAnnotations toolAnnotations maps a tool onto the MCP annotation object, which carries a readable title alongside the protocol’s behavioral hints.\nWhat a command says about itself travels by a different route. Both serving paths register with ModelDescription() rather than the plain Description(), so every surviving tag is appended to the description as a trailing Tags: line and reaches the peer as text. fisk info is the only surface that takes the plain description and gives tags a column of their own.\nApproval is deliberately not an annotation. Annotations are advisory hints a client may ignore, not a control channel, so expressing the confirm gate as one would imply an enforcement it does not have.\nBuilt-ins over MCP Two gates apply and both must pass. Each tool declares whether it may ever be served on a surface, and the operator’s builtins allowlist selects which of those to serve, validated by name at config parse time. The declaration is the ceiling and the allowlist can only narrow it, so a tool added alongside an allowlisted one is never served on its neighbour’s entry, which holds between the two knowledge tools as much as between strangers. Only knowledge_search and knowledge_enumerate declare MCP exposure; the memory and human-in-the-loop built-ins need operator state or interaction, so they declare none.\nThe declaration is default-deny: a new tool is served nowhere until someone says otherwise, and mustNew refuses to build a built-in that has not stated a posture, so a harness tool cannot arrive at “nowhere” by forgetting.\nEvery kind registers through one list and one handler. The caller orders wrapped-application commands ahead of built-ins, and the first tool to claim a name keeps it, so a built-in can never shadow a command tool and strip its confirm gate. A collision is a hard error in Serve; BuildServer skips the loser as a library-level backstop for a caller that bypasses Serve. The reason is the same as everywhere else: the model addresses every tool by one flat name.\nA tool that cannot report whether it is confirmation-gated is refused rather than served ungated, since an absent gate must not be indistinguishable from not needing one. Built-in calls share the semaphore and the timeout, and receive a default-deny prompter, since nothing on that path can prompt.\nBoth kinds return a toolkit.Outcome, and its exec metadata decides the rendering: a command’s output is wrapped in the CommandResult envelope carrying the exit code, while an in-process tool’s output is already the JSON the client asked for and travels verbatim.\nA2A ModeServer additionally requires application_path and nats_context. The server itself accepts any tool kind, but no built-in declares A2A exposure today, so an application-less A2A server would start with an empty tool set. The requirement is an earlier, clearer version of that empty-set error rather than a correctness gate, and it is deletable once a built-in first opts in.\nselectExposed drops five classes of tool, logging a reason for each: tools that do not declare A2A exposure, tools that cannot report whether they are confirmation-gated, confirm-gated tools, invalid tool names, and tools with an empty model-facing description. The exposure check is the ceiling the rest sit under. A tool that cannot answer the confirmation question is refused rather than served ungated, since over an interface the absence of a gate would otherwise be indistinguishable from not needing one. A description-less tool gives the model nothing to decide on, so it is dropped by the server and independently refused by the importer.\nThere is no A2A equivalent of the MCP builtins allowlist, which is why no built-in declares A2A exposure: without a selection mechanism, declaring one would serve it the moment an operator enables A2A, with nothing to narrow it.\nA gated tool is dropped from both the agent card and the dispatch map, so it is unadvertised and uninvokable rather than merely hidden.\nThe agent card is precomputed at construction. No work happens per discovery request, and the exposure decision cannot drift between what the card advertises and what dispatch will accept.\nThe NATS binding Aspect Value Subject prefix choria.fisk-ai, deliberately inside the existing Choria subject space Discovery subject choria.fisk-ai.discovery.\u003cidentity\u003e Tool subject choria.fisk-ai.tool.\u003cidentity\u003e Streams None. No JetStream, no KV, no consumers Pattern Core request-reply through a micro service Queue group The identity, so processes sharing it share load Request timeout 30 seconds when neither the caller’s context nor the transport config carries a deadline The subject split is a permission boundary. A NATS account can grant publish on choria.fisk-ai.discovery.* without granting choria.fisk-ai.tool.*, which is discovery without execution.\nThe micro service registers a fixed version of 0.0.0. That is service metadata only; the agent card carries the real version, which keeps the transport out of the versioning business.\nTransport errors travel in reply headers rather than the body, because that is what micro does with a service error.\nBack-pressure is structural The semaphore is acquired on the serving goroutine, before a worker is spawned. If the slot were taken inside the worker, intake would continue and requests would pile up in memory. Acquiring first means micro stops reading, so pressure reaches the transport. A test proves a second request never enters until the first releases its slot.\nEach request gets exactly one reply. The replier is single-shot and stays valid after the handler returns, so the spawned worker can answer.\nThe message model There is no separate envelope. Every message is one flat JSON object, because Header is an embedded struct whose fields marshal at the top level. A captured message is therefore fully self-describing outside any transport.\nFraming is filled by two unexported functions. stampRequest mints one id and assigns it to the message id, the request id, and the conversation id, sets sequence to zero, and names the sender. stampReply mints a fresh id, echoes the request and conversation ids, and swaps sender and recipient.\nSequence is always zero on the live paths, because a direct tool or discovery call is not part of a larger task and the transport’s reply inbox handles correlation.\nBlocks synthesize their type discriminator at marshal time from the Go type, so the two can never disagree. An empty block marshals to an error rather than null, and an unrecognized type fails to unmarshal.\nLoad-bearing decision Stamping is header population, not signing. There is no cryptographic signature, MAC, or verification of A2A messages anywhere. Header.Sender and Header.Recipient are unauthenticated assertions used for logging and reply stamping, never for authorization. What stops a forged sender from redirecting a reply is that the replier targets only the inbox the transport supplied; a destination is never taken from the message body. All authentication, authorization, and confidentiality are delegated to NATS: the credentials in the named context, account and subject permissions, and TLS.\nThinkingBlock.Signature is carried for display and audit only and is never replayed into a model across the agent boundary, because a provider-specific signature is not portable.\nSchema validation Eleven embedded schemas, one per protocol id plus a shared definitions file. Both sides validate in both directions, and the transport never decodes anything.\nTwo structural choices carry the weight:\nunevaluatedProperties: false combined with allOf and a header $ref. A plain additionalProperties: false would reject the header fields the reference contributes. unevaluatedProperties accounts for them and still rejects genuinely unknown keys. protocol is a const per file. The schema selected by a protocol id also asserts that id, so a mislabeled body cannot validate against the wrong schema. expectProtocol then re-checks on arrival rather than trusting the subject. The identity pattern is enforced in the schema as well as in config, in the server, in the MCP name check, and in the importer. Since identity keys the NATS subjects, that also prevents subject injection through a sender name.\nMessages are size-capped at 768 KiB before decoding on both sides, chosen to sit under the NATS 1 MiB default with framing headroom. A hostile peer cannot force a large allocation.\nTool input must be an object when present, which is why the client drops a null input: models emit null for no-argument tools, and the server treats absent input as an empty object.\nConsuming another agent’s tools remote_tools names hosts whose tools are imported into the local agent. Naming is globally deterministic rather than first-come: three passes count bare names across all hosts, compute final names, count final names, and only then build.\nThat buys four properties, each tested:\nA bare name is prefixed if a local tool holds it, or if more than one host exposes it, so two hosts sharing a name are prefixed symmetrically. Residual collisions are found by counting, so both colliding tools are skipped rather than one arbitrarily winning. The same inputs produce the same names between runs, which matters because the tool set is fingerprinted for resume. The remote’s own name travels on the wire while the model sees the possibly-prefixed local name. A remote result is reconstructed into the same CommandResult shape a local command returns, so the model sees byte-identical JSON either way, and a non-zero exit code stays a successful result rather than a tool error.\nRemote tools are never confirm-gated locally. functool.New refuses a spec that is both remote and gated, and no local required-argument validation runs on a remote tool, because authority sits with the serving agent and the two ends must not disagree.\nA run is strict and info is lenient. ImportForRun aborts on any host error or collision, because the prompt may depend on the missing tools. DiscoverForInfo warns and still renders local tools so info works offline.\nDiscovery carries no tags. That single fact is why a tag-based include filter on a remote host is warned about and ignored, and a tag-based exclude is rejected at config parse time: an exclude that cannot be honored would silently leave an unwanted tool imported.\nThere is no MCP client The symmetry a reader will assume does not exist. Fisk AI serves over MCP but never consumes MCP. There is no MCP client transport, no mcp_servers config key, and no adapter from a remote MCP tool to a local one; the MCP SDK is imported by exactly one package and its test. The only way a foreign capability becomes a local tool is A2A remote_tools against another Fisk AI agent.\nIn-band versus transport errors The separation is strict on both sides. An application-level failure, whether an unknown tool, a failed command, or a denied call, is always a normal reply with an error flag set. Only a malformed, mistyped, or oversized message is a transport error.\nA Go error returned from an MCP tool handler would become a protocol-level error the client cannot reason about, which is why an unknown tool over A2A is answered in-band too.\nReserved and aspirational The streaming task flow is defined but not wired. Request, Event, Result, ErrorMessage, Cancel, Ack, and all six block types have constructors, schemas, and round-trip tests, but no transport path sends or receives them. The route hint has only discovery and tool operations, and the server registers only those two handlers.\nAlso declared and unused:\nHeader.MustUnderstand is schema-valid and never read. Header.Parent is reserved for multi-hop correlation and never set. Header.Recipient.Instance is schema-supported and never populated. config.RemoteAgent has no field on Config and no reader. It is the placeholder for delegating a whole task, as opposed to importing tools. AgentCard.Description is never set, so discover always renders it absent. TransportConfig.Options exists and is strictly decoded, but the NATS binding has no options at all. The transport registry is complete, but A2ATransport() is hardwired to nats. conns.Option is documented as the extension point for a Choria connection kind; today the memory and session stores take a raw connection from the provider. Next Terminal and events covers the surface an operator actually watches during a run.",
    "description": "Two ways to hand an application's tools to something else, and why one exposes gated tools while the other refuses",
    "tags": [],
    "title": "Serving: MCP and A2A",
    "uri": "/fisk-ai/design/codemap/serving/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "The agent package formats no prose. It emits typed events and lets the caller decide how they look. Two terminal surfaces consume that contract: a line-oriented renderer and a full-screen tview application. A third, structured sink exists for a server that does not yet live in this repository.\nWhere it lives internal/agent/events.go defines the contract, slog_events.go the structured sink. run_events.go is the line UI and run_tui_events.go the full-screen one. internal/tui holds the screen: live.go, viewer.go, prompter.go, splash.go. internal/util supplies sanitization, markdown rendering, terminal detection, the trace, and the run summary.\nThe event contract Events is a typed sink with a three-clause contract: it is called from the single run goroutine, it may be called during teardown, and a per-run sink therefore needs no locking.\nThe payloads carry display decisions rather than rendered text. ToolTrace is the clearest example: it carries both a full display form and a middle-elided short form, plus the presentation and the provider kind. Renderers key suppression off presentation and never off kind.\nAll warning wording lives in one function shared by both surfaces, so the line UI and the full-screen UI cannot drift apart on phrasing. The same is true for tool-result lines and for the tool-call elision function, which the line UI calls with a width measured from stderr so both surfaces cut at the same point.\nWho owns the screen run goroutine single, needs no locks Events sink cli, tcell, or slog QueueUpdateDraw blocks until drawn tview loop sole screen owner drawn frame alt-screen, dev tty stdout and stderr answers to stdout every append is synchronous: the run goroutine waits for the frame, so an event storm throttles the agent, not memory The line UI writes directly. The full-screen UI marshals every mutation onto the loop, which owns the screen. The tview loop runs on the caller’s goroutine. The agent runs on a spawned goroutine with its own recover, and reaches the screen only through the queue.\nBecause the queue waits on a per-call done channel, every append is synchronous: the run goroutine blocks until the loop has applied the closure and drawn. That is the back-pressure mechanism. An event storm throttles the agent to the draw rate instead of growing an unbounded queue.\nA live run has seven goroutines: the tview loop, the agent, a spinner ticker, a signal watcher, a teardown coordinator, a stderr copier, and any notice-expiry timer.\nFour invariants keep a stopped loop from turning into a deadlock or a panic:\nThe loop is never stopped while the run goroutine can still queue, because teardown waits for the run to return first. The ticker is stopped and joined before the run is marked ended, closing the race window before the loop stops. Notice timers are always cancelled before a stop. Flags that cannot go through the loop are atomics: whether the spinner should animate, and whether an explicit leave was requested. The screen wrapper makes finalization idempotent, because tview finalizes on stop and again from its own panic recovery, while the viewer defers one of its own.\nThe one deliberate exception to the queue rule is the initial status refresh, called directly before the loop starts, since queueing onto a loop that has not started would block forever.\nThe widget layout pages: main | help | search | prompt | splash main: Flex(row) header TextView 1 row, only when non-empty view TextView flex, dynamic colors, regions, scrollable, wrap promptTop / promptInput / promptBottom inserted only in chat mode status TextView 1 row, filled bar Overlays are centered pages built from nested flexes. The splash page is added last so it sits on top of everything else.\nThree parallel slices stay index-aligned one to one: the raw lines, their sanitized plain text, and their rendered markup. A folded line still renders inside its search region, so a match still lands on it and the slices do not shift. Search walks the plain text, which is also what the copy key sends, so search stays authoritative over folding and revealing a match unfolds rather than skips.\nNothing renders until the first draw knows the width. A resize re-renders everything, which also re-picks tool-call elision and re-wraps markdown.\nFolding is a global mode rather than per-block state, because a flat text view has no cursor. Tool output folds at any size, since the raw result is rarely worth reading inline. Thinking folds only past a six-row threshold, because a short block saves nothing folded. Row counting hard-wraps at the current width, so a single very long JSON or base64 line counts as the many rows it actually paints.\nOne-shot and chat The TUI gate requires that the TUI is not disabled by flag or config and that both stdin and stdout are terminals. Config no_tui is an absolute veto; the flag can only turn it off.\nChat mode is TUI-only and fails loudly otherwise, with a distinct message for the resume case. A resumed session restores its own chat-ness, peeked from the stored metadata before the run, so --chat need not be re-passed.\nChat mode also starts with thinking folded, on the theory that a chat leans on the answers with reasoning a keystroke away.\nThe input row grows to five rows, lowered further so the transcript keeps at least three, with an overflow marker embedded in its bottom rule. Bracketed paste stays enabled so a multi-line paste reaches the text area verbatim instead of arriving as raw Enter keys that the key capture would read as a submit at the first newline.\nGrowing the input row re-pins the tail, because it steals height with no key pressed.\nConfirmation prompts The full-screen prompter shows a three-button modal with the safe option as button zero, so the escape key’s no-selection result maps to the safe default. The default-deny policy itself lives in the gate, not in the prompter.\nWhile a prompt page is front, every key goes to the widget except the abort key, so an abort is always possible while a prompt is up.\nBlocking recolors the status bar amber, sets a word describing the state, drops any lingering notice so it cannot mask the bar, and rings the bell when enabled. Clearing the blocked state only reverts from blocked, so a terminal state set by teardown after a cancelled prompt is not overwritten.\nThe two-step markup defense Load-bearing decision Every piece of model text follows the same order: sanitize for display, then escape for tview, then apply trusted tags. Escaping before translating ANSI is what stops model output from injecting markup. The same order is used for the prompter body, border titles, splash values, the status bar, and the header. Bracketed literals in trusted text are escaped too, because the bars have dynamic colors enabled.\nSanitization comes in two strengths. The display form strips escape and control sequences while preserving newlines and tabs, and does not truncate. The terminal form collapses to one line and caps runes, and is what command lines and model-supplied keys go through.\nTwo markdown renderers The split is deliberate.\nLine UISanitizes first, so a raw escape in the model's prose cannot set a style that outlives the message. Returns raw markdown when color is off or when the target is not a terminal, so redirected output is ANSI-free. Otherwise renders with automatic style matching the terminal background. Full-screen UINever inspects a terminal. It forces an explicit style and color profile so nothing queries the tty, which the UI owns while its screen is held. A minimum width floor exists because below it the word wrap produces mangled output. What no-color does and does not do --no-color affects markdown only. The per-kind line tags, the blue, green, and amber status bar, the splash accent, and the gray fold placeholders are not suppressed by it.\nThe design compensates by keeping information in words rather than color. The status bar carries a state word so the information survives on a monochrome terminal, fold placeholders say how many lines are hidden and which key expands them, and an errored tool result is prefixed with a word so the failure reads without the color.\nThe full-screen UI binds its screen to the controlling terminal rather than stdout, so stdout stays free for the piped answer.\nSuspend and abort For a checkpointed run, the first leave key requests a graceful suspend and appends a notice. A second press aborts, as does a leave on a non-checkpointed run, an already-ended run, or a run blocked on a prompt.\nAt the input bar the abort key aborts directly, because the loop-boundary suspend flag is useless there: the run is parked gathering a prompt, not in the loop, so it never polls the flag. The graceful leave at the input bar is the end-of-input key instead.\nOutcome classification uses the run’s real result rather than operator intent. A run counts as suspended only if it ended without error and the suspend flag is set, because the run may have completed at the very boundary the operator asked to suspend at.\nAn explicit leave skips the “press q to quit” park entirely. The operator already asked to go, so the answer, statistics, and any resume hint are reprinted to the restored terminal immediately.\nSurviving the alternate screen Standard error is redirected into a buffer for the run’s duration, so an SDK, a library, or a deferred write cannot draw on the alternate screen, and the buffer is flushed to the restored terminal afterwards. Nothing is lost, only deferred. That is also why HTTP debug output goes to a file rather than stderr.\nThe full-screen event sink hoards the answer, the warnings, the rotated session ids, and any panic value and stack, and the command reprints them after teardown: warnings and hints to stderr, the answer through the markdown renderer to stdout, so the run stays pipe-compatible.\nThe panic event deliberately writes nothing into the view, because it fires during unwind and races the teardown.\nThe terminal is always restored. The run defers the restore and then the screen finalization, so the pair unwinds in the right order even through a panic.\nLive counters must agree with the end-of-run summary, so the live view sums per-message usage exactly as the runner sums its statistics, and a resume seeds the bar from the restored counters.\nElapsed time is deliberately absent from the bar: it kept climbing through idle input waits, which read as odd. The summary still reports real latency.\nThe trace file --trace installs a middleware that writes JSON Lines. The file is created exclusively at mode 0600, so an existing path is an error rather than a clobber, and the permissions match the fact that it holds full prompts and responses.\nEvent Contents session Written once: model, config path, version request Id, iteration, attempt, method, URL, body response Same id, status, duration, body error Same id, duration, error text; the error still propagates summary Session, counters, all four token tiers, duration A request and its response share an id. The iteration comes from the agent loop through the context, and the attempt is parsed from the SDK’s retry-count header, so a retry reuses the iteration but gets a new id and an incremented attempt.\nBodies are read non-destructively, with the response buffered and restored even on a read error. A valid JSON body is embedded raw and anything else is wrapped as a JSON string, so a non-JSON body can never corrupt the one-object-per-line format.\nWrites never abort the run. The first failure warns once through an injected sink, which exists because util is a dependency of the agent rather than the other way around. Every method is nil-safe, so the run path wires the tracer unconditionally.\nThe per-kind tool counts are omitted on a resume, so a partial map can never look like it should sum.\nReserved and unused Cache-creation tokens are tracked but never shown on the live bar, so a resumed run’s counters stay whole without crowding the compact bar. They do reach the trace summary and the verbose statistics line. The full-screen sink deliberately drops the no-application notice, with a comment saying the TUI has no place for incidental notes yet and to restore it once a logs pane exists. A logs pane is the named future work. The line UI’s session-rotated handler is a defensive fallback, since chat runs in the full-screen UI. Two slash-command handlers ignore their argument parameter; the signature exists only to fit the command table. The tool-error line kind has no dedicated fold toggle. The tool-output toggle governs it, though the fold notice only ever names thinking or output. The splash card is a single opaque text view rather than nested flexes, because a flex leaves its background unfilled and the transcript bleeds through the gaps. Its width is fixed because it cannot be resized from the before-draw hook, which runs under the application lock where hiding a page would deadlock. Clipboard copy is fire-and-forget over the terminal escape, and the notice wording says what was sent rather than that it landed, because many terminals disable or cap it. Next Reference and map lists the command surface, the packages, and the vocabulary in one place.",
    "description": "One typed event contract, two terminal surfaces, and a screen owned by exactly one goroutine",
    "tags": [],
    "title": "Terminal and events",
    "uri": "/fisk-ai/design/codemap/terminal/"
  },
  {
    "breadcrumb": "Introduction \u003e Design \u003e Code Map",
    "content": "A lookup page. Where the other pages explain reasoning, this one answers “where does that live” and “what does that word mean here”.\nCommand surface Command Purpose fisk run [query...] Runs the agent. The largest flag set: --config, --api-key, --base-url, --http-debug, --no-color, --verbose, --tool-output, --no-tui, --chat, --trace, --checkpoint, --name, --resume, --force, --state-dir fisk session ls Lists checkpointed sessions, newest first. Alias list fisk session show Shows one session, with --transcript for the full-screen viewer. Alias view fisk session rm Removes a checkpointed session. Alias delete fisk info Explains a config without contacting a model: tools, tags, confirm column, globals, prompt fisk knowledge index Builds or updates the index, incrementally by content hash fisk knowledge watch Watches the configured paths and re-indexes on change fisk knowledge search Retrieves from the index for tuning, printing citations and snippets fisk knowledge match Lists every document containing the words, as a complete set. Aliases enumerate, which fisk knowledge words Lists the vocabulary of the index with document counts. Aliases vocab, terms fisk knowledge show Prints one chunk verbatim, resolving a citation fisk knowledge rm Removes specific indexed sources by path fisk knowledge reset Wipes the entire index fisk knowledge sources Lists indexed files with chunk counts and last-indexed time fisk knowledge doctor Checks the index, its full-text integrity, and when configured the embeddings server fisk knowledge rebuild Rebuilds the search indexes from the stored text, without re-embedding fisk knowledge stats Prints the tier banner and index counts and sizes fisk mcp Serves the tools over MCP on streamable HTTP fisk a2a Serves the tools to other agents over NATS fisk serve Hosts the agent behind the surfaces its configuration enables, taking queued work: --config, --workers, --state-dir, --work-dir, --api-key, --base-url, --no-telemetry, --verbose fisk discover \u003cagent\u003e Discovers a remote agent and prints its tools The knowledge command carries rag and k as aliases.\nSource map Line counts exclude tests, at the snapshot commit.\nPackage Files Lines Role Page main (repo root) 15 3869 The CLI: command registration, flags, signals, and both terminal renderers Configuration config 1 1281 The whole configuration surface, parsing, defaulting, and validation Configuration internal/agent 5 2975 Run setup, the loop, hooks, and the events contract The agent loop internal/toolkit 8 637 Neutral tool contracts: Tool, Kind, Presentation, Prompter Tools internal/toolkit/fisk 5 1384 Introspection, filtering, argv construction, execution Tools internal/toolkit/builtin 4 1299 The human-in-the-loop, memory, and knowledge tools Tools internal/toolkit/functool 3 446 The generic function-tool backend Tools internal/llm 6 423 The provider-neutral message model and registry Providers internal/llm/anthropic 3 534 The only provider, and the only SDK importer Providers internal/rag 16 4905 The knowledge index: chunking, embedding, hybrid search, enumeration, vocabulary, watching Knowledge internal/memory 6 513 The memory contract and shared rules Memory internal/memory/file 3 316 One markdown file per memory Memory internal/memory/jetstream 1 487 One KV entry per memory, with a read-before-update guard Memory internal/runstate 7 986 Records, the fold, the fingerprint, and the schemas Sessions and replay internal/runstate/file 3 472 A JSON-lines journal per run, with an advisory lock Sessions and replay internal/runstate/jetstream 1 627 One record per subject, with a tail fence Sessions and replay internal/a2a 13 1627 The agent-to-agent protocol engine and its transport contract Serving internal/a2a/nats 1 242 The one live transport binding Serving internal/mcpserver 1 638 The whole MCP serving mode Serving internal/tui 4 2740 The full-screen surface: live view, viewer, prompter, splash Terminal and events internal/util 8 1149 Sanitization, markdown, the confirm gate, the tracer, run stats Terminal and events internal/remotetools 1 315 Import policy: discovery, filtering, deterministic naming Serving internal/conns 1 89 Connection establishment and ownership Serving internal/agenttest 8 976 Fakes proving each contract is implementable from outside the package The root main package holds one file per command plus the two event renderers: main.go, run_command.go, run_events.go, run_tui_events.go, session_command.go, resume_replay.go, info_command.go, rag_command.go, rag_match.go, rag_words.go, rag_watch.go, mcp_command.go, a2a_command.go, discover_command.go, remote_tools.go.\nKey types Type Package What it is agent.Options internal/agent Everything a caller supplies for one run, including every injection point agent.Events internal/agent The typed display sink; the package decides what happened, the caller how it looks agent.Hooks internal/agent Eight optional callbacks on the run goroutine toolkit.Tool internal/toolkit The four-method interface every tool kind satisfies toolkit.Kind internal/toolkit The accounting axis: who provides the tool toolkit.Presentation internal/toolkit The visibility axis: how a call is shown toolkit.Prompter internal/toolkit The only sanctioned path to an operator toolkit.CommandResult internal/toolkit The JSON shape a command-ish tool returns, local or remote llm.Provider internal/llm The one place a concrete SDK is spoken on the request path llm.Message internal/llm The neutral message model, and also the on-disk format runstate.Store internal/runstate Describe, create, open, load, list, delete for run journals runstate.RunState internal/runstate The folded, resumable state runstate.Fingerprint internal/runstate The configuration a journal was written against memory.Store internal/memory Describe, list, read, write, delete over durable model-written notes rag.Store internal/rag One handle over the single SQLite index file a2a.Transport internal/a2a Round-trip, serve, describe, close; moves bytes and never decodes a2a.Header internal/a2a Framing embedded flat into every message config.Config config The whole YAML surface, read through nil-safe accessors Constants worth knowing Value Meaning 10 Tool count at which deferral and tool search engage 200000 / 50 / 120s Default token budget, iteration budget, and per-call timeout 8192 / 16384 Default max output tokens, and the value used when thinking is on 2 / 30s Default concurrency and per-call timeout for both serving modes 64 KiB / 32 KiB Head and tail kept from a tool’s output before truncation 16 MiB Introspection output cap, rejected rather than truncated 768 KiB A2A message size cap, sitting under the NATS 1 MiB default 64 KiB / 1024 Memory content cap and entry count cap 69600 Memory entry cap including frontmatter, the nats kv add figure 1200 / 1500 Knowledge chunk target and maximum in bytes 50 / 20 / 6000 Search fanout per tier, result ceiling, and default injected-token budget 8080 / 127.0.0.1 Default MCP port and address choria.fisk-ai The A2A NATS subject prefix Glossary agentOne configuration driving one model over one tool set. Named by its identity. identityThe agent's name. Also the NATS queue group and the namespace for on-disk state. toolAnything the model may call: a wrapped leaf command, a built-in, an imported remote tool, or a caller-injected custom tool. kindWho provides a tool. Used for accounting and log tokens, never for display suppression. presentationHow a tool call is displayed. Used for suppression, never for accounting. confirm gateThe operator approval checkpoint on a command the model chose. Default deny. HITLHuman in the loop: the built-in tools with which the model asks the operator a question. deferralWithholding full tool definitions so the model finds them through tool search instead. checkpointA run that writes a durable journal and can therefore be suspended and resumed. sessionOne journal, identified by a run id. A context reset rotates to a new one. journalThe append-only record stream for one session. foldThe pure function turning a record stream back into a resumable state. fingerprintThe hashed configuration a journal was written against, checked before a resume. memoryShort markdown notes the model writes for its future self. memory indexThe key-and-description listing injected into the system prompt at startup. knowledgeThe operator-owned document index the model searches. RAG is the technique; knowledge is the feature. tierWhether knowledge search is lexical only or hybrid, always reported on its own line. citationA knowledge reference of the form path#ordinal. Ordinals shift after a reindex. A2AAgent to agent: the NATS protocol for serving tools to, and importing tools from, another Fisk AI agent. agent cardThe discovery reply describing an agent's name, version, and exposed tools. remote toolAnother agent's tool imported into this one, presented to the model as if it were local. elicitationThe MCP mechanism used to request approval from a client when there is no local operator. Reserved and unused, at a glance Collected from the subsystem pages so a reader can see the shape of the unfinished edges in one place.\nItem Status remote_agents config key and its type Declared, read by nothing. remote_tools is the working feature The A2A streaming task flow Fully defined, schema-validated, round-trip tested; no transport path sends or receives it Header.MustUnderstand, Header.Parent, Recipient.Instance Declared and schema-valid, never set or read The runstate JSON schema validator Compiled and tested, wired into no read or write path llm.Caps.MaxOutputTokens Declared, never read; nothing clamps the per-call cap Three exported anthropic codec functions No non-test callers; migration residue agent.SlogEvents No in-tree consumer; exists for a job runner that does not exist yet Most of agent.Options Embedder-only; the CLI sets fewer than half its fields functool confirm and validation specs No production users; for embedder-supplied tools IndexOptions.Extensions A real extension point no caller sets documents.title in the knowledge schema Written on every upsert, read on no path A second transport, provider, or MCP client The registries are complete; no second implementation exists, and there is no MCP client at all Next For operator-facing documentation rather than design, see the Agents, MCP Servers, Knowledge, and Reference sections.",
    "description": "The command surface, the source map by package, key types, and the vocabulary",
    "tags": [],
    "title": "Reference and map",
    "uri": "/fisk-ai/design/codemap/reference/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "",
    "description": "",
    "tags": [],
    "title": "Categories",
    "uri": "/fisk-ai/categories/"
  },
  {
    "breadcrumb": "Introduction",
    "content": "",
    "description": "",
    "tags": [],
    "title": "Tags",
    "uri": "/fisk-ai/tags/"
  }
]
